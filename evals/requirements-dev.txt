# Always-on test dependencies (deterministic harness unit tests).
# The behavioural eval layer additionally needs evals/requirements-eval.txt,
# but only when RUN_EVALS=1.
pytest>=9,<10
PyYAML==6.0.3  # tests/test_spec_inheritance.py + scripts/check_spec_inheritance.py
jsonschema>=4.21,<5  # tests/test_validate_schemas.py + scripts/validate_schemas.py
