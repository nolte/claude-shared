---
audit-type: link-validation
repo-revision: dac0375
created: 2026-09-13
scope: external+offline
status: closed
---

# Link Validation Report — 2026-Q3

## Scope

- Date: 2026-09-13; trigger: first recorded external run, for calendar quarter 2026-Q3, remediation of spec-drift-audit 2026-Q4 findings F88 and F90 (#587)
- Git revision: `dac0375f8e7aa201496253940c64fa003e15dcd0`
- Performed by: the `link-rot-scanner` agent against the primary checkout on `develop`
- Commands: `python3 scripts/check_links.py --external --format json` (run twice, byte-identical output), `python3 scripts/check_links.py --offline --format json`, and the `--internal` and `--cross-tree` slices separately
- Config: no `.linkcheck.toml` at audit time, so checker defaults applied: `timeout=10s`, `retries=2`, `concurrency=8`

## Summary

- Critical: 122 (2 on tracked pages, 120 on generated catalog pages)
- Warning: 1
- Suggestion: 0
- Info: 264 (8 on tracked pages, 256 on generated catalog pages)

**Verdict:** the offline slice (internal, anchor, cross-tree) is clean. On tracked pages the external slice found one bot-hostile 403, one permanent redirect, and eight branch-ref permalinks. The 120 catalog 404s are one generator defect, not 120 dead links.

## Findings

### Critical

- [x] `docs/de/guides/image-generation.md:31` and `docs/en/guides/image-generation.md:31` → `https://dash.cloudflare.com/sign-up`: `HTTP 403` reproducing across retries on a host outside the built-in soft-403 list. The page is the Cloudflare sign-up that serves browsers and refuses automated probes.
  - Fix: `.linkcheck.toml` lists `dash.cloudflare.com` under `soft_403`, so the answer classes as rate-limited, not dead.
- [x] 120 findings on the generated `docs/{en,de}/{skills,agents}/` pages: `HTTP 404` on `github.com/nolte/claude-shared/blob/main/{skills,agents}/…`. `scripts/docs/gen_catalog.py` built source links without the `plugins/<name>/` directory of the carved-out plugins.
  - Fix: handed to #593, which makes the generator prefix the plugin directory.

### Warning

- [x] `README.md:86` → `https://docs.claude.com/en/docs/claude-code/plugins`: permanent redirect (301) to `https://code.claude.com/docs/en/plugins`.
  - Fix: the link now points at the redirect target.

### Info

- [ ] Eight branch-ref permalinks on tracked pages (`docs/{de,en}/by-task.md:31`, `docs/{de,en}/portfolio/index.md:104`, `docs/{de,en}/references/specs/index.md:15`, `docs/de/using.md:40`, `docs/en/using.md:38`), linking `blob/main` or `blob/develop`. Link quality only, never failing; they point at living documents on purpose, so pinning them to a tag would freeze them. Left as they are.
- [ ] 256 branch-ref permalink notices on the generated catalog pages, same reasoning.

## Processing log

- 2026-09-13 — audit run by `link-rot-scanner` at `dac0375`.
- 2026-09-13 — Critical (Cloudflare) resolved with the `soft_403` entry, Warning resolved in `README.md`, the catalog 404 class handed to #593; Info findings kept deliberately (#587).
