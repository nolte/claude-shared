---
audit-type: spec-drift-audit
period: 2026-Q2
trigger: operator-invoked full-scope audit (every skill/agent vs specs and every spec vs implementation)
target: nolte/claude-shared
repo-revision: b91b67b4578779f9a5784f2e7b4e8d8ccc9ce838
created: 2026-05-30
status: open
scope: all 62 spec topics under spec/ with non-empty Requirements/Acceptance Criteria
tools-executed: [multi-agent fan-out audit (Sonnet review + adversarial Sonnet verify), deterministic dedup, Opus synthesis/triage, completeness critics]
severity-source: spec/claude/review-plan §Severity scale
---

# Spec-Drift Audit — 2026-Q2 (full scope)

## Scope

Backward (spec → implementation) reconciliation of all **62** spec topics under `spec/` carrying a non-empty `## Requirements` or `## Acceptance Criteria` section, audited against this repository's implementation surface (`skills/`, `agents/`, `.github/`, `.claude/`, `Taskfile.yml`, `mkdocs.yml`, docs, workflows) at revision `b91b67b`. Each acceptance criterion was scored `pass` / `fail` / `blocked` / `not-applicable`; every `fail`/`blocked` carries a finding. Consumer-facing specs (ansible/*, design/*, frontend/webview-ui-optimization, tools/gemini-image-generation, post-*) whose implementation lives in downstream repos are scored `not-applicable` here with a reason. The complementary forward direction (artefact → spec binding) lives in `.audits/skills-agents-sweep/2026-05-30-full-audit.md`. Findings below are the **raw post-dedup set**; the genuine-versus-over-flagged triage and the systemic recommendation live in the sweep report's executive summary and roadmap.

## Summary

- **Critical**: 66
- **Warning**: 131
- **Suggestion**: 10
- **Info**: 13
- Per-criterion results across all specs: pass=806, fail=226, blocked=3, not-applicable=250

**Go/no-go**: NO-GO for a clean bill of health — 66 Critical (unimplemented MUST) findings remain open and must each receive a documented decision (adjust implementation / adjust spec / open question) per spec-drift-audit §Feedback loop. This audit record is the traceable history entry for 2026-Q2.

## Per-spec results

| Spec | Overall | pass | fail | blocked | n/a | Findings (C/W/S/I) | Binding artefacts |
|---|---|--:|--:|--:|--:|---|---|
| `ansible/playbook-development` | not-applicable | 0 | 0 | 0 | 31 | 0/0/0/0 | /home/nolte/repos/github/claude-shared/spec/ansible/playb… |
| `ansible/role-development` | not-applicable | 0 | 0 | 0 | 13 | 0/0/0/0 | spec/ansible/role-development/en.md, spec/ansible/role-de… |
| `claude/agent-management` | fail | 23 | 3 | 0 | 1 | 1/3/0/0 | /home/nolte/repos/github/claude-shared/agents/audience-do… |
| `claude/agent-review` | fail | 10 | 2 | 0 | 4 | 0/3/0/0 | /home/nolte/repos/github/claude-shared/skills/agent-revie… |
| `claude/permission-allowlist` | fail | 15 | 4 | 0 | 1 | 1/3/0/0 | /home/nolte/repos/github/claude-shared/skills/permission-… |
| `claude/plugin-scoping` | fail | 23 | 2 | 0 | 0 | 2/0/1/0 | /home/nolte/repos/github/claude-shared/.claude-plugin/plu… |
| `claude/png-to-transparent-svg` | fail | 15 | 2 | 0 | 0 | 0/2/0/0 | /home/nolte/repos/github/claude-shared/agents/png-to-tran… |
| `claude/research-triangulate` | fail | 1 | 16 | 0 | 0 | 5/4/0/1 | spec/claude/research-triangulate/en.md, spec/claude/resea… |
| `claude/resumable-work` | fail | 4 | 3 | 0 | 8 | 0/2/0/0 | /home/nolte/repos/github/claude-shared/spec/claude/resuma… |
| `claude/review-plan` | fail | 3 | 5 | 0 | 0 | 0/6/0/2 | .audits/portfolio-inflight/2026-05-23.md, .audits/portfol… |
| `claude/skill-agent-catalog` | fail | 59 | 2 | 0 | 0 | 1/1/0/0 | scripts/docs/gen_catalog.py, docs/catalog-sources.yml, mk… |
| `claude/skill-management` | fail | 29 | 3 | 0 | 0 | 0/3/0/0 | skills/skill-management/SKILL.md, skills/skill-management… |
| `claude/skill-review` | fail | 5 | 3 | 2 | 0 | 2/4/0/1 | skills/skill-review/SKILL.md, skills/skill-review/templat… |
| `claude/skill-vs-agent` | fail | 6 | 3 | 0 | 2 | 2/1/1/0 | /home/nolte/repos/github/claude-shared/spec/claude/skill-… |
| `claude/skills-agents-sweep` | fail | 8 | 2 | 0 | 4 | 0/3/0/2 | skills/skills-agents-sweep/SKILL.md, skills/skills-agents… |
| `design/corporate-design-colors` | not-applicable | 2 | 0 | 0 | 51 | 0/0/0/0 | /home/nolte/repos/github/claude-shared/spec/design/corpor… |
| `design/graphic-prompt-authoring` | pass | 9 | 0 | 0 | 0 | 0/1/0/1 | /home/nolte/repos/github/claude-shared/agents/graphic-pro… |
| `frontend/webview-ui-optimization` | pass | 6 | 1 | 0 | 1 | 0/0/0/0 | /home/nolte/repos/github/claude-shared/skills/webview-ui-… |
| `portfolio/portfolio-inflight-management` | fail | 8 | 4 | 1 | 0 | 2/3/0/0 | skills/portfolio-inflight-triage/SKILL.md, agents/portfol… |
| `portfolio/portfolio-management` | fail | 8 | 7 | 0 | 0 | 5/3/0/0 | /home/nolte/repos/github/claude-shared/skills/portfolio-a… |
| `portfolio/tech-stack` | fail | 11 | 3 | 0 | 2 | 2/2/0/0 | /home/nolte/repos/github/claude-shared/portfolio/tech-sta… |
| `portfolio/tech-stack-discovery` | fail | 9 | 2 | 0 | 3 | 0/2/0/0 | spec/portfolio/tech-stack-discovery/en.md, spec/portfolio… |
| `project/audience-identification` | fail | 16 | 2 | 0 | 2 | 0/2/0/0 | /home/nolte/repos/github/claude-shared/AUDIENCES.md, /hom… |
| `project/blog-author` | fail | 21 | 3 | 0 | 3 | 0/3/0/0 | /home/nolte/repos/github/claude-shared/skills/blog-author… |
| `project/blog-author-trigger` | fail | 10 | 1 | 0 | 1 | 0/1/1/0 | skills/blog-author-trigger/SKILL.md, skills/blog-author-t… |
| `project/branching-model` | fail | 9 | 3 | 0 | 0 | 1/2/0/1 | /home/nolte/repos/github/claude-shared/.github/workflows/… |
| `project/code-security-audit` | fail | 18 | 1 | 0 | 0 | 1/1/0/0 | /home/nolte/repos/github/claude-shared/agents/code-securi… |
| `project/continuous-improvement` | fail | 9 | 6 | 0 | 3 | 3/3/0/0 | /home/nolte/repos/github/claude-shared/spec/project/conti… |
| `project/cookiecutter-template-authoring` | pass | 1 | 0 | 0 | 6 | 0/1/1/0 | /home/nolte/repos/github/claude-shared/agents/cookiecutte… |
| `project/dependency-audit` | fail | 11 | 13 | 0 | 12 | 1/10/0/1 | /home/nolte/repos/github/claude-shared/skills/dependency-… |
| `project/diagram-opportunity` | fail | 21 | 2 | 0 | 0 | 2/0/0/0 | /home/nolte/repos/github/claude-shared/agents/diagram-opp… |
| `project/docs-audience-tracks` | fail | 7 | 9 | 0 | 0 | 3/4/0/0 | skills/docs-audience-tracks-apply/SKILL.md, AUDIENCES.md,… |
| `project/docs-freshness` | fail | 13 | 9 | 0 | 2 | 0/6/0/0 | /home/nolte/repos/github/claude-shared/agents/docs-freshn… |
| `project/docs-multilingual-authoring` | fail | 13 | 5 | 0 | 2 | 0/2/0/1 | /home/nolte/repos/github/claude-shared/spec/project/docs-… |
| `project/feature` | fail | 9 | 5 | 0 | 1 | 0/5/0/0 | skills/feature-decompose/SKILL.md, agents/feature-consist… |
| `project/github-issue-templates` | fail | 21 | 1 | 0 | 0 | 1/0/0/0 | /home/nolte/repos/github/claude-shared/.github/ISSUE_TEMP… |
| `project/i18n-completeness` | fail | 25 | 2 | 0 | 0 | 1/2/0/0 | /home/nolte/repos/github/claude-shared/agents/i18n-comple… |
| `project/lektorat` | fail | 28 | 4 | 0 | 0 | 0/1/0/1 | /home/nolte/repos/github/claude-shared/skills/lektorat-ap… |
| `project/mermaid-diagrams` | fail | 8 | 3 | 0 | 1 | 1/2/0/0 | /home/nolte/repos/github/claude-shared/skills/mermaid-dia… |
| `project/mission` | fail | 14 | 1 | 0 | 0 | 0/1/2/0 | /home/nolte/repos/github/claude-shared/project/mission.md… |
| `project/mkdocs-structure` | fail | 18 | 4 | 0 | 2 | 4/2/0/0 | /home/nolte/repos/github/claude-shared/mkdocs.yml, /home/… |
| `project/parallel-working-copies` | fail | 12 | 1 | 0 | 3 | 1/1/0/0 | /home/nolte/repos/github/claude-shared/CLAUDE.md, /home/n… |
| `project/post-audience-communication` | not-applicable | 3 | 0 | 0 | 25 | 0/0/0/0 | /home/nolte/repos/github/claude-shared/spec/project/post-… |
| `project/post-writing-style` | pass | 2 | 0 | 0 | 18 | 0/0/0/0 | /home/nolte/repos/github/claude-shared/skills/blog-author… |
| `project/project-structure` | fail | 33 | 3 | 0 | 3 | 1/3/0/0 | /home/nolte/repos/github/claude-shared/README.md, /home/n… |
| `project/prose-style` | fail | 9 | 5 | 0 | 6 | 2/4/0/0 | /home/nolte/repos/github/claude-shared/.vale.ini, /home/n… |
| `project/pull-request-workflow` | fail | 14 | 3 | 0 | 1 | 4/1/0/0 | /home/nolte/repos/github/claude-shared/.github/pull_reque… |
| `project/quality-gate` | fail | 25 | 5 | 0 | 2 | 1/3/1/0 | skills/quality-gate/SKILL.md, Taskfile.yml, .github/workf… |
| `project/readme-structure` | fail | 15 | 2 | 0 | 0 | 0/3/0/0 | /home/nolte/repos/github/claude-shared/README.md, /home/n… |
| `project/release-artifact` | fail | 8 | 2 | 0 | 2 | 0/2/0/0 | /home/nolte/repos/github/claude-shared/skills/sprint-revi… |
| `project/release-automation` | fail | 35 | 4 | 0 | 1 | 2/4/0/0 | .github/workflows/release-publish.yml, nolte/gh-plumbing:… |
| `project/release-notes-audience-analysis` | fail | 0 | 19 | 0 | 5 | 1/2/0/0 | /home/nolte/repos/github/claude-shared/AUDIENCES.md, /hom… |
| `project/release-skill-layer` | fail | 10 | 3 | 0 | 0 | 1/3/0/0 | /home/nolte/repos/github/claude-shared/skills/release-not… |
| `project/roadmap` | fail | 24 | 1 | 0 | 0 | 0/1/0/0 | spec/project/roadmap/en.md, project/roadmap.md, project/g… |
| `project/spec-drift-audit` | fail | 16 | 9 | 0 | 1 | 6/1/1/1 | spec/project/spec-drift-audit/en.md, spec/project/spec-dr… |
| `project/spec-driven-development` | fail | 7 | 7 | 0 | 2 | 0/3/0/0 | spec/project/spec-driven-development/en.md, spec/project/… |
| `project/spec-readiness` | fail | 15 | 5 | 0 | 2 | 2/1/0/1 | agents/spec-readiness-reviewer.md, .audits/spec-readiness… |
| `project/sprint` | fail | 21 | 1 | 0 | 1 | 1/0/0/0 | skills/sprint-plan/SKILL.md, skills/sprint-execute/SKILL.… |
| `project/test-case-derivation` | fail | 17 | 3 | 0 | 0 | 0/2/1/0 | /home/nolte/repos/github/claude-shared/agents/test-case-e… |
| `project/workflow-health` | fail | 11 | 4 | 0 | 1 | 0/2/1/0 | /home/nolte/repos/github/claude-shared/skills/workflow-he… |
| `project/yaml-json-schema` | fail | 3 | 8 | 0 | 5 | 3/1/0/0 | /home/nolte/repos/github/claude-shared/skills/yaml-json-s… |
| `tools/gemini-image-generation` | not-applicable | 0 | 0 | 0 | 16 | 0/0/0/0 | — |

## Findings

_Raw post-dedup findings, grouped by severity; the `[spec-slug.shorthand]` prefix traces each back to the source spec requirement._

### Critical

- [ ] [branching-model.branch-protection-main-no-push] main branch is not protected on GitHub. The GitHub API returns HTTP 404 for the branch-protection endpoint (protected: false). Additionally, .github/settings.yml sets restrictions: null for main, meaning no push-restriction actor is configured. Any authenticated user can push directly to main, violating the MUST that only release automation may write to main.
      Where: /home/nolte/repos/github/claude-shared/.github/settings.yml lines 38-44; live GitHub branch protection state for nolte/claude-shared:main.
      Fix: In .github/settings.yml, replace restrictions: null under the main protection block with a restrictions block that allows only the nolte-portfolio-app GitHub App (matching the pattern declared in gh-plumbing commons-settings.yml for 'master'). Verify the Probot Settings app is installed on the repository so the rule is actually enforced on GitHub, then confirm via `gh api repos/nolte/claude-shared/branches/main/protection` that the response is no longer 404..
      Verify: gh api repos/nolte/claude-shared/branches/main/protection --jq '.restrictions' returns an object with apps: [nolte-portfolio-app]; gh api repos/nolte/claude-shared/branches/main --jq '.protected' returns true..

- [ ] [claude/agent-management.tool-access-read-only-bash-narrow-exception] webview-ui-expert declares Bash in its tools list and explicitly describes itself as a read-only agent ('Read-only: never edits or runs destructive commands'), but the agent body does not contain the required ## Read-only Bash justification section. The spec states: 'without the section, Bash on a read-only agent stays a Critical.'
      Where: /home/nolte/repos/github/claude-shared/agents/webview-ui-expert.md — frontmatter tools field (line 5) and agent body (no justification section found).
      Fix: Add a ## Read-only Bash justification section to the agent body that (1) names the exact subset of read-only Bash commands the agent invokes (e.g. git rev-parse --is-inside-work-tree, grep/find equivalents used in phases 1–3), and (2) explicitly states that writes, network mutations, package installs, and file edits are forbidden. The existing 'Tool restriction is load-bearing' paragraph in the ## Why this is an agent section does not satisfy the named-section requirement; a standalone ## Read-only Bash justification section is required..
      Verify: grep '## Read-only Bash justification' /home/nolte/repos/github/claude-shared/agents/webview-ui-expert.md should return a match; the section must name specific allowed Bash commands and contain an explicit prohibition on writes..

- [ ] [claude/plugin-scoping.namespace-naming-coherence-must-consistent] Two skills — 'spec' and 'yaml-json-schema' — use bare-noun / noun-compound names that do not follow the declared verb-noun convention. The plugin-scoping spec §Namespace and naming coherence MUST requires one naming convention for every skill and agent, citing this plugin's verb-noun choice. Neither deviation is documented as an approved exception in plugin-scoping or skill-management.
      Where: /home/nolte/repos/github/claude-shared/skills/spec/SKILL.md (name: spec) and /home/nolte/repos/github/claude-shared/skills/yaml-json-schema/SKILL.md (name: yaml-json-schema).
      Fix: Either rename both skills to a verb-noun form (e.g., spec → spec-manage or spec-author; yaml-json-schema → schema-author or schema-validate) with a deprecation alias period per skill-vs-agent §Portfolio-wide consistency, OR add a documented exception note in skill-management §Frontmatter validation listing these two names alongside the rationale (e.g., 'these predate the verb-noun rule and renaming would be a breaking change for consumers')..
      Verify: grep -h '^name:' skills/*/SKILL.md | sort — all names must follow the same compound-action pattern (verb-noun or the documented alternative). Any exception must appear explicitly in skill-management §Frontmatter validation..

- [ ] [claude/plugin-scoping.namespace-naming-coherence-must-consistent] Agents follow a distinct object-role naming convention (code-security-reviewer, spec-readiness-reviewer, dependency-audit-scanner, etc.) that is internally consistent but differs from the verb-noun convention that plugin-scoping §Namespace and naming coherence MUST cites as the single plugin-wide rule. Agent-management specifies only 'ASCII kebab-case', not a verb-noun or object-role pattern, leaving the agent surface's convention undocumented and creating a cross-artifact-type naming inconsistency.
      Where: /home/nolte/repos/github/claude-shared/agents/ (all 26 agent files); /home/nolte/repos/github/claude-shared/spec/claude/agent-management/en.md (§Frontmatter validation, no naming-pattern rule); /home/nolte/repos/github/claude-shared/spec/claude/plugin-scoping/en.md (§Namespace and naming coherence).
      Fix: Add a naming-convention note to spec/claude/agent-management/en.md §Frontmatter validation that records the object-role pattern (noun-role: <subject>-<role-noun>) as the nolte-shared plugin's chosen agent naming convention — parallel to the verb-noun note in skill-management. Then update plugin-scoping §Namespace and naming coherence to state that the plugin's 'one convention per artifact type' rule means verb-noun for skills and object-role for agents, satisfying the spirit of intra-collection consistency without requiring a full rename..
      Verify: After the spec update: grep -h '^name:' agents/*.md | sort — all names follow object-role. The agent-management spec has a parallel naming note to the one in skill-management §Frontmatter validation. plugin-scoping §Namespace and naming coherence references both conventions..

- [ ] [code-security-audit.output-severity-vocabulary] The Step 3 report template uses a P0/P1/P2/P3 severity scale ('## P0 — critical', '## P1 — high', '## P2 — medium', '## P3 — low / hardening', 'Sort by severity (P0 → P3)'), which both the spec §Output MUST and the referenced spec/claude/review-plan/ §Severity scale MUST NOT explicitly prohibit. The mandatory vocabulary is Critical/Warning/Suggestion/Info in Title Case, verbatim.
      Where: /home/nolte/repos/github/claude-shared/agents/code-security-reviewer.md lines 92–108.
      Fix: Replace the four heading lines and the sort instruction so the template uses '### Critical', '### Warning', '### Suggestion', '### Info' subsection headings (matching spec/claude/review-plan/ §Findings format) and remove all P0/P1/P2/P3 references from the report template and the sort instruction..
      Verify: grep -n 'P0\|P1\|P2\|P3' agents/code-security-reviewer.md returns no matches; grep -n 'Critical\|Warning\|Suggestion\|Info' returns the four heading lines in the template..

- [ ] [continuous-improvement.loop-specialist-dispatch-notation] The Q2 2026 spec-drift audit (.audits/spec-drift/2026-Q2.md) resolved all 14 findings inline by the generalist without noting either a dispatched specialist or an explicit 'no matching specialist exists' note for any finding. AC-3 requires that no in-scope finding from the most recent spec-drift run is recorded as 'no specialist considered'. The spec-drift-audit skill does not prompt for these fields, so the next quarterly audit will repeat the same omission.
      Where: /home/nolte/repos/github/claude-shared/.audits/spec-drift/2026-Q2.md — findings C-1 through W-4 all show -> RESOLVED inline.
      Fix: Extend skills/spec-drift-audit/SKILL.md §audit operation step 5 (per-criterion check) to include: after recording each finding's disposition, the auditor MUST note either (a) the specialist dispatched (name + subagent_type) or (b) 'no matching specialist — generalist handled'. Add this as a mandatory field in the audit template. Also extend the update operation to carry the specialist notation forward into the Decisions section..
      Verify: The next spec-drift audit artifact must have, for every finding entry, either a 'Dispatched specialist: ...' line or 'No matching specialist: generalist remediation' — confirmed by reading the .audits/spec-drift/ artifact after the audit runs..

- [ ] [continuous-improvement.traceability-finding-source] 1 of the last 10 in-scope remediation PRs carry the required originating-finding-source field in Risk/rollout notes. The spec MUST requires the PR to be traceable back to its triggering audit entry. Without this field the audit → fix → audit loop is broken.
      Where: Risk/rollout notes section of the same 10 PRs — only PR #228 has a partial reference to a spec-drift source.
      Fix: Same fix as F1: pre-populate 'Originating source: <audit-source name + link or artifact path>' in every remediation PR opened via a triage skill. Extend the pull-request-create skill's body template or the triage skills' pre-PR dispatch step to prompt for this field before finalising the PR body..
      Verify: After the fix, grep the Risk/rollout notes of 10 consecutive in-scope remediation PRs for 'Originating source:' — all 10 must have the field..

- [ ] [continuous-improvement.traceability-specialist-field] 0 of the last 10 in-scope remediation PRs carry the required specialist field in Risk/rollout notes. The spec MUST requires every remediation PR to record either the dispatched specialist or the literal phrase 'no matching specialist existed and a generalist handled it'. This gap prevents portfolio-level coverage analysis from PR history alone. Affected sample: PRs #160 (stale-pin fix), #162 (stale-pin fix), #205, #207 (prose-vocab), #223 (phantom-agent), #228, #231 — all merged after both the spec (2026-04-23) and the workflow-health-triage skill (2026-05-09) were in place.
      Where: Risk/rollout notes section of merged PRs #160, #162, #205, #207, #218, #223, #228, #231 and more — none carry the specialist field.
      Fix: For each future in-scope remediation PR, the dispatching triage skill (workflow-health-triage or continuous-improvement-triage) MUST pre-populate two lines in Risk/rollout notes before the PR is created: (1) 'Dispatched specialist: <subagent_type literal>' or 'Dispatched specialist: no matching specialised agent — generalist remediation'; (2) 'Originating source: <audit source name + link>'. For the ~38 already-merged PRs, open a tracking Issue listing all missing entries so the gap is visible in audit history even if the PRs cannot be amended..
      Verify: Run the last 10 in-scope remediation PRs through `gh pr view <n> --json body` and grep Risk/rollout notes for both required lines. All 10 must have them..

- [ ] [dependency-audit.audit-artifact-persist] The skill has no operation step to persist the audit result. The spec MUST says every full audit result must be persisted as a commit, issue, or file. Steps 0-6 cover detection through follow-up offers but none writes or commits the artifact. The canonical path .audits/dependency-audit/dependencies-YYYY-Q<n>.md is not mentioned anywhere in the skill.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Operations section (no save-artifact step between Step 5 and Step 6).
      Fix: Add a Step 5.5 (or renumber) titled 'Persist the audit artifact'. It should: (1) propose writing the report to .audits/dependency-audit/dependencies-YYYY-Q<n>.md by default, or offer a GitHub issue labelled security-audit as an alternative, (2) include all spec-required fields in the persisted document (see artifact-fields finding below), (3) ask the operator to confirm before writing, then commit the file..
      Verify: After update, invoke the skill on a test repo and confirm the artifact appears at .audits/dependency-audit/dependencies-YYYY-Q<n>.md (or as a GitHub issue). Run the spec-drift-audit on dependency-audit and confirm the persist criterion passes..

- [ ] [docs-audience-tracks.developer-docs-content-contract] The developer-docs tech-stack MUST block is absent. The spec requires a page or stable H2 section listing the project's languages, frameworks, runtimes, libraries, and external services with pinning intent, referencing or including from spec/portfolio/tech-stack/ rather than restating. No such block exists in docs/en/ outside the skill catalog pages (which describe tools in the plugin, not the project's own stack).
      Where: docs/en/ — no tech-stack page or section exists outside skill/agent catalog pages.
      Fix: Add a tech-stack block (dedicated page or H2 in a reference page) under docs/en/ and docs/de/ tagged track: developer-docs, content_mode: reference, listing Python, MkDocs, Taskfile, pre-commit, Vale/vale-style, the MkDocs plugin stack, and any external services. Reference spec/portfolio/tech-stack/ when it exists rather than restating the portfolio baseline inline..
      Verify: docs/en/ contains a page or named H2 section with a tech-stack block reachable from Home within one navigation step, declaring track: developer-docs..

- [ ] [docs-audience-tracks.developer-docs-content-contract] The developer-docs setup/prerequisites MUST block is absent. The spec requires a block specific enough that a newcomer can decide in one read whether they can contribute and exactly which commands to run before the repository is workable: local toolchain (language runtimes, package managers, task runners), pinned versions or version ranges, prerequisite system tools and external accounts, and the first-time bootstrap sequence (task setup, pre-commit install, equivalent). docs/en/getting-started/installation.md has a 'Prerequisites' section, but it covers end-user plugin installation (requires Claude Code and a local checkout), not the contributor setup toolchain. No page lists Python version, pip/uv, MkDocs, pre-commit, Taskfile, or the 'task setup' bootstrap command.
      Where: docs/en/ and docs/de/ — no setup/prerequisites block for contributors exists; installation.md covers end-user plugin installation only.
      Fix: Add a dedicated setup/prerequisites page (or a stable ## Prerequisites H2 section in guides/development.md) covering: Python runtime version, pip/uv, MkDocs and plugin dependencies (docs/requirements.txt), pre-commit, Taskfile, and the bootstrap sequence ('task setup' or equivalent). The block must be reachable from Home within one navigation step..
      Verify: Read docs/en/guides/development.md and docs/en/getting-started/installation.md; confirm neither contains the toolchain bootstrap sequence. Check that no other page reachable from Home within one step covers Python version + task setup + pre-commit install..

- [ ] [docs-audience-tracks.user-docs-content-contract] AUDIENCES.md maps downstream-user (primary, direct-consumer) and downstream-end-user to user-docs track, but no explicit opt-out of user-docs is recorded in the artefact, and the four MUST user-docs content blocks (installation/onboarding for end users, value proposition, audience block, use-case/scenario) are entirely absent from docs/en/ and docs/de/. The spec MUST requires all four blocks to be reachable from Home within one navigation step when the audience artefact does not opt out of user-docs.
      Where: AUDIENCES.md (downstream-user entry, track: user-docs); docs/en/ and docs/de/ — no user-docs pages exist.
      Fix: Either (a) record an explicit 'no audience maps to user-docs' omission note in AUDIENCES.md with a rationale (e.g. downstream-user's interaction is tool-invocation, not documentation consumption), OR (b) create the four MUST user-docs content blocks — a user-facing installation/onboarding page in getting-started/, a value-proposition block and audience block on the Home page or a user-docs landing page, and a use-case/scenario block — all tagged track: user-docs. Run docs-audience-tracks-apply in audit mode first to scope the full work..
      Verify: AUDIENCES.md carries a 'no audience maps to user-docs' note OR docs/en/ contains at least one page with track: user-docs plus the four MUST blocks reachable from the Home page..

- [ ] [i18n-completeness.multi-tree-isolation] Spec §Audit dimensions line 41 MUST: treat each independent locale tree (per package / subroot) as a separate audit scope and never merge keys across trees; each scope's reference locale, parity, and orphan/missing math is computed within that tree alone. The agent's Step 2 prose ('Against the reference locale, compute keys missing in another locale...') gives no instruction about per-tree scoping when multiple independent locale trees exist. Nothing in the procedure, report template, or quality rules tells the LLM to identify tree boundaries or refuse to merge keys across trees. An LLM following the agent body on a monorepo with multiple locale trees would likely flatten and merge them, producing incorrect cross-tree parity results.
      Where: /home/nolte/repos/github/claude-shared/agents/i18n-completeness-checker.md — Step 2 (lines 62-64) and Quality rules (lines 110-116); no per-tree isolation instruction anywhere in the agent body..
      Fix: Add an explicit Step 2 instruction: 'When multiple independent locale trees are discovered (e.g. per package or subroot), treat each as a separate audit scope — flatten, diff, and scan each tree independently, never merging key sets across trees. Emit one report section per scope.' Also add the per-tree scoping requirement to Quality rules..
      Verify: Check that the agent body contains instructions to (a) identify multiple locale trees and (b) compute parity and usage within each tree independently, with no cross-tree key merging..

- [ ] [mermaid-diagrams.authoring-rules] Four Mermaid blocks are not preceded by a heading at level 3 or deeper (or a bold caption) that names what the diagram shows, and none of those blocks has a dedicated one-sentence prose lead-in stating the question the diagram answers. Affected files: de/index.md and en/index.md (diagram follows H1 + general intro paragraph, no diagram-specific sub-heading); de/guides/development-lifecycle.md and en/guides/development-lifecycle.md (diagram follows H1 + two page-intro paragraphs, no dedicated sub-heading). de/project/index.md and en/project/index.md violate the heading-depth rule (H2 ## Lifecycle-Übersicht / ## Lifecycle overview used instead of H3+) and lack a prose sentence between the heading and the diagram.
      Where: /home/nolte/repos/github/claude-shared/docs/de/index.md line 13; /home/nolte/repos/github/claude-shared/docs/en/index.md line 13; /home/nolte/repos/github/claude-shared/docs/de/guides/development-lifecycle.md line 17; /home/nolte/repos/github/claude-shared/docs/en/guides/development-lifecycle.md line 17; /home/nolte/repos/github/claude-shared/docs/de/project/index.md line 17; /home/nolte/repos/github/claude-shared/docs/en/project/index.md line 17.
      Fix: For de/index.md and en/index.md: add an H3 or bold caption (e.g. ### Plugin architecture or **Plugin architecture**) immediately above the diagram-source comment, followed by a one-sentence lead-in such as The diagram below shows how claude-shared is packaged as the nolte-shared plugin and which artefact types it delivers to consumer projects. For de/guides/development-lifecycle.md and en/guides/development-lifecycle.md: add an H3 or bold caption before the diagram-source comment. For de/project/index.md and en/project/index.md: either demote ## Lifecycle-Übersicht / ## Lifecycle overview to ### or add a bold caption, and insert a one-sentence lead-in between the heading and the diagram-source comment..
      Verify: Confirm that each of the six mermaid blocks has (1) an H3+ or bold caption within the two lines above the diagram-source comment, and (2) at least one prose sentence between that caption and the fence that starts with a question the diagram answers..

- [ ] [permission-allowlist.MUST-maintenance-cadence] The allowlist has not been reviewed since PR #18 (April 2026), despite five Taskfile changes adding new targets (validate:skills, docs:catalog, check) and 97+ skill/agent-related commits introducing new automation patterns. The spec MUST requires a review after new skills, new Taskfile targets, or new automation commands are introduced.
      Where: /home/nolte/repos/github/claude-shared/.claude/settings.json (last touched commit 0e3b6f9, April 2026); Taskfile.yml (commits 72, 83, 168, 224, 223 added targets); skills/ (97 commits since PR #18).
      Fix: Invoke /nolte-shared:permission-allowlist-maintain on a fresh chore/allowlist-cadence-review branch. Walk the newly-introduced Taskfile targets (task validate:skills, task docs:catalog, task check) and any new command patterns observed in recent sessions through the three-criterion dialogue. Apply and PR the result. Record in the PR body: 'Triggered by cadence MUST: five Taskfile target additions and 97 skill/agent commits since last review (PR #18, April 2026).'.
      Verify: PR merged to develop with a chore-typed commit. PR body lists each candidate evaluated (accepted, narrowed, or rejected) with the criterion citations. .claude/settings.json updated or confirmed-current..

- [ ] [portfolio-inflight-management.AC-10] The v2 audit run bypassed the mandatory portfolio-inflight-collector agent dispatch and executed the data collection inline within the orchestrating skill conversation, violating §Audit operation MUST 'dispatch a read-only specialist agent portfolio-inflight-collector for per-repository data-source collection'. The Processing log documents this explicitly: 'the orchestrator session executed the equivalent read-only gh procedure inline because no agent dispatch surface was registered in the active Claude Code session'.
      Where: /home/nolte/repos/github/claude-shared/.audits/portfolio-inflight/2026-05-23-v2.md — ## Processing log, first entry (2026-05-23T21:40:00Z).
      Fix: The next audit run must dispatch the bound portfolio-inflight-collector agent rather than executing collection inline. The skill already specifies the dispatch pattern in Operation step 2. If the agent dispatch surface was unavailable in the active session, the correct response is to stop and route the operator to a session where agent dispatch works (e.g. claude --plugin-dir . from the claude-shared checkout), not to execute inline. No code change needed — operational discipline on the next run..
      Verify: Run the audit and confirm the Processing log entry reads 'collector dispatched via Agent(portfolio-inflight-collector)' rather than the inline fallback note. The v3 artefact must not contain the fallback pattern..

- [ ] [portfolio-inflight-management.AO-gate3-bypass] The v2 audit self-confirmed gate-3 (pre-write operator confirmation) rather than presenting the finding counts to the operator for approval. The SKILL.md Hard rules state 'Never bypass the three user-confirmation gates' and §Audit operation explicitly justifies the MUST-be-a-skill classification on the load-bearing interactive checkpoints. Gate-3 in v2 reads 'self-confirmed for the fire-and-forget 2nd-audit-post-archival sanity check. The orchestrating session pre-approved this run.' This is a direct bypass of a mandatory operator gate. Gate-2 was similarly 'pre-confirmed per orchestrator-session decision' rather than waiting for operator input.
      Where: /home/nolte/repos/github/claude-shared/.audits/portfolio-inflight/2026-05-23-v2.md — ## Processing log, entries for gate-2 and gate-3.
      Fix: Each audit run must present the three user-confirmation gates to the operator independently, regardless of whether prior runs confirmed similar decisions. Gate-3 MUST present the per-severity counts and the 'Write Findings-Report? [yes / show-findings / abort]' prompt before any write. Gate-2 MUST re-present each roster-gap token that crosses the 3-recurrence threshold even if a prior run already escalated it, so the operator can affirm or reverse the escalation..
      Verify: In the next audit run, confirm the Processing log records 'operator confirmed' (not 'self-confirmed' or 'pre-confirmed by orchestrating session') for all three gate decisions..

- [ ] [portfolio-management.continuous-improvement-integration] portfolio-audit is not listed in spec/project/continuous-improvement/en.md §Finding sources in scope. AC-8 (MUST) states continuous-improvement must list portfolio-audit as a recognized audit source.
      Where: /home/nolte/repos/github/claude-shared/spec/project/continuous-improvement/en.md lines 25-35.
      Fix: Add a bullet under §Finding sources in scope in both spec/project/continuous-improvement/en.md and spec/project/continuous-improvement/de.md (the DE translation must stay in sync) citing 'a portfolio-audit Findings-Report entry of Critical or Warning severity per spec/portfolio/portfolio-management/ §Portfolio audit'..
      Verify: grep 'portfolio-audit' spec/project/continuous-improvement/en.md and spec/project/continuous-improvement/de.md both return a match inside the §Finding sources in scope section..

- [ ] [portfolio-management.documentation-rendering] The aggregated portfolio inventory under docs/en/portfolio/ and docs/de/portfolio/ has never been generated. AC-7 (MUST) requires the inventory to be published and verified via task docs. Neither directory exists.
      Where: /home/nolte/repos/github/claude-shared/docs/ (no portfolio/ subdirectory in either en/ or de/).
      Fix: Run /nolte-shared:portfolio-audit Render operation to generate docs/en/portfolio/ and docs/de/portfolio/ from the existing project/portfolio.yml manifests. Verify with task docs --strict..
      Verify: ls docs/en/portfolio/ and ls docs/de/portfolio/ both show index.md (and any sub-pages). task docs exits 0..

- [ ] [portfolio-management.documentation-rendering-ci] No CI check verifies that the rendered portfolio inventory matches what regenerating from manifests would produce. AC-7 (MUST) requires a CI check for this. ci.yml contains no portfolio-related step.
      Where: /home/nolte/repos/github/claude-shared/.github/workflows/ci.yml.
      Fix: Add a CI step (or extend the existing docs job) that runs the Render operation in check-only mode and fails if the output would differ from what is committed under docs/*/portfolio/. A simple approach is running the render script with --check and diffing the result..
      Verify: ci.yml contains a portfolio-inventory-check step; a deliberate stale inventory causes the CI job to fail..

- [ ] [portfolio-management.portfolio-audit-findings-report] No quarterly audit Findings-Report exists under .audits/portfolio/. AC-6 (MUST) requires at least one report conforming to the review-plan four-section structure. The .audits/portfolio/ directory does not exist.
      Where: /home/nolte/repos/github/claude-shared/.audits/ (no portfolio/ subdirectory).
      Fix: Run /nolte-shared:portfolio-audit (Audit operation) to produce the first .audits/portfolio/<YYYY-MM-DD>.md report. Ensure the file has sections ## Scope, ## Summary, ## Findings, ## Processing log with canonical severity vocabulary..
      Verify: ls .audits/portfolio/ shows at least one dated .md file whose structure matches spec/claude/review-plan/ §Mandatory sections..

- [ ] [portfolio-management.portfolio-audit-skill-review] The portfolio-audit skill has never been reviewed against skill-review. AC-5 requires 'reviewed against skill-review at least once with the resulting plan closed'. The .audits/skill-review/ directory contains only a .gitkeep.
      Where: /home/nolte/repos/github/claude-shared/.audits/skill-review/ (.gitkeep only).
      Fix: Run /nolte-shared:skill-review against skills/portfolio-audit/SKILL.md, close the resulting review plan, and save the plan under .audits/skill-review/portfolio-audit-<YYYY-MM-DD>.md..
      Verify: ls .audits/skill-review/ shows a portfolio-audit review plan file with status: closed..

- [ ] [portfolio/tech-stack.global-tech-stack-manifest.schema-validation] No CI check or pre-commit hook validates portfolio/tech-stack.yml against the spec's Entry schema. The MUST requires that a non-conformant manifest cannot land on develop, but currently there is zero enforcement: kind-enum violations, group-enum violations, missing mandatory fields, lifecycle-enum violations, and schema-violating field types would all land undetected.
      Where: /home/nolte/repos/github/claude-shared/.pre-commit-config.yaml, /home/nolte/repos/github/claude-shared/.github/workflows/ci.yml, /home/nolte/repos/github/claude-shared/Taskfile.yml — none of these reference portfolio/tech-stack.yml validation.
      Fix: Add a validator script (e.g. scripts/validate_tech_stack.py) that loads portfolio/tech-stack.yml and checks: all five mandatory fields present on every entry, kind within the 12-value enum, group within the 5-value enum, status within active/experimental/deprecated, lifecycle (when present) within development/build/runtime/all, and deprecated_in_favor_of resolves to a non-deprecated entry. Wire it into .pre-commit-config.yaml as a local hook and add a validate:tech-stack task to Taskfile.yml called by the test task (or a dedicated ci step)..
      Verify: Run pre-commit run --all-files and deliberately introduce a kind: invalid-kind value; pre-commit must reject it. Then run with a valid entry and confirm zero failures..

- [ ] [portfolio/tech-stack.portfolio-audit-integration.tech-stack-consistency] The portfolio-audit skill does not extend its Audit operation to verify tech-stack consistency. The spec MUST requires that the same audit run that verifies capability consistency also verifies tech-stack consistency (shadow-without-override detection, broken-override-reference detection, signal-presence checks for kind: package-manager / ci / dep-bot / docs / lint, deprecated-entry-still-inherited detection). The portfolio-audit SKILL.md step 3 defines only four checks (manifest presence, validity, duplicate detection, gap analysis) with no tech-stack checks. The Findings-Report template's Required sections list has no ## Tech stack subsection.
      Where: /home/nolte/repos/github/claude-shared/skills/portfolio-audit/SKILL.md — step 3 (lines ~57-71) and the Required sections list (line 68).
      Fix: Add a fifth check to portfolio-audit SKILL.md step 3: '**Tech-stack consistency**: for each Portfolio-Member, run the signal-presence checks for kind: package-manager, ci, dep-bot, docs, lint; detect shadow-without-override, broken-override-reference, broken-regroup-reference, no-op regroup, dead-code regroup-plus-override, and deprecated-entry-still-inherited-after-sprint patterns. Classify findings per the severity table in spec/portfolio/tech-stack/ §Portfolio audit integration.' Also add ## Tech stack as a required section in the Findings-Report template..
      Verify: Run portfolio-audit on a test repo that has a missing renovate.json5 (breaking the dep-bot signal); the resulting Findings-Report must include a Warning under a ## Tech stack section for the missing signal..

- [ ] [project-structure.renovate-app-installed] The Renovate GitHub App does not appear to be installed on nolte/claude-shared. No Dependency-Dashboard issue, no renovate-bot authored PRs, and no Renovate bot events are present. Without the app, renovate.json5 is inert and no dependency-update PRs are produced.
      Where: GitHub repository nolte/claude-shared — App installations; renovate.json5 at repository root is the inert config.
      Fix: Install the Renovate GitHub App (<https://github.com/apps/renovate>) on the nolte/claude-shared repository via the GitHub App's repository access settings. This is a human-only action per the spec. Once installed, a Dependency Dashboard issue will appear and Renovate will begin opening update PRs..
      Verify: After installation, confirm a 'Dependency Dashboard' issue is opened by renovate-bot, or observe a pin-bump PR from app/renovate-bot within the next Renovate run cycle..

- [ ] [project/diagram-opportunity.structural-anti-patterns-demotion] The agent is missing the structural anti-patterns demotion logic entirely. The spec MUST requires demoting to low confidence (and thus discarding) any candidate match wholly contained in a FAQ question-and-answer pair, fenced command/install sequence, or flat error-message bullet list. None of these three recognized non-diagram structures are mentioned anywhere in agents/diagram-opportunity-reviewer.md, so the agent will surface false-positive diagram candidates from these well-known structural contexts that the spec explicitly intends to suppress.
      Where: /home/nolte/repos/github/claude-shared/agents/diagram-opportunity-reviewer.md — no section covers FAQ Q&A, fenced install sequences, or flat error-message bullet list demotion.
      Fix: Add a dedicated '## Structural anti-patterns' section (parallel to §Confidence model) that lists the three recognized structures (FAQ Q&A pairs, fenced command/install sequences, flat error-message bullet lists), states that any candidate match wholly contained in one of these structures is demoted to low confidence and therefore discarded before emission, and notes that this demotion complements (not replaces) the <!-- diagram-opportunity-skip: ... --> mute marker..
      Verify: After the fix, grep agents/diagram-opportunity-reviewer.md for 'FAQ', 'fenced command', 'install sequence', and 'error-message bullet' to confirm all three structures are named with the demotion rule attached..

- [ ] [project/diagram-opportunity.volume-control-no-cap-overrides] The agent exposes per-file and per-run cap overrides as accepted optional inputs (lines 65-66), directly violating the spec's MUST NOT that forbids exposing caps as invocation-time overrides. The spec mandates that 3 (per-file) and 15 (per-run) are the only supported values, fixed portfolio-wide. The agent's §Inputs section also states 'When the caller passes per-file or per-run cap overrides via input, honour them' (line 213), reinforcing the override path throughout the document.
      Where: /home/nolte/repos/github/claude-shared/agents/diagram-opportunity-reviewer.md lines 65-66 (§Inputs optional inputs) and line 213 (§Volume control 'honour them').
      Fix: Remove the 'Per-file cap override (integer)' and 'Per-run cap override (integer)' bullet points from §Inputs. Replace line 213's 'When the caller passes per-file or per-run cap overrides via input, honour them; otherwise the spec defaults apply' with 'The per-file cap is always 3 and the per-run cap is always 15; these values are fixed portfolio-wide and cannot be overridden by the caller.' Update the §Hard rules section to add a 'Never accept or honour caller-supplied per-file or per-run cap overrides' rule..
      Verify: After the fix, grep agents/diagram-opportunity-reviewer.md for 'cap override', 'cap_override', 'per_file_cap', 'per_run_cap', and 'honour them' to confirm no override path remains. Confirm 'per_file: 3' and 'per_run: 15' are the only values in the caps JSON example..

- [ ] [project/github-issue-templates.derivation-step-6-security-contact-link] The spec states 'A security contact_link is REQUIRED' in step 6 of the derivation procedure. The generated config.yml comments out the security contact_link instead of wiring it as an active config entry, violating this MUST-class requirement. The comment acknowledges the intent but deferring it still leaves the chooser without a visible security reporting path.
      Where: /home/nolte/repos/github/claude-shared/.github/ISSUE_TEMPLATE/config.yml lines 10–17.
      Fix: Uncomment (or re-add) the contact_links block pointing at GitHub private vulnerability reporting (<https://github.com/nolte/claude-shared/security/advisories/new>). Per the spec's open-question note, once a SECURITY.md is added, update the URL to point there instead. The active entry must be present in the real YAML, not only in comments..
      Verify: Run 'gh api repos/nolte/claude-shared/contents/.github/ISSUE_TEMPLATE/config.yml | base64 -d' and confirm contact_links is an active (non-commented) YAML key with a security advisory URL. Then open a new issue on the repo in a browser and confirm the security reporting link appears in the issue chooser alongside the templates..

- [ ] [project/mkdocs-structure.audience-targeting-home-page] The Home page 'Weiter' section lists links to every top-level nav section with a short prose description but does not include an audience annotation per section. The spec MUST requires the section-to-audience mapping to be recorded both in the section's index page frontmatter (satisfied) and in the section description on the Home page (missing).
      Where: /home/nolte/repos/github/claude-shared/docs/de/index.md §Weiter and /home/nolte/repos/github/claude-shared/docs/en/index.md.
      Fix: Annotate each section entry in the Home page 'Weiter' (DE) / 'Next' (EN) list with the primary audience ID(s) — for example: '- [Erste Schritte](getting-started/index.md) — Plugin laden (Zielgruppe: maintainer, external-contributor)'. Apply consistently in both language trees..
      Verify: grep -A1 'getting-started\|skills\|agents\|references\|guides' docs/de/index.md | grep -i 'audience\|zielgruppe\|maintainer\|downstream' should return at least one match per section link..

- [ ] [project/mkdocs-structure.extension-hooks-declaration] spec/claude/skill-agent-catalog/en.md introduces the Skills and Agents extension sections and the mkdocs-literate-nav plugin, but does not satisfy the four extension-hook MUST declarations required by mkdocs-structure §Extension hooks: (1) no insertion position stated relative to the 7 standard sections, (2) no primary audience declared per added section in the extension-spec body, (3) no explicit statement that sections follow standard language-parity (or are single-language), (4) no plugin pin or rationale for mkdocs-literate-nav in the extension-hook format.
      Where: /home/nolte/repos/github/claude-shared/spec/claude/skill-agent-catalog/en.md §Navigation and layout and §Generation mechanism.
      Fix: Add an '### MkDocs extension-hook declaration' (or equivalent) section to spec/claude/skill-agent-catalog/en.md that states: (a) Skills and Agents sections are inserted after References and before Project (or after Project — choose and commit); (b) primary audience for each section (e.g. developer-docs / maintainer); (c) both sections follow the standard language-parity rule (existing DE+EN trees confirm this); (d) mkdocs-literate-nav is required with pin constraint >=0.6 (rationale: literate-nav SUMMARY.md files are the only way catalog navigation works with folder-strategy i18n). Also note that no baseline MUSTs are relaxed..
      Verify: grep -n 'insertion\|primary audience\|language.parity\|single.language\|literate-nav.*>=\|>=.*literate' spec/claude/skill-agent-catalog/en.md should return at least one hit per topic..

- [ ] [project/mkdocs-structure.plugin-baseline-pins] docs/requirements.txt uses >= (floating) version constraints for every plugin and there is no lockfile (uv.lock, poetry.lock, Pipfile.lock). The spec MUST pins every plugin including mkdocs, mkdocs-material, pymdown-extensions, and mkdocs-include-markdown-plugin in the project's Python dependency manifest.
      Where: /home/nolte/repos/github/claude-shared/docs/requirements.txt (all 7 lines use >= e.g. mkdocs>=1.6, mkdocs-material>=9.5).
      Fix: Replace each >= constraint with an exact pin matching the currently installed version (e.g. mkdocs==1.6.1), or adopt a lockfile-based workflow (uv lock / poetry lock) that resolves to exact versions. Verify by running pip install -r docs/requirements.txt in a clean venv and confirming the build is reproducible across runs..
      Verify: grep -E '^[a-zA-Z].*[><=]=.*[^=]$' docs/requirements.txt should return zero lines (no floating constraints). If a lockfile is used instead, confirm it exists and all versions are exact pins..

- [ ] [project/mkdocs-structure.plugin-baseline-pins] The skill-agent-catalog extension spec requires mkdocs-literate-nav (spec/claude/skill-agent-catalog/en.md §Generation mechanism MUST 'wire catalog navigation through mkdocs-literate-nav declared in mkdocs.yml'). Per mkdocs-structure §Extension hooks Plugin extension, the extension spec MUST name the plugin's pin. docs/requirements.txt pins it with >=0.6, which is a floating constraint — the same floating-version violation as finding [0] but for the extension-added plugin.
      Where: /home/nolte/repos/github/claude-shared/docs/requirements.txt line 4: mkdocs-literate-nav>=0.6.
      Fix: Pin mkdocs-literate-nav to an exact version (e.g. ==0.6.0) or a constrained range that resolves to a pin in a lockfile, and state that pin in spec/claude/skill-agent-catalog/en.md §Extension hooks declaration (once that declaration is added per finding [3])..
      Verify: grep 'literate-nav' /home/nolte/repos/github/claude-shared/docs/requirements.txt — confirm no >= operator.

- [ ] [project/parallel-working-copies.path-layout-must-not-claude] A stale non-git directory /home/nolte/repos/github/claude-shared/.claude/worktrees/catalog-overhaul/ exists inside the primary checkout under .claude/ — the exact forbidden path pattern the spec calls 'the historical .claude/worktrees/<slug>/ drift this clause forbids.' The directory is not a registered git worktree (no .git pointer file, no entry in .git/worktrees/), but it is a remnant of a prior agent/harness session that materialized a working directory under the forbidden path. Because all its content (a settings.local.json) is hidden by the global ~/.config/git/ignore pattern **/.claude/settings.local.json, the directory is not surfaced by git status, which means the spec's 'MUST remain surfaced by git status so a contributor or reviewer notices it' secondary safeguard is also defeated (albeit by a user-level global ignore rather than a repo .gitignore entry, which is outside the AC14 MUST's literal scope).
      Where: /home/nolte/repos/github/claude-shared/.claude/worktrees/catalog-overhaul/.
      Fix: Remove the stale directory: rm -rf /home/nolte/repos/github/claude-shared/.claude/worktrees/catalog-overhaul. Once empty, remove the parent: rm -rf /home/nolte/repos/github/claude-shared/.claude/worktrees. Then verify git status no longer shows any untracked entries under .claude/worktrees/ (run: git -C /home/nolte/repos/github/claude-shared ls-files --others --directory | grep .claude). Separately consider adding a note in CLAUDE.md or settings.json to pre-set CLAUDE_AGENT_WORKTREE_ROOT to ~/repos/.worktrees/claude-shared/ so the harness default cannot recreate this pattern..
      Verify: Run: ls /home/nolte/repos/github/claude-shared/.claude/worktrees 2>/dev/null && echo STILL_EXISTS || echo CLEAN. Expected: CLEAN. Also run: git -C /home/nolte/repos/github/claude-shared ls-files --others --directory | grep worktrees — expected: no output..

- [ ] [project/prose-style.RedHat-not-activated] The spec MUST at §Shared Vale configuration line 23 requires the .vale.ini to 'compose the Microsoft and RedHat packages plus a pinned release of nolte/vale-style'. RedHat is listed in Packages (the zip is downloaded on vale sync and present under .github/styles/RedHat/) but is never listed in any BasedOnStyles entry — neither in .vale.ini nor in the .vale-config/2-nolte-styles.ini extension file. The [*.md] section activates Vale and Microsoft only; the nolte-styles config activates Vale and nolte-styles only. RedHat rules (Spelling, Conjunctions, PascalCamelCase) are therefore entirely inert at runtime, meaning the required three-way composition is not implemented.
      Where: /home/nolte/repos/github/claude-shared/.vale.ini — [*.md] BasedOnStyles = Vale, Microsoft; /home/nolte/repos/github/claude-shared/.github/styles/.vale-config/2-nolte-styles.ini — BasedOnStyles = Vale, nolte-styles; RedHat never appears in any BasedOnStyles line.
      Fix: Add RedHat to the BasedOnStyles value in the main [*.md] section of .vale.ini (e.g., BasedOnStyles = Vale, Microsoft, RedHat) or in the .vale-config/2-nolte-styles.ini extension, so all three packages are actively applied during every Vale run..
      Verify: Run 'task lint:prose' and confirm Vale reports at least one RedHat rule ID (e.g., RedHat.Spelling or RedHat.Conjunctions) in its output, or run 'vale --no-exit ls-rules README.md' and verify RedHat.* rules appear in the active rule list..

- [ ] [project/prose-style.pr-description-vale-check] No CI workflow validates pull-request descriptions against Vale at error level. The spec MUST requires a PR-check workflow that runs Vale against PR descriptions with the same MinAlertLevel as documentation.
      Where: /home/nolte/repos/github/claude-shared/.github/workflows/ — no dedicated PR-description prose check exists across all six workflow files.
      Fix: Add a CI job (or a separate workflow triggered on pull_request) that extracts the PR description body and runs Vale on it. One common approach is a GitHub Action that writes the PR body to a temporary .md file and runs 'vale --minAlertLevel=error <tmp>.md'. Wire it as a required check..
      Verify: Open a test PR with a known prose violation (e.g., title-case heading) in the description and confirm CI fails with a Vale error..

- [ ] [project/quality-gate.triggers-fast-scope-skipped-verdict] The spec states that when the `fast` scope is active (lint + typecheck, tests skipped), the tests row MUST report status=skipped and the overall verdict MUST note the skip. SKILL.md Inputs mentions `fast` scope but the Operations section and the step 6 verdict block do not explicitly enforce these MUSTs. Step 6 only covers `skipped: no tooling detected`, not the intentional fast-scope skip path.
      Where: skills/quality-gate/SKILL.md — step 6 Overall verdict (lines 131-135) and Inputs section (line 42).
      Fix: Add a bullet to step 6 Overall verdict: 'Any row explicitly skipped due to scope restriction (e.g. fast scope omits Tests): verdict MUST note the intentional skip so the caller knows the gate is partial.' Add a corresponding Hard rule: 'When fast scope is active, always set Tests row status=skipped and note the scope restriction in the verdict.'.
      Verify: SKILL.md step 6 explicitly handles the fast-scope-skipped case with a distinct verdict note, separate from the no-tooling-detected case..

- [ ] [project/spec-drift-audit.ac-coverage-gap] The acceptance criterion that the most recent audit covers every in-scope spec (or lists omissions) fails: 21 specs added after Q2 are neither covered nor explicitly listed as omitted.
      Where: .audits/spec-drift/2026-Q2.md (covers 41 specs at 2026-05-19; current corpus is 62 specs, 21 of which post-date the audit with no omission note).
      Fix: Either record a thematic catch-up audit that covers the 21 new specs, or ensure the 2026-Q3 artifact explicitly states 'specs added between 2026-05-19 and <Q3-start> are included in this Q3 scope' and evaluates them..
      Verify: Either a follow-up artifact covers the 21 uncovered specs, or the 2026-Q3 artifact lists all 62 current specs with per-criterion results..

- [ ] [project/spec-drift-audit.artifact-per-criterion] The 2026-Q2 artifact records findings as narrative prose rather than a testable pass / fail / blocked / not-applicable table, violating the MUST for a testable outcome per spec acceptance criterion.
      Where: .audits/spec-drift/2026-Q2.md §Findings & Resolutions (narrative paragraphs per finding, no structured table with result column).
      Fix: For all future artifacts, use the ## Per-criterion results table from the template with one row per spec-AC and the four result values. Narrative prose under ## Findings & Resolutions can supplement but cannot replace the table..
      Verify: Next artifact contains a populated ## Per-criterion results table with at least one row per in-scope spec, each with a result value of pass, fail, blocked, or not-applicable..

- [ ] [project/spec-drift-audit.artifact-structure] The 2026-Q2 audit artifact does not conform to the template-mandated structure: it lacks YAML frontmatter (audit-type, repo-revision, scope, triggers, tools-used, status) and has no ## Per-criterion results table, ## Decisions section, or ## Processing log section.
      Where: .audits/spec-drift/2026-Q2.md (no YAML frontmatter block; sections are ## Scope, ## Summary, ## Findings & Resolutions, ## Caller follow-ups, ## Related artefacts, ## Health instead of the template layout).
      Fix: Migrate or supersede the Q2 artifact to match the current template format when the next artifact is created, or add a brief prose note in Q2 explaining it pre-dates the template. Ensure all future artifacts open with the YAML frontmatter from templates/audit.template.md..
      Verify: The next artifact under .audits/spec-drift/ has valid YAML frontmatter (--- block with audit-type, repo-revision, created, scope, triggers, tools-used, status) and the five section headings: ## Scope, ## Per-criterion results, ## Findings, ## Decisions, ## Processing log..

- [ ] [project/spec-drift-audit.execution-per-criterion] All five MUST-class violations in findings [0], [2], [3], [4], [5] are classified as Warning by the auditor, but the review-plan severity scale explicitly defines Critical as 'violates a MUST in the source spec'. Systematic downgrading of MUST violations to Warning without a waiver is itself a violation of the severity vocabulary MUST in spec/claude/review-plan §Severity scale.
      Where: Prior audit report (findings [0], [2], [3], [4], [5]) — all should be Critical per review-plan §Severity scale line 57-64.
      Fix: Reclassify findings [0], [2], [3], [4], [5] as Critical in the audit report, or record a documented waiver in the ## Processing log explaining why a lower severity was chosen..
      Verify: Each finding that cites a MUST rule carries severity Critical; any downgrade has a written waiver in ## Processing log..

- [ ] [project/spec-drift-audit.execution-reproducible] The 2026-Q2 artifact does not include a structured tools-used list with versions or SHAs, violating the MUST that every audit name the tools used and their versions.
      Where: .audits/spec-drift/2026-Q2.md (contains 'Auditor: Claude Code (Opus 4.7)' informally; no tools-used list for project-structure-apply, vocab-drift-audit, task lint).
      Fix: Going forward, populate the tools-used YAML frontmatter field (defined in the template) for every new audit artifact. For the Q2 artifact, add a post-hoc note or update the artifact in the next PR..
      Verify: Next audit artifact has a filled 'tools-used:' YAML list with at least three entries (project-structure-apply, vocab-drift-audit, task lint) each with a version or skill git SHA..

- [ ] [project/spec-drift-audit.triggers-partial] No thematic partial audit was recorded for any of the 22 new specs or the multiple significant MUST-rule changes that landed after the 2026-Q2 audit (2026-05-19).
      Where: .audits/spec-drift/ (only 2026-Q2.md exists; no thematic files for plugin-scoping, resumable-work, parallel-working-copies amendments, or any of the 22 new spec introductions).
      Fix: Open a thematic partial audit (or a combined catch-up partial audit) covering the specs changed or introduced since 2026-05-19. Record it as .audits/spec-drift/2026-Q2-catchup.md or incorporate it in the upcoming 2026-Q3 quarterly audit with an explicit note that the spec-change trigger was deferred..
      Verify: Either a thematic audit artifact exists under .audits/spec-drift/ covering the new specs, or the 2026-Q3 artifact explicitly lists them in scope with a note explaining the delay..

- [ ] [project/spec-readiness.audit-artifact-git-revision] The existing Q2 audit artifact (.audits/spec-readiness/2026-Q2.md) records only the branch name, not the Git revision (commit SHA) that was audited. The agent's Option A output template (§Output shape) and its Preconditions section also omit capturing or emitting a Git revision field. The spec §Audit artifact line 83 states the artifact MUST include 'the Git revision audited'; this MUST is unmet in the only completed artifact and in the agent template that generates future artifacts.
      Where: /home/nolte/repos/github/claude-shared/.audits/spec-readiness/2026-Q2.md line 5 (Branch only); /home/nolte/repos/github/claude-shared/agents/spec-readiness-reviewer.md §Output shape Option A Scope block and §Preconditions.
      Fix: 1. In the agent's Preconditions, add step: capture HEAD revision via `git rev-parse HEAD` and store it for the report. 2. In the Option A output template's Scope block, add a line: `- Git revision audited: <SHA>`. 3. In the Q2 artifact (retroactively or in a follow-up note), record the SHA of the branch at the time of the audit (recoverable via `git log --oneline feat/tech-stack-cicd-plugin-layers`)..
      Verify: Run the agent on any spec, confirm the produced artifact Scope section contains a `Git revision audited:` line with a 40-char SHA. Check 2026-Q2.md for the revision field..

- [ ] [project/spec-readiness.quarterly-cadence] ~55 of 62 draft specs with non-empty Requirements or Acceptance Criteria sections have no readiness-audit entry since spec-readiness was introduced (2026-04-24). The only completed quarterly artifact (Q2) was a spec-change follow-up covering 7 specs. The Q3 frame explicitly defers the full portfolio-wide pass to July–September 2026. This means Q2 will close without a full quarterly run, leaving AC line 96 partially unmet for the entire non-audited corpus.
      Where: /home/nolte/repos/github/claude-shared/.audits/spec-readiness/2026-Q2.md (scope: 7 specs); /home/nolte/repos/github/claude-shared/.audits/spec-readiness/2026-Q3.md line 12 (deferred to Q3 window).
      Fix: Complete the full quarterly audit in Q2 before June 30, 2026 — or document a waiver that Q2's partial audit is the accepted Q2 record and the full corpus sweep begins in Q3. If deferring to Q3, update the Q3 frame to explicitly state this is also the first full portfolio-wide run and record the exception for the ~55 unaudited specs..
      Verify: Check that every spec listed by `find spec -name 'en.md' | xargs grep -l '## Requirements\|## Acceptance Criteria'` appears as an audited slug in at least one artifact under .audits/spec-readiness/, or has a documented exception in that artifact's Scope section..

- [ ] [project/sprint.value-delivery-contract] skills/sprint-plan/SKILL.md (line 66) instructs operators to consult .github/sprint-rejection-rules.yml for per-project widening of the rejection-verb list. Both docs pages (docs/en/skills/nolte-shared/sprint-plan.md and docs/de/skills/nolte-shared/sprint-plan.md, each at line 80) repeat the same reference. The spec §Value-delivery contract explicitly states per-repo widening lives under .github/release-skill-layer.yml and that a project MUST NOT introduce .github/sprint-rejection-rules.yml. The open-questions resolution (Q5 in .audits/open-questions/2026-05-29/apply-resolvable/project__sprint.json) resolved this in the spec text but the skill and docs were never updated to reflect the resolution.
      Where: /home/nolte/repos/github/claude-shared/skills/sprint-plan/SKILL.md:66, /home/nolte/repos/github/claude-shared/docs/en/skills/nolte-shared/sprint-plan.md:80, /home/nolte/repos/github/claude-shared/docs/de/skills/nolte-shared/sprint-plan.md:80.
      Fix: Replace all three occurrences of 'consult `.github/sprint-rejection-rules.yml` if it exists' with 'add a `sprint_rejection_verbs:` key under `.github/release-skill-layer.yml`' (the override surface named by the spec). Remove any examples/02-reject-operator-internal-value.md reference to sprint-rejection-rules.yml if present..
      Verify: grep -r 'sprint-rejection-rules' skills/ docs/ -- should return no results after the fix..

- [ ] [pull-request-workflow.branch-freshness] `required_status_checks.strict` is declared `true` in settings.yml (and in the gh-plumbing commons extension) but the live GitHub API returns `strict: false`. This means the platform does not enforce that a PR branch is up-to-date with develop before merging, removing a key CI integrity guarantee.
      Where: /home/nolte/repos/github/claude-shared/.github/settings.yml line 33 (`strict: true`); live API endpoint `repos/nolte/claude-shared/branches/develop/protection/required_status_checks` returns `strict: false`.
      Fix: Same remediation as the enforce_admins gap: trigger a probot/settings re-sync. Verify with `gh api repos/nolte/claude-shared/branches/develop/protection/required_status_checks --jq '.strict'` that it returns `true`. Investigate why both enforce_admins and strict are out of sync — this pattern suggests the settings bot may not be actively running against this repository..
      Verify: `gh api repos/nolte/claude-shared/branches/develop/protection/required_status_checks --jq '.strict'` must return `true`; a test PR whose branch lags develop should be blocked from merging by the platform..

- [ ] [pull-request-workflow.ci-gate] `enforce_admins` is declared `true` in settings.yml but the live GitHub API returns `enabled: false`. This means admin accounts can currently bypass required status checks and merge into develop without passing CI — the spec explicitly prohibits any bypass path and states `enforce_admins: true` has no exception.
      Where: /home/nolte/repos/github/claude-shared/.github/settings.yml line 36 (`enforce_admins: true`); live API endpoint `repos/nolte/claude-shared/branches/develop/protection/enforce_admins` returns `enabled: false`.
      Fix: Trigger the probot/settings bot to re-sync by pushing a no-op change to settings.yml (add a comment or bump the pin version), then verify with `gh api repos/nolte/claude-shared/branches/develop/protection/enforce_admins --jq '.enabled'` that it returns `true`. If the bot is not installed or not functioning, apply the protection manually via `gh api -X POST repos/nolte/claude-shared/branches/develop/protection` with the correct payload, or investigate the settings app installation status..
      Verify: `gh api repos/nolte/claude-shared/branches/develop/protection/enforce_admins --jq '.enabled'` must return `true`..

- [ ] [pull-request-workflow.pr-lint-workflow] No PR lint workflow exists. The spec mandates a workflow (e.g. `pr-lint.yml`) that validates PR title format (Conventional Commits) and verifies all five required body sections on `pull_request` events opened/edited/synchronize/ready_for_review. Neither a local workflow nor a reusable workflow from gh-plumbing covers this. The `pr-lint` check is also absent from the `required_status_checks.contexts` in settings.yml, so the gate is entirely missing.
      Where: /home/nolte/repos/github/claude-shared/.github/workflows/ (no pr-lint.yml present); /home/nolte/repos/github/claude-shared/.github/settings.yml (contexts: lint, test, docs — no pr-lint).
      Fix: 1) Create or call a reusable `pr-lint` workflow (preferably under nolte/gh-plumbing as `reusable-pr-lint.yaml`) that fires on `pull_request` events `opened`, `edited`, `synchronize`, `ready_for_review` and validates: (a) title matches `<type>(<scope>)?: <summary>` with type in {feat,fix,chore,docs,exp}; (b) body contains all five required headings in order; (c) Summary, Changes, Testing are non-empty and not literal `None`. 2) Add the workflow's job name to `required_status_checks.contexts` in `.github/settings.yml`..
      Verify: After adding the workflow: open a test PR with a malformed title and confirm the pr-lint check fails. Confirm the check name appears in the required_status_checks list returned by `gh api repos/nolte/claude-shared/branches/develop/protection/required_status_checks`..

- [ ] [pull-request-workflow.pr-lint-workflow] The spec MUST requires the pr-lint workflow's job to be registered as a required status check for develop in .github/settings.yml. Even if a pr-lint.yml workflow were added, the settings.yml contexts list (lint, test, docs) does not include the pr-lint job name, so the gate registration itself is a separate missing MUST.
      Where: /home/nolte/repos/github/claude-shared/.github/settings.yml lines 32-35 (contexts: lint, test, docs — pr-lint absent).
      Fix: Add the pr-lint job name (e.g. pr-lint) to the required_status_checks.contexts list in .github/settings.yml alongside creating the pr-lint.yml workflow..
      Verify: After adding the workflow and context entry, run: gh api repos/nolte/claude-shared/branches/develop/protection/required_status_checks | python3 -c "import sys,json; print(json.load[sys.stdin]('contexts'))" and confirm the pr-lint job name appears..

- [ ] [release-automation.operational-contract] The spec line 49 states: 'MUST surface the target tag, title, and a diff summary of the body in the workflow run output so the human triggerer can verify before the irreversible step.' The reusable's 'Disclose target state in job summary' step outputs Tag, Target SHA, Draft created, Triggerer, Run URL, Project type, Dry run, and a body preview — but the release *title* (the name field) is never fetched or displayed. `gh release view "$TAG" --json name` is not called anywhere in the reusable. The title is a load-bearing human-verification field per the spec MUST.
      Where: nolte/gh-plumbing:.github/workflows/reusable-release-publish.yml — 'Disclose target state in job summary' step (blob sha 9d3bce3903fff5f41280ff86ea82d60290f562d9); spec/project/release-automation/en.md line 49.
      Fix: In the 'Disclose target state in job summary' step, add `title=$(gh release view "$TAG" --json name --jq .name)` and emit a `| Title | $title |` row to the summary table before the body preview section..
      Verify: Trigger a dry_run dispatch and open the workflow run's job summary; confirm a 'Title' row appears with the human-readable release name (e.g. 'v0.1.5')..

- [ ] [release-automation.version-bearing-files] The spec portfolio convention table (line 62) declares that for Claude Code plugin repos, `.claude-plugin/marketplace.json` has TWO required selectors: `$.metadata.version` AND `$.plugins[].version`. The reusable's VBF detection step (blob sha 9d3bce3903fff5f41280ff86ea82d60290f562d9, 'Detect project type' step) registers only a single entry for marketplace.json with selector `metadata.version`. The `plugins[].version` field is never verified during pre-publish alignment checks. A release where `metadata.version` is correct but `plugins[0].version` is stale would pass all pre-publish gates and be published with a misaligned marketplace.json.
      Where: nolte/gh-plumbing:.github/workflows/reusable-release-publish.yml blob 9d3bce3903fff5f41280ff86ea82d60290f562d9, 'Detect project type and load version-bearing files' step, lines 154-158; spec/project/release-automation/en.md line 62 and line 100.
      Fix: In the Python VBF detection block for the `claude-plugin` case, add a second entry for marketplace.json that targets the plugins array. Because the selector model is flat (single-key path), either (a) add a separate entry `{"path": ".claude-plugin/marketplace.json", "selector": "plugins.0.version", ...}` and handle list-index notation in the verification step, or (b) extend the verification step's JSON-path resolver to iterate `plugins[*].version` and assert each equals the target tag..
      Verify: Manually set `.claude-plugin/marketplace.json` `plugins[0].version` to a stale value while keeping `metadata.version` correct, then trigger a dry_run dispatch; confirm the workflow fails with a version-bearing file alignment error referencing the `plugins[].version` selector..

- [ ] [release-notes-audience-analysis.bounded-context-declaration] No release-notes-scoped audience artefact exists. The existing AUDIENCES.md declares a bounded context of 'the nolte/claude-shared plugin repository' — not 'release notes of a GitHub release of this project'. All downstream MUST requirements (eight-candidate evaluation, content dimensions, confirmed/assumed tagging, release-drafter traceability) remain unmet because this foundational artefact is absent.
      Where: /home/nolte/repos/github/claude-shared/AUDIENCES.md — bounded context section declares the plugin as a whole, not the release-notes sub-context. No docs/release-audiences.md exists..
      Fix: Invoke /nolte-shared:audience-identify with bounded context set to 'release notes of a GitHub release of nolte/claude-shared'. Produce a release-notes-scoped artefact (docs/release-audiences.md or a dedicated ## Release-notes audiences section) that: declares the context in writing; evaluates all eight candidate audiences; links each to content dimensions (release-drafter section, detail depth, language register, CTA, machine-readability); tags each confirmed or assumed; classifies breaking-change and security-disclosure audiences as primary..
      Verify: After creation: grep docs/release-audiences.md (or AUDIENCES.md) for 'release notes of nolte/claude-shared' as the first bounded-context statement; verify all eight candidate audience slots are present (each either populated or marked 'not applicable' with reason); verify each entry carries a content-dimensions block with at least release-drafter section, detail depth, language register, CTA, and machine-readability fields..

- [ ] [release-skill-layer.shared-shape-MUST] The spec §Skill split and shared shape MUST (line 32) requires that BOTH skills follow the same project-type detection signals used by github-issue-templates-apply, and AC3 (line 107) requires this to be 'verifiable by reading the detection step of each SKILL.md'. Skill B (release-publish-trigger) contains no project-type detection step at all. Its step 2b reads 'the default table by repo type, or the override at .github/release-automation.yml' without any preceding step that detects the repo type via the six signals. The release-automation §Version-bearing files table is indexed by repo type (Claude Code plugin, Python package, Node.js package, HACS integration), so Skill B cannot correctly look up the default version-bearing file list without knowing the repo type. Skill A fully implements the six-signal detection (SKILL.md lines 63-74); Skill B implements none of it.
      Where: /home/nolte/repos/github/claude-shared/skills/release-publish-trigger/SKILL.md (§2. Validate every pre-publish gate, step 2b, lines 67-76); spec requirement at /home/nolte/repos/github/claude-shared/spec/project/release-skill-layer/en.md line 32 and AC3 line 107.
      Fix: Add a project-type detection step to release-publish-trigger/SKILL.md before step 2 (gate validation), applying the same six-signal ordered walk used in release-notes-curate/SKILL.md lines 63-74. The detected type is then used in step 2b to select the correct rows from release-automation §Version-bearing files default table. Also add a note confirming the override path (.github/release-skill-layer.yml) applies here too..
      Verify: Read skills/release-publish-trigger/SKILL.md and confirm it contains a numbered step that walks .claude-plugin/plugin.json, pyproject.toml, package.json, and mkdocs.yml signals in the same order as release-notes-curate, before the pre-publish gate validation section. AC3 should then be satisfiable by reading that detection step..

- [ ] [research-triangulate.author-time-assertions] The spec requires author-time assertions in SKILL.md, agents/*.md, and spec files that direct behaviour toward repo-external writes to be triangulated to at least the Release/dispatch tier (three independent sources) before the PR is merged. No PR checklist, skill-review gate, spec-readiness-reviewer agent, or quality-gate enforcer implements this requirement.
      Where: /home/nolte/repos/github/claude-shared/agents/spec-readiness-reviewer.md, /home/nolte/repos/github/claude-shared/skills/skill-review/SKILL.md, /home/nolte/repos/github/claude-shared/skills/quality-gate/SKILL.md.
      Fix: Add an author-time triangulation check to the spec-readiness-reviewer agent and to the skill-review SKILL.md: any SKILL.md or agents/*.md that pins an external version, path in a sister repo, or third-party API signature must carry a ≥3-source citation before passing review..
      Verify: A skill-review run against a SKILL.md containing a hard-coded version pin without three cited sources should produce a blocking finding..

- [ ] [research-triangulate.conflict-resolution] No skill or agent (other than a partial implementation in cookiecutter-template-author.md) implements conflict-stop logic: when two source hits disagree, the skill must halt, surface both sources with their classes and dates, and await operator decision. There is no autonomous-loop abort-on-conflict path anywhere.
      Where: /home/nolte/repos/github/claude-shared/skills/, /home/nolte/repos/github/claude-shared/agents/ (all files except agents/cookiecutter-template-author.md).
      Fix: Add a conflict-resolution procedure to every WebSearch/WebFetch-using skill and agent: on disagreement between sources, emit the conflict report format (which sources, which detail, source class each, verifiable date each) and stop; in autonomous-loop context, abort the write and persist a findings report under .audits/<skill>/<run>/..
      Verify: A test scenario where two WebSearch results disagree on a version pin should cause the skill to stop rather than proceed..

- [ ] [research-triangulate.conflict-resolution] cookiecutter-template-author.md, the only artifact in the repo that actually invokes WebFetch and WebSearch, does not implement the autonomous-loop abort path required by the conflict-resolution MUST. When the agent runs headless (dispatched via cookiecutter-template-manage with no interactive operator), a source conflict must abort the write and persist a findings report under the audit directory (spec line 85). The agent currently surfaces conflicts to the caller when one is present, but contains no logic to abort the write or persist a findings report when no operator is reachable — the fire-and-forget agent lifecycle makes this gap especially critical.
      Where: /home/nolte/repos/github/claude-shared/agents/cookiecutter-template-author.md (lines 100-108, Working procedure step 3; lines 43, 136).
      Fix: Add an explicit autonomous-loop conflict-abort path to the Working procedure: when two or more web sources disagree on a factual claim, the agent MUST stop the write, persist the conflict details (sources, classes, dates, specific disagreement) to '.audits/cookiecutter-template-author/<run-id>/conflict-<n>.md', and return a structured report flagging the unresolved conflict rather than proceeding with the write..
      Verify: Confirm the Working procedure step 3 now includes: (a) source-class classification per hit, (b) date extraction per hit, (c) explicit conflict detection, (d) abort-write-and-persist path when sources disagree, and (e) escalate-to-caller path when operator is reachable..

- [ ] [research-triangulate.interaction-with-existing-skills] The three skills explicitly named by the spec as MUST-apply targets — dependency-audit, release-notes-curate, cookiecutter-template-manage — contain zero triangulation language: no minimum source count, no source-class requirements, no conflict-stop logic, no `unverified` marking, and no source-list documentation.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md, /home/nolte/repos/github/claude-shared/skills/release-notes-curate/SKILL.md, /home/nolte/repos/github/claude-shared/skills/cookiecutter-template-manage/SKILL.md.
      Fix: Add a triangulation section to each of these three SKILL.md files that (a) mandates the blast-radius source-count table, (b) defines source-class precedence (Primary / Secondary / Web aggregator / Model memory), (c) requires an `unverified` label when the source count is unreachable, and (d) requires the source list (URL, class, retrieval date) to appear inline or in `.audits/<skill>/<run>/`..
      Verify: After the fix, grep each SKILL.md for 'triangulat' or 'independent source' and confirm the blast-radius table conditions are present..

- [ ] [research-triangulate.minimum-source-count-scales-with-blast-radius] No skill or agent in the repository implements the blast-radius tiered source-count table (2 sources for conversational/local-edit, 3 for release-config writes, 3 + operator confirmation for force-push/dispatch/cross-repo PRs). The methodology is defined only in the spec and is completely absent from all consuming artifacts.
      Where: /home/nolte/repos/github/claude-shared/skills/, /home/nolte/repos/github/claude-shared/agents/.
      Fix: Add the blast-radius table — or a direct spec reference — to each skill and agent that makes repo-external assertions, so the runtime Claude can look it up and apply the correct tier..
      Verify: A running session invoking dependency-audit with a version-pin assertion (Release-config blast radius) should be blocked from writing without three independent sources..

- [ ] [skill-agent-catalog.generation-mechanism-no-committed-catalog] The spec MUST NOT commit generated catalog Markdown back into docs/; the docs-deploy pipeline MUST invoke the catalog generator on the deploy build. Both rules are violated: 245 generated pages (skills + agents + tags.md, both languages) are tracked in git, and the deploy workflow (release-cd-deliver-docs.yml) invokes mhausenblas/mkdocs-deploy-gh-pages which runs mkdocs gh-deploy directly without task docs, so the catalog is never regenerated on deploy — the committed snapshot is published as-is.
      Where: .github/workflows/release-cd-deliver-docs.yml (line 8: reusable-mkdocs.yaml) + committed docs/en/skills/, docs/de/skills/, docs/en/agents/, docs/de/agents/, docs/en/references/tags.md, docs/de/references/tags.md.
      Fix: The mitigation already in place (CI docs job runs task docs then git diff --exit-code to catch stale commits) is acceptable as a temporary compensating control. The permanent fix requires either (a) replacing the deploy action with one that installs python deps and runs task docs before mkdocs gh-deploy, or (b) upgrading reusable-mkdocs.yaml in nolte/gh-plumbing to invoke task docs when a Taskfile docs target exists, as the spec prescribes. Either way, once the deploy pipeline regenerates the catalog, remove the generated files from git tracking (add docs/*/skills/, docs/*/agents/, docs/*/references/tags.md to .gitignore) so the MUST NOT commit rule holds..
      Verify: After fix: run git ls-files docs/en/skills/ docs/de/skills/ docs/en/agents/ docs/de/agents/ and confirm zero results; trigger a release and verify the deploy workflow produces the live catalog from source without committed pages..

- [ ] [skill-review.reserved-token-scope] spec/claude/skill-review/en.md line 82 states the reserved-token check applies to 'name nor any other frontmatter value', but spec/claude/skill-management/en.md (the authoritative authoring spec) explicitly restricts the ban to 'name' only, stating that descriptive fields like 'description' MAY legitimately mention 'claude' (line 42). The skill-review spec's relationship section (line 137) states that when this spec and skill-management diverge, the authoring spec wins and this spec needs updating. The current wording would produce spurious Critical findings when reviewing skills whose descriptions legitimately say 'Claude Code skill for X'.
      Where: spec/claude/skill-review/en.md line 82; spec/claude/skill-management/en.md lines 42–43 and 182.
      Fix: Update skill-review/en.md line 82 to match skill-management's narrower scope: the reserved-token ban applies only to 'name'; description and other frontmatter fields MAY mention 'claude' or 'anthropic'. The narrow exception for artefacts targeting Claude/Anthropic surfaces (the Reserved-token rationale exception) should also be referenced..
      Verify: Reread both specs and confirm line 82 wording aligns with skill-management lines 42 and 174/182..

- [ ] [skill-review.reserved-token-scope] Finding [3] was mis-graded as Warning. The skill-review spec line 82 violates the MUST NOT in its own relationship section (line 137): 'MUST NOT re-specify anything already covered by skill-management or skill-vs-agent.' Skill-management line 42 restricts the reserved-token ban to name only; skill-review line 82 extends it to 'any other frontmatter value.' That re-specification contradicts skill-management and the spec's own tie-breaker rule, which is itself a MUST NOT violation. The effect is that a reviewer following skill-review line 82 would generate spurious Critical findings against valid skills whose description fields legitimately contain 'claude'.
      Where: spec/claude/skill-review/en.md line 82 versus spec/claude/skill-management/en.md lines 42-43 and 182.
      Fix: Narrow line 82 to apply the reserved-token ban only to the `name` field, matching skill-management's explicit rule: replace 'neither `name` nor any other frontmatter value' with 'the `name` field'..
      Verify: After edit, grep skills/*/SKILL.md descriptions for 'claude' — no finding should be generated by the corrected line 82 for those description values..

- [ ] [skill-vs-agent.rationale-section-heading] skills/webview-ui-optimize/SKILL.md uses '## Rationale (why a skill, not just an agent)' instead of the required '## Why this is a skill, not an agent'. The spec explicitly calls alternative phrasings non-conformant and states the deterministic heading is load-bearing for grep-based portfolio audits.
      Where: /home/nolte/repos/github/claude-shared/skills/webview-ui-optimize/SKILL.md:38.
      Fix: Replace the heading on line 38 with exactly '## Why this is a skill, not an agent'. No changes to the rationale content are needed — it is substantive and compliant..
      Verify: grep -n '## Why this is a skill, not an agent' skills/webview-ui-optimize/SKILL.md should return line 38..

- [ ] [skill-vs-agent.rationale-section-heading] agents/audience-doc-author.md, agents/claude-plugin-developer.md, and agents/cookiecutter-template-author.md each use the non-conformant heading '## Rationale (why an agent, not a skill)' instead of the required '## Why this is an agent, not a skill'. All three artifacts have substantive rationale content; only the heading string is wrong.
      Where: /home/nolte/repos/github/claude-shared/agents/audience-doc-author.md:34, /home/nolte/repos/github/claude-shared/agents/claude-plugin-developer.md:41, /home/nolte/repos/github/claude-shared/agents/cookiecutter-template-author.md:28.
      Fix: In each of the three agent files, replace '## Rationale (why an agent, not a skill)' with exactly '## Why this is an agent, not a skill'. No changes to the rationale body are needed..
      Verify: grep '## Why this is an agent, not a skill' agents/audience-doc-author.md agents/claude-plugin-developer.md agents/cookiecutter-template-author.md should return one match per file..

- [ ] [yaml-json-schema.data-validation-gate] No data-validation step exists in the quality gate. The spec MUST: 'validate every data file ... against its declared schema as part of the same quality gate'. No .schemas-config.yaml mapping exists, no sidecar-comment scanning hook is wired, and task lint does not invoke any data-vs-schema validator.
      Where: /home/nolte/repos/github/claude-shared/Taskfile.yml and /home/nolte/repos/github/claude-shared/.pre-commit-config.yaml.
      Fix: When the first *.schema.yaml is authored via the yaml-json-schema skill, simultaneously wire a data-validation pre-commit hook or Taskfile step that resolves schema associations via .schemas-config.yaml or sidecar comments and runs the chosen validator. Document the validator choice in Taskfile.yml as the spec SHOULD requires..
      Verify: Introduce a data file with a deliberate conformance error and confirm `task lint` exits non-zero..

- [ ] [yaml-json-schema.meta-validation-gate] task lint has no meta-validation step for *.schema.yaml files. The spec MUST (§Meta-validation and data validation): 'validate every*.schema.yaml file against the JSON Schema 2020-12 meta-schema as part of the repository quality gate'. The pre-commit config contains only check-yaml, yamllint, vale, docs-catalog-fresh, and validate-skills — none of which run check-jsonschema or ajv against a meta-schema. A new schema file committed to this repo would pass the gate regardless of its validity.
      Where: /home/nolte/repos/github/claude-shared/Taskfile.yml (task lint) and /home/nolte/repos/github/claude-shared/.pre-commit-config.yaml.
      Fix: Add a pre-commit hook (or a dedicated Taskfile task called from task lint) that runs `check-jsonschema --check-metaschema` on every file matching `**/*.schema.yaml`. Wire it into the `lint` task so it fires in CI. Since no schema files exist yet, the hook can use `find . -name '*.schema.yaml' | xargs check-jsonschema --check-metaschema` with a guard that exits 0 when the glob is empty (first-time setup) but exits non-zero as soon as a schema file exists and fails meta-validation..
      Verify: Add a test *.schema.yaml with a deliberate dialect error (e.g. missing $schema) and confirm `task lint` exits non-zero..

- [ ] [yaml-json-schema.meta-validation-gate] The spec MUST NOT treat absence of a validator as a passing gate (line 110). The current quality gate has no validator installed for *.schema.yaml files, meaning the gate passes silently when no schema files exist — but would also pass silently if a broken schema were added. This is the same root issue as finding [0] but the spec rule is distinct: it is not just 'add a step' but 'the gate MUST fail with an install hint if the validator is absent'. The pre-commit config has no check-jsonschema or ajv entry at all, so there is no failure-with-hint path.
      Where: /home/nolte/repos/github/claude-shared/.pre-commit-config.yaml and /home/nolte/repos/github/claude-shared/Taskfile.yml.
      Fix: Add a pre-commit hook (or Taskfile step) that runs check-jsonschema or ajv and exits non-zero with an install hint if the binary is not found. Wire it to files matching '*.schema.yaml'..
      Verify: Run 'task lint' in a checkout that has a *.schema.yaml file present but without check-jsonschema installed; the gate must exit non-zero with a message indicating the missing validator..

### Warning

- [ ] [agent-review.ac2] 25 of 26 agents in agents/ have never been reviewed against agent-management since the agent-review spec was adopted; only portfolio-inflight-collector has a closing commit.
      Where: /home/nolte/repos/github/claude-shared/agents/ — 25 agents lack both an open plan and a closing commit in git log; the spec was adopted at commit 0e3b6f9..
      Fix: Run the agent-review skill for each unreviewed agent (one plan per target) and merge or close each plan before the next release. Priority order: agents added since sprint 0004 (code-security-reviewer, diagram-opportunity-reviewer, etc.) first, then the remainder..
      Verify: git log --all --diff-filter=D --name-only --format='%s' -- '.audits/agent-review/*.md' shows one entry per agent slug; or an open plan exists under .audits/agent-review/<slug>.md for each agent still in progress..

- [ ] [agent-review.review-procedure] The skill's step 5 (check enumeration) omits four MUST-level check categories defined in the spec: model-choice checks, spec-anchor check (spec-driven-development), subagent-boundary check for Agent(/Task( dispatch, and plugin-distribution constraint checks (hooks/mcpServers/permissionMode). The skill delegates to 'Apply the checks from spec/claude/agent-review/' but does not enumerate these categories, so an operator following step 5 literally can miss them.
      Where: /home/nolte/repos/github/claude-shared/skills/agent-review/SKILL.md lines 74–82 (step 5 check list). Missing from enumeration: spec §Model-choice checks, §Checks derived from spec-driven-development (added in #134, May 2026), §Plugin-distribution constraint checks, and the Agent(/Task( half of §Subagent-boundary checks..
      Fix: Add four numbered items to step 5 between items 2 and 3: (2b) model-choice: verify model value is opus/sonnet/haiku, rationale present when pinned; (2c) plugin-distribution constraints: for distribution: plugin, verify no hooks/mcpServers/permissionMode; (3b) spec-anchor: grep body for spec/ path, absence is Critical; and extend item 4 to also grep for Agent( and Task( in addition to Skill(..
      Verify: Re-read SKILL.md step 5 and confirm each of the five spec sections (Checks derived from agent-management, Checks derived from skill-vs-agent, Checks derived from spec-driven-development, Model-choice checks, Plugin-distribution constraint checks, Subagent-boundary checks) maps to at least one numbered item..

- [ ] [agent-review.review-procedure] The one existing agent-review deletion commit (b2d150c, portfolio-inflight-collector) does not conform to the spec-required format 'review(agent-review): close <agent-name>—<C>C/<W>W/<S>S/<I>I'. The actual subject is 'review: close completed portfolio-inflight review plans' — missing the scope token, missing the severity-count suffix, using plural 'plans', and adding extra prose.
      Where: git log at commit b2d150cde657a7e222f35758f6ca64d55b9f5351; spec requirement in §Review procedure MUST: 'emit exactly one review-plan file … MUST follow every lifecycle rule from review-plan, including … the deletion-commit message format'; skill §close operation step 5 states the format correctly..
      Fix: Ensure future closing commits use exactly: subject 'review(agent-review): close <agent-name>—<C>C/<W>W/<S>S/<I>I' (one agent per commit, em-dash before counts). The historical commit cannot be retroactively fixed, but conformance going forward restores AC12..
      Verify: Run: git log --all --diff-filter=D --format='%s' -- '.audits/agent-review/*.md' and confirm every line matches the regex 'review\(agent-review\): close [a-z-]+—[0-9]+C/[0-9]+W/[0-9]+S/[0-9]+I'..

- [ ] [blog-author-trigger.deferral-artefact-sprint-review-surfacing] The spec SHOULD requires sprint-review to surface unconsumed deferral artefacts at sprint close so deferrals do not accumulate silently. skills/sprint-review/SKILL.md contains no mention of project/blog-triggers/, deferral artefacts, or unconsumed entries.
      Where: /home/nolte/repos/github/claude-shared/skills/sprint-review/SKILL.md (entire file — no deferral reference found).
      Fix: Add a step to sprint-review's closure sequence (Operation 6 or a new pre-close step) that reads project/blog-triggers/*.yml, filters for status: deferred, and surfaces any unconsumed entries to the operator before promoting to closed. The step should be clearly marked as a SHOULD-class advisory, not a hard closure gate..
      Verify: After the edit, grep sprint-review/SKILL.md for 'blog-triggers' or 'deferral'; confirm at least one mention surfaces unconsumed deferrals at sprint close..

- [ ] [blog-author.briefing-inputs-named-third-party-preferred-name] The SHOULD requiring the preferred name and capitalisation of named third parties to be recorded in the briefing (e.g. `npm` not `NPM`, `Astro` not `astro`) is absent from the skill. The skill's §Inputs 'Named-third-party proof' paragraph covers evidence and consent but omits this orthography field entirely.
      Where: /home/nolte/repos/github/claude-shared/skills/blog-author/SKILL.md §Inputs — Named-third-party proof block.
      Fix: Add a sentence to the named-third-party proof paragraph: 'Optionally record the preferred name and capitalisation of the third party (e.g. `npm`, `Astro`) so the spelling need not be researched on every draft (per spec §Briefing inputs SHOULD).'.
      Verify: grep -n 'preferred\|capitalisation' skills/blog-author/SKILL.md should return a hit in the named-third-party proof section..

- [ ] [blog-author.pre-handover-self-check-per-pair-language-switcher] The per-pair SHOULD ('flip the EN↔DE pair once via the language switcher in the consumer's development server') is absent from the skill's Steps 5–6 and from §Gotchas. The spec places it in the per-pair self-check block as an explicit author duty.
      Where: /home/nolte/repos/github/claude-shared/skills/blog-author/SKILL.md §Operations Step 5 — per-pair checks block.
      Fix: Add to Step 5's per-pair checklist: 'SHOULD flip the EN↔DE pair in the consumer's local development server once to confirm the language-switcher resolves to the correct paired file (per spec §Pre-handover self-check, per-pair SHOULD).' A §Gotchas note explaining why the skill cannot perform this check automatically (no headless browser) and must prompt the operator is a suitable alternative..
      Verify: grep -n 'switcher\|flip.*language\|language.*flip' skills/blog-author/SKILL.md should return a hit in Step 5 or §Gotchas..

- [ ] [blog-author.workflow-should-read-aloud] The spec's SHOULD for a read-aloud pass (or TTS pass) between steps 2–3 and 4–5 is not surfaced in the skill's workflow. The spec explicitly notes 'this spec restates it here so it stays visible in the workflow', yet the skill omits it entirely.
      Where: /home/nolte/repos/github/claude-shared/skills/blog-author/SKILL.md §Operations steps 3 and 5 — no mention of read-aloud.
      Fix: Add a SHOULD note at the end of Step 3 and Step 5 in §Operations: 'Between drafting and self-check the author SHOULD read the language file aloud (or use TTS) per spec/project/blog-author/ §Workflow and post-writing-style §Editing pass; prompt the operator to confirm before proceeding.'.
      Verify: grep -n 'aloud\|TTS' skills/blog-author/SKILL.md should return hits in Steps 3 and 5..

- [ ] [branching-model.exp-excluded-from-release-notes] Neither .github/release-drafter.yml nor the extended gh-plumbing commons-release-drafter.yml defines an autolabeler for the exp/ branch prefix, an 'exp' label, or an exclude-labels entry for it. exp/ PRs have no label assigned and will surface in the uncategorized section of release notes, violating the SHOULD that experimental work must not appear as shipped features.
      Where: /home/nolte/repos/github/claude-shared/.github/release-drafter.yml (extends only); nolte/gh-plumbing:.github/commons-release-drafter.yml (no exp entry).
      Fix: Add an autolabeler entry for the exp/ branch prefix in .github/release-drafter.yml and either (a) add the generated label to exclude-labels so exp PRs are suppressed entirely from release notes, or (b) map the label to a hidden / non-user-facing category (e.g. 'Internal / Experiments'). Example addition to .github/release-drafter.yml:  autolabeler:   - label: experimental     branch:       - '/^exp\/.*$/' exclude-labels:   - experimental.
      Verify: Open a test PR from an exp/YYYY-WW-* branch, confirm the 'experimental' label is auto-applied, then check the current release draft — the PR must not appear in any user-facing category..

- [ ] [branching-model.main-head-equals-release-tag] Remote main HEAD is a merge commit (646e0431) rather than the release tag commit. git tag --points-at returns nothing for main HEAD. The Acceptance Criterion requires the HEAD of main to equal a published release tag; the reusable-release-cd-refresh-master.yml workflow is producing merge commits instead of fast-forwards, so this invariant is never met.
      Where: remote nolte/claude-shared:main HEAD 646e0431; latest release tag v0.1.4 at 419bf080.
      Fix: Investigate why reusable-release-cd-refresh-master.yml@v1.1.19 produces a merge commit rather than a fast-forward onto main. If the workflow itself is correct, check whether required_linear_history: true (declared in settings.yml for main but currently unenforced) is being applied at merge time in a way that forces a merge commit. Fixing the protection issue (finding above) and ensuring a proper FF-capable workflow run should resolve this. Verify after the next release that `git tag --points-at origin/main` returns the expected release tag..
      Verify: After the next release is published, run: git fetch origin main && git tag --points-at origin/main — the output must be the newly published tag (e.g. v0.1.5)..

- [ ] [claude/agent-management.acceptance-criteria-agent-review-output-location] The acceptance criterion requires that individual agent reviews follow spec/claude/agent-review/ and that output lives under .audits/agent-review/<name>.md. The .audits/agent-review/ directory exists but contains only a .gitkeep file — no review has been conducted for any of the 26 agents in the repository.
      Where: /home/nolte/repos/github/claude-shared/.audits/agent-review/ — directory is empty except .gitkeep.
      Fix: Run /nolte-shared:agent-review for each agent (or at minimum for any agent scheduled for a release or that has been recently changed). Review outputs should be persisted as .audits/agent-review/<name>.md. Prioritise agents with high change velocity or the new webview-ui-expert Critical finding..
      Verify: find /home/nolte/repos/github/claude-shared/.audits/agent-review/ -name '*.md' -not -name '.gitkeep' should return at least one result per reviewed agent..

- [ ] [claude/agent-management.description-concrete-triggers] feature-consistency-reviewer's description field does not contain an explicit 'invoke when' / 'use when' phrase. The dispatch trigger is expressed indirectly ('Typically dispatched mid-flow by feature-decompose before a feature transitions draft → ready; users rarely invoke it directly'). The use_when metadata field does carry explicit trigger conditions, but the description string is the primary routing signal for the Claude dispatcher.
      Where: /home/nolte/repos/github/claude-shared/agents/feature-consistency-reviewer.md — description frontmatter field.
      Fix: Prepend or append a brief 'Invoke when feature-decompose needs the pre-draft→ready consistency check, or when the user asks to check whether a planned feature already has prior art.' to the description string so the dispatcher has an unambiguous positive trigger without relying solely on the use_when metadata..
      Verify: After update: grep '^description:' agents/feature-consistency-reviewer.md should contain 'invoke when' or 'use when' phrasing in the prose..

- [ ] [claude/agent-management.tag-vocabulary-should-prefer-starter] Five agents use tags that are not in the spec's starter vocabulary, creating catalog-cluster fragmentation. Affected agents and non-starter tags: code-security-reviewer ('security'), graphic-prompt-generator ('design'), i18n-completeness-checker ('i18n', 'frontend'), test-case-extractor ('testing', 'requirements'). The spec SHOULD: prefer a term from the starter vocabulary when one applies.
      Where: /home/nolte/repos/github/claude-shared/agents/code-security-reviewer.md (tags line), /home/nolte/repos/github/claude-shared/agents/graphic-prompt-generator.md (tags line), /home/nolte/repos/github/claude-shared/agents/i18n-completeness-checker.md (tags line), /home/nolte/repos/github/claude-shared/agents/test-case-extractor.md (tags line).
      Fix: Map non-starter tags to the closest starter term where a fit is reasonable: 'security' → 'audit' (code-security-reviewer); 'design' → omit or use 'prose' if the graphic-prompt focus fits (graphic-prompt-generator); 'i18n' and 'frontend' → 'audit' already present covers the audit aspect, 'frontend' has no starter equivalent so either drop it or propose it as a new starter term via a vocabulary extension PR; 'testing' and 'requirements' → 'scaffolding' is already present for test-case-extractor; drop 'testing' and 'requirements' or document the new terms with a starter-vocabulary extension. If a new tag genuinely fills a gap, add it to the starter vocabulary list in the spec rather than silently introducing it in an agent..
      Verify: After update: grep '^tags:' agents/code-security-reviewer.md should show no 'security'; agents/graphic-prompt-generator.md should show no 'design'; agents/i18n-completeness-checker.md should show no 'i18n' or 'frontend' (or the spec's starter vocabulary should be updated to include them); agents/test-case-extractor.md should show no 'testing' or 'requirements'..

- [ ] [claude/png-to-transparent-svg.AC5] The agent at agents/png-to-transparent-svg.md does not cite its authorising spec (spec/claude/png-to-transparent-svg) in the description frontmatter field or in the body. The spec-drift-audit context §Context explains this spec was created specifically to give the agent a discoverable reference, so the missing back-link defeats that stated goal.
      Where: /home/nolte/repos/github/claude-shared/agents/png-to-transparent-svg.md — description field (line 3) and body.
      Fix: Add a spec citation to the agent. In the description field, append something like: 'Governed by spec/claude/png-to-transparent-svg/en.md.' Alternatively, add a line in the Why-this-is-an-agent section: 'This agent is governed by `spec/claude/png-to-transparent-svg/en.md`.'.
      Verify: grep -i 'spec/claude/png-to-transparent-svg' /home/nolte/repos/github/claude-shared/agents/png-to-transparent-svg.md should return at least one match..

- [ ] [claude/png-to-transparent-svg.AC6] AC-6 states the minimum tools list should be '(Read, Write, Bash, Glob)'. The agent declares 'tools: Read, Bash, Glob' — Write is absent. The agent body explains the omission (image writes happen inside Python via Bash, so Write is not needed in the harness allow-list), but this creates a mismatch between the spec and the implementation. Either the spec must be updated to remove Write from the stated minimum, or the agent must add Write to its tools list.
      Where: /home/nolte/repos/github/claude-shared/agents/png-to-transparent-svg.md — frontmatter line 'tools: Read, Bash, Glob'; spec AC-6 at /home/nolte/repos/github/claude-shared/spec/claude/png-to-transparent-svg/en.md line 45.
      Fix: Preferred: update spec AC-6 to reflect the agent's deliberate design — change '(Read, Write, Bash, Glob)' to '(Read, Bash, Glob)' since the agent body explicitly justifies the Write omission and the reasoning is sound. Alternative: add Write to the agent's tools frontmatter if the spec intent was to allow but not require Write..
      Verify: After whichever change is made, the spec AC-6 tools list and the agent frontmatter 'tools:' line must agree. Re-run spec-drift-audit to confirm no mismatch..

- [ ] [claude/resumable-work.persistence-location] The spec MUST (§Persistence location) says 'when a downstream project consumes this spec, the `project-structure` scaffold step is the place to add the entry [`/.resume/`]'. The `project-structure-apply` skill's operative scaffold reference (`references/operations.md`, line 88) describes `.gitignore` patching but does not instruct adding `/.resume/`. Downstream repos scaffolded via `project-structure-apply` will not receive the required gitignore entry.
      Where: /home/nolte/repos/github/claude-shared/skills/project-structure-apply/references/operations.md — `.gitignore` scaffolding paragraph (line 88).
      Fix: Add a sentence to the `.gitignore` scaffolding paragraph in `references/operations.md` instructing the skill to ensure `/.resume/` is present in `.gitignore` when scaffolding or patching the file for a downstream repo (cross-referencing `spec/claude/resumable-work/`). Example addition: 'Also ensure `/.resume/` (anchored) is present per `spec/claude/resumable-work/` §Persistence location.'.
      Verify: Run `grep -c '/.resume/' skills/project-structure-apply/references/operations.md` — should return at least 1..

- [ ] [claude/resumable-work.scope-of-applicability] Three skills with `resumable: true` use the abbreviated phrase 'Supports resume per `spec/claude/resumable-work/`.' in their `description:` instead of the spec-prescribed wording 'supports resume on re-invocation'. Affected: `skill-management` (line 3), `lektorat-apply` (line 3), `feature-decompose` (line 3).
      Where: /home/nolte/repos/github/claude-shared/skills/skill-management/SKILL.md:3, /home/nolte/repos/github/claude-shared/skills/lektorat-apply/SKILL.md:3, /home/nolte/repos/github/claude-shared/skills/feature-decompose/SKILL.md:3.
      Fix: Replace 'Supports resume per' with 'Supports resume on re-invocation per' in each of the three description fields so the wording matches the spec-prescribed clause and what operators see in the catalog is consistent..
      Verify: Run `grep -c 'Supports resume per' skills/skill-management/SKILL.md skills/lektorat-apply/SKILL.md skills/feature-decompose/SKILL.md` — all three should return 0. Run `grep -c 'Supports resume on re-invocation per' ...` — all three should return 1..

- [ ] [claude/skill-management.frontmatter-description-third-person] The description opens with the bare imperative 'Author or revise' rather than the third-person present form the spec requires ('Authors or revises'). The spec MUST states: write description in third person ('Generates…', 'Reviews…'); imperative / bare-infinitive form is non-conformant. The local validator's pronoun-only check (I/you/we) does not catch this.
      Where: skills/skill-management/SKILL.md line 3 — description field.
      Fix: Change the description opening from 'Author or revise Claude Code skills…' to 'Authors or revises Claude Code skills…' (and correspondingly 'Scaffolds the target folder…' already conforms). Also change 'Invoke when' to 'Invokes when' or re-order so the third-person clause leads..
      Verify: Run python3 scripts/validate_skills.py and confirm no description-third-person finding; also manually verify the opening verb ends in -s (third-person singular present)..

- [ ] [claude/skill-management.operations-heading-numbered] '### Review / audit' is an unnumbered heading with a slash-compound form. The spec MUST states sub-operations must be titled as '### N. <verb>' (numbered); alphabetic letters and Step N are non-conformant. The slash-compound 'Review / audit' is doubly non-conformant: unnumbered and compound.
      Where: skills/skill-management/SKILL.md line 72 — '### Review / audit' heading.
      Fix: This section is an out-of-scope redirect, not an actual operation. Remove it from the ## Operations block and fold its content into a brief prose note at the top of ## Operations (e.g., 'Review and audit are out of scope — invoke skill-review.'). Alternatively, if retained as an operation, rename to '### 3. audit' and restrict its body to audit-related steps..
      Verify: After the fix, confirm every H3 under ## Operations is numbered ('### N. <verb>') and that <verb> is from the closed vocabulary..

- [ ] [claude/skill-management.operations-verb-vocabulary] Operation sub-headings use verbs outside the closed vocabulary. '### 1. Create a new skill' uses 'create' (should be 'scaffold' — greenfield create). '### 2. Revise' uses 'revise' (should be 'update' — mutate an existing artefact). The spec MUST states: each operation MUST be named with one verb from the closed set {audit, scaffold, patch, apply, migrate, run, update, close}.
      Where: skills/skill-management/SKILL.md lines 55 and 68 — operation sub-headings.
      Fix: Rename '### 1. Create a new skill' to '### 1. scaffold' and '### 2. Revise' to '### 2. update'. Update any internal cross-references (e.g., SKILL.md §Operations references in Gotchas or body prose)..
      Verify: Confirm every ### heading under ## Operations starts with a numbered form '### N. <verb>' where <verb> is exactly one word from the closed vocabulary..

- [ ] [claude/skills-agents-sweep.acceptance-criteria] Acceptance Criterion 4 fails: no consolidated sweep report exists on disk and no sweep is currently open. The .audits/skills-agents-sweep/ directory has never been committed to the repository. The spec's acceptance criterion requires the most recent report to have been opened within the last six months, or a sweep to be currently open.
      Where: /home/nolte/repos/github/claude-shared/.audits/ (no skills-agents-sweep subdirectory).
      Fix: Create the .audits/skills-agents-sweep/ directory (add a .gitkeep per Precondition 4 of the skill) and immediately open a sweep via `nolte-shared:skills-agents-sweep audit`. The spec was introduced 9 days ago and 20+ artifacts have been added since; the full-inventory sweep is overdue..
      Verify: Confirm `git ls-files .audits/skills-agents-sweep/` returns at least one file; confirm the report's YAML frontmatter has status: open..

- [ ] [claude/skills-agents-sweep.triggers] The SHOULD trigger for running a sweep when more than five new skills or agents have landed since the last closed sweep has been breached. Since the spec was introduced on 2026-05-21, at least 20 skill/agent commits have landed on develop (feat(skill), feat(agent), feat(agents) prefixes), and no sweep has ever been opened.
      Where: .audits/skills-agents-sweep/ (directory does not exist); 20+ skill/agent commits after 2026-05-21.
      Fix: Run `nolte-shared:skills-agents-sweep audit` to open a full-inventory sweep. Commit the consolidated report under .audits/skills-agents-sweep/<date>-<slug>.md with status: open to satisfy the lifecycle lock requirement..
      Verify: After the sweep report is committed, confirm git ls-files .audits/skills-agents-sweep/ shows the report file and its YAML frontmatter has status: open..

- [ ] [claude/skills-agents-sweep.triggers] The spec MUST trigger a sweep before each major plugin release (a release that increments the first version segment). While no v1.x.x release has occurred yet (current is v0.1.4, so the MUST trigger has not fired), the spec also SHOULD ensure this gate is enforced prospectively. More concretely: the skills-agents-sweep skill's Preconditions section does not include a check that a sweep has been run before proceeding with a major release, and there is no release-pipeline gate referencing the sweep. This means the MUST trigger could silently be skipped when a v1.0.0 release is cut.
      Where: spec/claude/skills-agents-sweep/en.md line 44 (MUST trigger); skills/skills-agents-sweep/SKILL.md Preconditions section; spec/project/release-automation/ (no reference to sweep gate).
      Fix: Add a check to the release-automation spec or skills-agents-sweep skill Preconditions that blocks a major-release promotion if no sweep report has been produced for the release candidate. Alternatively, document the gap as a tracked issue so the MUST is not silently violated at v1.0.0 cut..
      Verify: Confirm that spec/project/release-automation/en.md §Pre-publish verification references or links to the skills-agents-sweep MUST trigger, or that the skills-agents-sweep skill Preconditions include a major-release detection step..

- [ ] [code-security-audit.audit-coverage-software-integrity] The spec §Audit coverage MUST (line 45) requires covering 'software/data-integrity concerns' as part of the OWASP Top 10 categories. The agent's Step 2 procedure lists bullets for A01, A02, A03, A05, A07, plus supplementary categories (secret handling, input validation, AI/LLM, frontend) but has no bullet for software/data-integrity (OWASP A08: Software and Data Integrity Failures — unsigned updates, insecure deserialization, dependency integrity).
      Where: /home/nolte/repos/github/claude-shared/agents/code-security-reviewer.md lines 62–71 (Step 2 audit bullet list).
      Fix: Add a bullet to the Step 2 audit list for software/data-integrity (A08): unsigned or unverified update mechanisms, insecure deserialization, dependency integrity checks (lockfile pinning, hash verification), and unsafe use of serialization libraries..
      Verify: grep -n 'integrity\|A08\|deserializ\|software.*data' agents/code-security-reviewer.md — should return a matching bullet after the fix..

- [ ] [continuous-improvement.loop-quarterly-coverage-section] The Q2 2026 spec-drift audit (committed 2026-05-19) does not contain a '## Specialist coverage review' section. The spec MUST include this named section when the coverage review is folded into the drift-audit artifact, and MUST NOT let a drift audit (even a partial-trigger one) suppress the quarterly review. The spec was introduced 2026-04-23, making Q2 the first quarter in scope. The Q2 deadline is 2026-06-30 (31 days remain), so the review has not yet missed its window — but the committed spec-drift artifact cannot be retroactively amended and a standalone artifact has not been created.
      Where: /home/nolte/repos/github/claude-shared/.audits/spec-drift/2026-Q2.md — no '## Specialist coverage review' heading present.
      Fix: Before 2026-06-30: create a standalone specialist-coverage review artifact at .audits/continuous-improvement/2026-Q2.md by running continuous-improvement-triage `audit` operation. This satisfies the quarterly MUST via the standalone path. For future quarters: extend skills/spec-drift-audit/SKILL.md to include a step (after step 6 'Draft the artifact') that asks the operator whether to fold in the quarterly specialist-coverage review and, if yes, appends a '## Specialist coverage review' section..
      Verify: A .audits/continuous-improvement/2026-Q2.md artifact with `status: closed` (or a '## Specialist coverage review' section in the Q2 spec-drift artifact) exists before 2026-06-30..

- [ ] [continuous-improvement.loop-spec-drift-integration] The spec-drift-audit SKILL.md has zero references to the specialist-coverage review fold requirement. The continuous-improvement spec SHOULD fold the coverage review into the quarterly spec-drift-audit run, and MUST include a named section if folded. Without a prompt in the spec-drift-audit skill, operators have no signal to include the section, and the omission will recur every quarter.
      Where: /home/nolte/repos/github/claude-shared/skills/spec-drift-audit/SKILL.md — no mention of 'specialist coverage', 'continuous-improvement', or coverage review.
      Fix: Add to skills/spec-drift-audit/SKILL.md, in the audit operation after step 6 (draft the artifact): 'Quarterly coverage review (per spec/project/continuous-improvement/): if this is a full quarterly audit OR if the current calendar quarter lacks a standalone coverage-review artifact, ask the operator whether to fold the specialist-coverage review into this artifact. If yes, append a ## Specialist coverage review section (populated by running the continuous-improvement-triage audit operation in fold mode).' Add a see_also: reference to continuous-improvement-triage..
      Verify: A new spec-drift audit run prompts the operator about the coverage-review fold, and the resulting artifact either contains '## Specialist coverage review' or the operator explicitly chose standalone..

- [ ] [continuous-improvement.specialist-dispatch-workflow-health-tension] The workflow-health spec §74 declares recording the specialist in Risk/rollout notes as a SHOULD, while continuous-improvement §54 raises the same requirement to a MUST for 'every remediation PR'. The continuous-improvement spec Non-Goals (§21) simultaneously say 'workflow-health remains the authority for workflow-scoped remediation', creating an unresolved normative tension: for workflow-health-originated findings (stale-pin fixes, defects, secret drift), it is ambiguous whether the SHOULD from workflow-health or the MUST from continuous-improvement governs. The spec cannot simultaneously say 'identical rules (not weaker ones) apply to other finding sources' (§69) and 'workflow-health remains the authority for workflow-scoped remediation' (§21) without clarifying the precedence when the two specs disagree on level.
      Where: /home/nolte/repos/github/claude-shared/spec/project/continuous-improvement/en.md §21 (Non-Goals) vs §54 (Traceability MUST) vs §69 (Relationship — 'identical rules'); cross-reference /home/nolte/repos/github/claude-shared/spec/project/workflow-health/en.md line 74 (SHOULD).
      Fix: Resolve the precedence explicitly in the continuous-improvement spec. Either: (a) add a carve-out in §54 acknowledging that for workflow-health-originated findings, workflow-health's SHOULD still governs until that spec is updated; or (b) remove the carve-out from §21 and state that continuous-improvement §54 upgrades workflow-health §74 from SHOULD to MUST across all finding sources, and update workflow-health §74 to MUST in lockstep..
      Verify: After the fix, read both specs and confirm there is a single unambiguous level (MUST or SHOULD) for recording the specialist field in Risk/rollout notes for workflow-health-originated remediation PRs..

- [ ] [cookiecutter-template-authoring.rendered-project-conformance-audiences] The agent's Precondition 6 reads five specs at runtime (project-structure, pull-request-workflow, branching-model, release-automation, release-skill-layer) but omits spec/project/audience-identification/, which the agent's own introduction (line 26) declares as part of the bound corpus. This means the runtime MUST checklist compiled at Precondition 7 will not include the audience-identification MUST (AUDIENCES.md stub or ## Audiences section), so the agent cannot enforce §Rendered-project conformance bullet 6 at the operational precondition-check level even though the introduction claims it does.
      Where: /home/nolte/repos/github/claude-shared/agents/cookiecutter-template-author.md — Precondition 6 (lines 80–81) vs. line 26 (bound corpus declaration).
      Fix: Add spec/project/audience-identification/<lang>.md to the list of specs read in Precondition 6, consistent with the declared bound corpus in the agent's introduction. Example addition: 'Then read every canonical file under: ... `spec/project/audience-identification/<lang>.md`.' This aligns the operational precondition with the stated bound corpus and ensures the AUDIENCES.md MUST is included in the per-spec checklist..
      Verify: After the fix, re-read Precondition 6 and confirm it lists six spec paths, including spec/project/audience-identification/<lang>.md. Optionally invoke the agent in scaffold mode and confirm the spec-conformance audit table in the report includes an audience-identification row..

- [ ] [dependency-audit.audit-artifact-required-fields] The Step 5 report template is missing spec-required artifact fields: (1) Date of the audit run, (2) Trigger type (quarterly / pre-release / manifest-change), (3) Git revision audited, (4) auditor versions (template says 'Auditors run: <list>' but not '<tool>@<version>'). Per-finding response decision is also absent (the spec MUST includes the response decision for each finding). The SHOULD to link to the prior audit artifact is not implemented.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Step 5 report template; /home/nolte/repos/github/claude-shared/agents/dependency-audit-scanner.md — Output shape section.
      Fix: Expand the report template header to include: Date: <ISO 8601>, Trigger: <quarterly|pre-release|manifest-change|ad-hoc>, Git revision: <short SHA>, Prior audit: <path or issue URL or 'none'>. Change 'Auditors run: <list>' to 'Auditors run: <tool@version list>'. Add a Response decision field to each finding entry. Mirror the tool-version change in the agent's Output shape..
      Verify: Template header contains Date, Trigger, Git revision, Prior audit, and tool@version. Each finding entry has a response-decision placeholder..

- [ ] [dependency-audit.execution-mechanics-skip-blocked] The spec §Execution mechanics MUST NOT says 'silently skip a subroot whose auditor isn't installed; the audit reports the skip and the install hint, and the gate treats a skipped subroot as blocked, not pass'. The SKILL.md Hard rules say 'Never silently skip an auditor that isn't installed. Emit an install hint and record the skip in the Health section.' The 'blocked, not pass' gate treatment is absent from both the Hard rules and the Operations steps — the skill records the skip but does not declare the gate outcome as 'blocked'.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Hard rules section; Step 3.
      Fix: Add to Hard rules: 'A subroot whose auditor is not installed is reported as blocked in the Health section; the overall gate result is blocked, not pass, until the auditor is installed and the subroot is audited.' Mirror this in the report template's Health section..
      Verify: Search SKILL.md and the agent for 'blocked' — neither file uses the term in the gate-outcome sense required by the spec..

- [ ] [dependency-audit.ignore-discipline-native-location] The skill does not enforce that ignore lists be stored in the auditor-native location (e.g. pyproject.toml [tool.pip-audit], .npm-audit-ignore.json). Step 6 offers to add an entry to the auditor's ignore list but does not specify the native location requirement or warn against free-form prose.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Step 6, Hard rules.
      Fix: In Step 6, when drafting an ignore entry, specify the target location per ecosystem: pyproject.toml [tool.pip-audit] for Python, .npm-audit-ignore.json for Node/npm. Add a Hard rule: 'Never store ignore entries as free-form prose that only the audit artifact sees; use the auditor-native location so the ignore list is picked up on direct auditor invocations.'.
      Verify: Step 6 names the per-ecosystem native location. Hard rules contain the native-location constraint..

- [ ] [dependency-audit.ignore-discipline-no-global-silence] The MUST NOT against silencing a finding globally without a valid-until date ('--ignore-vuln <id>' without a date) is absent from the Hard rules.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Hard rules.
      Fix: Add a Hard rule: 'Never add a dateless global ignore entry (e.g. --ignore-vuln <id> without a valid-until date) just to make the gate green. Every ignore entry must carry a valid-until date and a rationale; dateless silencing violates the spec.'.
      Verify: Hard rules section contains the no-dateless-silence constraint..

- [ ] [dependency-audit.ignore-discipline-required-entry-fields] The skill does not specify the four required fields every ignore entry must carry (advisory ID, affected package, valid-until ISO 8601 date, one-line rationale), nor does it implement the gate that fails the audit on entries missing these fields.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Step 6.
      Fix: In Step 6 (and in a new ignore-discipline subsection if clarity warrants), enumerate all four required fields when drafting an ignore entry. Add audit-gate logic: 'Before finalising the report, scan any existing ignore entries in the discovered ignore list; if any entry is missing advisory ID, affected package, valid-until, or rationale, add a finding of severity warning and note that the audit cannot be marked clean until those fields are populated.'.
      Verify: Step 6 lists all four required fields when drafting an entry. The skill body describes the pre-report validation pass over existing entries..

- [ ] [dependency-audit.response-to-findings-revisit-ignore-entries] The MUST to revisit every 'ignore with rationale' entry on its valid-until date and the MUST NOT on renewal without a fresh rationale are not present in the skill.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Step 6, Hard rules.
      Fix: In Step 6, after the ignore-with-valid-until offer, add: 'Remind the operator that this entry MUST be revisited by the valid-until date and may not be renewed without a fresh rationale.' Add a matching Hard rule..
      Verify: Step 6 and Hard rules both encode the revisit-on-expiry and no-renewal-without-rationale constraints..

- [ ] [dependency-audit.response-to-findings-three-options] The three-option response taxonomy (upgrade / ignore with rationale / accept as known) from §Response to findings is not surfaced in the skill. Step 6 mentions upgrade and ignore-with-valid-until but 'accept as known' does not appear, nor is the response window logic (7-day / 30-day / quarterly / best-effort) encoded. The MUST NOT against accept-as-known for critical/high is also absent.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Step 6 (Offer follow-up actions), Hard rules.
      Fix: Revise Step 6 to name all three response options explicitly. Add a note on response windows per severity. Add a Hard rule: 'Never offer accept-as-known for critical or high findings; that response is reserved for medium/low tiers per the spec.'.
      Verify: Step 6 and Hard rules name all three responses; the critical/high guard is explicit..

- [ ] [dependency-audit.scope-transitive-required] The spec §Scope MUST requires covering transitive dependencies, not just direct ones, and requires the report to distinguish the two. The SKILL.md Step 3 records 'path (direct or transitive via <parent>)', which satisfies the distinction requirement. However, the SKILL.md Gotchas section notes the skill stops when no lockfile is present rather than producing a misleading direct-only report. The stop behaviour is correct, but neither the Hard rules nor Operations encode the MUST to cover transitive dependencies as an explicit requirement, leaving the implementation reliant on a Gotchas note rather than a binding rule.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Hard rules section.
      Fix: Add to Hard rules: 'Always include transitive dependencies; a lockfile-absent run MUST stop with a clear message rather than producing a direct-only report.' This elevates the Gotchas observation to a binding constraint matching the spec MUST..
      Verify: Check SKILL.md Hard rules for any mention of transitive dependencies — none present..

- [ ] [dependency-audit.severity-classification-cvss-scale] The skill does not encode the spec-mandated severity scale. CVSS thresholds (>=9.0 critical, 7.0-8.9 high, 4.0-6.9 medium, <4.0 low) and the 'unknown treated as high until classified otherwise' rule are absent from both SKILL.md and the agent. The skill records the auditor's native severity labels but provides no mapping or validation logic.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Operation 3, Hard rules; /home/nolte/repos/github/claude-shared/agents/dependency-audit-scanner.md — Phase 4.
      Fix: Add a Severity mapping section to SKILL.md (and mirror it in the agent's Phase 4 normalise-and-filter step) that enumerates the CVSS boundaries and states: 'when the auditor's native tag is unknown, treat as high until the advisory is formally classified'..
      Verify: After update, confirm the Severity mapping section lists all four levels with CVSS ranges and the unknown-as-high rule. Run /nolte-shared:spec-drift-audit on dependency-audit and confirm this criterion passes..

- [ ] [dependency-audit.severity-classification-no-downgrade] The MUST NOT against downgrading a severity on local judgement is absent from the skill. The spec requires that any disagreement with the auditor's classification be expressed as an ignore-list entry with explicit rationale, never as a silent reclassification. The Hard rules in SKILL.md do not include this guard.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Hard rules section.
      Fix: Add a Hard rule: 'Never downgrade a severity label on local judgement. If the auditor's classification is disputed, express the disagreement as an ignore-list entry with advisory ID, valid-until date, and rationale. Reclassification outside that mechanism violates the spec.'.
      Verify: Hard rules section contains the no-downgrade constraint referencing ignore-list as the only legitimate disagreement path..

- [ ] [design/graphic-prompt-authoring.prompt-document-output] The agent's see_also list references only png-to-transparent-svg but omits gemini-image-generation, which the spec explicitly names as the downstream consumer of the authored prompt documents (§References [R2]). Agent-management §Use-case metadata SHOULD declare see_also entries when overlap with other artefacts is likely.
      Where: /home/nolte/repos/github/claude-shared/agents/graphic-prompt-generator.md lines 17-18 (frontmatter see_also).
      Fix: Add 'gemini-image-generation' to the see_also list in the agent frontmatter, since the spec's References [R2] identifies it as the direct downstream consumer..
      Verify: grep -n 'see_also' agents/graphic-prompt-generator.md should show both png-to-transparent-svg and gemini-image-generation..

- [ ] [docs-audience-tracks.audience-to-track-mapping] docs/en/index.md and docs/en/getting-started/nutzung.md (and their DE counterparts) declare audience: [maintainer, downstream-user] but carry track: developer-docs. AUDIENCES.md maps downstream-user → user-docs, so both pages trigger an audience-track mismatch — a violation of the spec's no-contradiction rule (§Audience-to-track mapping). The mismatch is symmetrical across EN and DE trees (4 pages total).
      Where: docs/en/index.md line 3-5; docs/en/getting-started/nutzung.md line 3-5; docs/de/index.md; docs/de/getting-started/nutzung.md.
      Fix: Resolve the contradiction deliberately by one of two paths: (a) change track: on these pages to user-docs and update the content to meet the user-docs contract, OR (b) override the audience artefact entry for downstream-user with a one-line rationale explaining why the interaction surface is developer-docs rather than user-docs (e.g. 'plugin slash-command invocation is a developer-class surface; downstream-user track overridden to developer-docs'). Apply the same fix symmetrically to all four affected pages..
      Verify: No page in docs/en/ or docs/de/ has an audience value whose track mapping in AUDIENCES.md differs from the page's declared track: value..

- [ ] [docs-audience-tracks.developer-docs-content-contract] The developer-docs interface/contracts MUST block is incomplete. The spec requires every interface the project exposes (public CLI slash commands, plugin manifest schema, marketplace.json contract, skill/agent YAML frontmatter contract) to be listed. docs/en/references/specs/skill-management.md and agent-management.md cover authoring rules excerpts but do not enumerate all public interfaces the plugin exposes to callers.
      Where: docs/en/references/specs/skill-management.md; docs/en/references/specs/agent-management.md.
      Fix: Extend the references section with a dedicated interface/contracts page (or extend the existing skill-management and agent-management reference pages) to enumerate: all bundled slash commands (namespaced skill names), the plugin.json and marketplace.json schemas, the per-skill YAML frontmatter contract (name, description, tags, phase, summary, use_when, dont_use_when, resumable, etc.), and the per-agent YAML contract. Tag track: developer-docs, content_mode: reference..
      Verify: docs/en/ contains a stable interface/contracts block listing every public interface the plugin exposes, reachable from Home within one navigation step..

- [ ] [docs-audience-tracks.developer-docs-content-contract] Three of the four developer-docs SHOULD blocks are missing: (1) no architecture-overview block (context, building blocks, load-bearing decisions); (2) no glossary block (project-specific terminology, alphabetically sorted); (3) no developer-troubleshooting block (contributor/operator failure modes with symptom/cause/workaround/resolution). The contribution-flow block partially exists in guides/contributing.md but does not reference the spec-based branching/PR/quality-gate model as the spec prescribes.
      Where: docs/en/ — no architecture-overview, no glossary, no developer-troubleshooting page; guides/contributing.md lacks spec cross-references.
      Fix: Add the three missing SHOULD blocks: (a) an architecture-overview page in guides/ or as an H2 on the Home page with context, building blocks (skills, agents, specs, plugin manifest, docs pipeline), and decisions linked to ADRs; (b) a glossary page in references/ with project-specific terms (skill, agent, track, bounded context, etc.); (c) a developer-troubleshooting section in guides/contributing.md or a dedicated page. Extend contributing.md to cross-reference spec/project/branching-model/, spec/project/pull-request-workflow/, and spec/project/quality-gate/..
      Verify: docs/en/ contains architecture-overview, glossary, and developer-troubleshooting blocks, each tagged track: developer-docs with the appropriate content_mode value..

- [ ] [docs-audience-tracks.home-page-contract] docs/en/index.md and docs/de/index.md do not contain a track-routing paragraph. The spec SHOULD requires the Home page to name both tracks when the repository serves both (per AUDIENCES.md, it does) — a single paragraph pointing end users at the user-docs entry and contributors at the developer-docs entry. The current Home page points only to developer-oriented sections.
      Where: docs/en/index.md lines 46-50 (Next section); docs/de/index.md.
      Fix: Add a track-routing paragraph to docs/en/index.md (and its DE counterpart) that explicitly names both tracks and gives each audience a clear entry point — e.g. 'If you want to use the plugin in your project, start at [Getting Started](getting-started/index.md). If you want to contribute to or extend the plugin, start at [Development](guides/development.md).' Adjust when/if the user-docs track gap (Critical finding 1) is resolved to point end users at the user-docs landing page instead..
      Verify: docs/en/index.md contains a paragraph that names both the user-docs and developer-docs entry points, symmetric with docs/de/index.md..

- [ ] [docs-freshness.ac4-artifact-date-trigger] The agent's output template Scope section is also missing the 'date' and 'trigger' (quarterly / pre-release / PR-change) fields. Spec §Audit artifact (line 69) MUST includes both in every artifact, and AC#4 (line 83) MUST requires them. The auditor only flagged the missing Git revision but date and trigger are equally absent from the Scope block (lines 91-95).
      Where: /home/nolte/repos/github/claude-shared/agents/docs-freshness-checker.md lines 91-95 (Scope block in output template).
      Fix: Add '- Date: <ISO date>' and '- Trigger: <quarterly | pre-release | PR-change>' lines to the Scope block in the output template so all three required metadata fields (date, trigger, Git revision) are present..
      Verify: Read agents/docs-freshness-checker.md lines 91-95 and confirm 'Date:' and 'Trigger:' entries appear in the Scope block alongside the Git revision entry..

- [ ] [docs-freshness.ac4-artifact-git-revision] The agent's output template Scope section does not include the audited Git revision field. AC#4 requires every artifact to record the audited Git revision.
      Where: /home/nolte/repos/github/claude-shared/agents/docs-freshness-checker.md lines 87-95 (Scope block in output template).
      Fix: Add '- Git revision: <git rev-parse HEAD>' to the Scope block in the output template so every generated artifact automatically includes the revision..
      Verify: An artifact produced by the agent contains a 'Git revision:' line in the Scope section..

- [ ] [docs-freshness.ac6-agent-output-1to1-categories] The agent's output summary table (6 rows) does not achieve 1-to-1 coverage of the 11 spec-defined drift categories. Missing from the summary table: 'Track-frontmatter drift', 'Content-mode drift', 'Audience-track mismatch'. 'Content-staleness delta' is embedded under Language parity instead of having its own row. AC#6 requires output that maps 1-to-1 onto the categories declared in the spec.
      Where: /home/nolte/repos/github/claude-shared/agents/docs-freshness-checker.md lines 98-106 (Summary table in output template).
      Fix: Add four rows to the summary table: 'Content staleness', 'Track-frontmatter', 'Content-mode', 'Audience-track'. Split the existing Language parity row to be parity-only, and add a separate Content staleness row..
      Verify: Agent output template summary table has exactly one row per spec category (11 data rows + Total). Track-frontmatter, Content-mode, and Audience-track each appear as a named row..

- [ ] [docs-freshness.categories-adr-index-generated-skip] Agent Phase 5 (ADR hygiene) unconditionally checks adr/index.md for index drift. The spec MUST requires skipping the drift check when index.md is declared generated (e.g. via a 'last_updated: generated' frontmatter marker or a generator hook declaration). The agent has no such detection logic.
      Where: /home/nolte/repos/github/claude-shared/agents/docs-freshness-checker.md lines 250-254 (Phase 5 Index drift block).
      Fix: Before running index-drift checks, read adr/index.md frontmatter. If it carries 'last_updated: generated' (per spec/project/mkdocs-structure/ §Per-page structure) or a declared generator hook, skip the ADR-index-drift phase for that index and note the skip in the report..
      Verify: Agent Phase 5 documents the generated-index skip logic; running against a repo with a generated adr/index.md produces no false ADR-index-drift findings..

- [ ] [docs-freshness.triggers-must-pre-release] No pre-release docs-freshness gate is wired in the CI/CD pipeline. The spec MUST requires running the audit before every release tag that includes documentation changes; neither release-publish.yml nor release-cd-deliver-docs.yml invokes the agent.
      Where: /home/nolte/repos/github/claude-shared/.github/workflows/release-publish.yml, /home/nolte/repos/github/claude-shared/.github/workflows/release-cd-deliver-docs.yml.
      Fix: Add a workflow step (or a manual pre-release checklist item in the release-publish-trigger skill) that runs the docs-freshness-checker agent and fails the release if any critical findings are unresolved or unwaiverred..
      Verify: Trigger a release with a docs change and confirm the freshness check runs and blocks on critical findings..

- [ ] [docs-freshness.triggers-should-pre-pr] The SHOULD trigger for pre-PR freshness gate is unmet. No CI job or pre-commit hook runs docs-freshness when a PR modifies documentation.
      Where: /home/nolte/repos/github/claude-shared/.github/workflows/ci.yml, /home/nolte/repos/github/claude-shared/.pre-commit-config.yaml.
      Fix: Add a CI job conditioned on docs/** path changes that runs the docs-freshness-checker agent and posts a summary comment on the PR, or document in the pull-request-workflow spec that this SHOULD is intentionally deferred..
      Verify: Open a PR that touches docs/ and observe a docs-freshness check result in the PR checks or PR description..

- [ ] [i18n-completeness.inputs-config-source-attribution] The spec §Inputs MAY clause contains an embedded MUST: when the optional `project/i18n-audit.yml` config file is present, the report MUST state, per resolved input, whether the value came from the config file, an operator argument, or discovery. The agent does not mention `project/i18n-audit.yml` at all and does not instruct the agent to attribute the source of resolved inputs in the report, so if a consumer creates the config file, the attribution requirement will silently go unmet.
      Where: /home/nolte/repos/github/claude-shared/agents/i18n-completeness-checker.md — Step 1 discovery prose (lines 56-59) and the report Scope line (line 84); no reference to the config file or per-input source attribution..
      Fix: Add a Step 1 sub-bullet instructing the agent to check for a `project/i18n-audit.yml` file, apply its values with precedence over discovery, and include a per-input source note in the report Scope line (e.g. `reference: en [from config]`, `source roots: src/ [operator-named]`, `locale dir: locales/ [discovered]`)..
      Verify: After the fix, the agent body mentions `project/i18n-audit.yml` and the report template Scope line shows source attribution tokens. A grep for 'i18n-audit.yml' in the agent file should return a match..

- [ ] [i18n-completeness.output-icu-opaque-caveat] The spec §Output MUST requires that wherever the agent reports placeholder-parity findings, it states that parity is checked at simple-placeholder granularity (`{{var}}` / `{var}` / `%s`) and that ICU MessageFormat plural and select bodies are treated as opaque strings, so consumers are not misled. The agent body describes the `{{var}}` / `{var}` / `%s` parity check in Step 4 and the report template, but neither the Step 4 prose, the report template, nor the Quality rules section contains any instruction to add the ICU-opaque disclaimer to the report output.
      Where: /home/nolte/repos/github/claude-shared/agents/i18n-completeness-checker.md — Step 4 (line 75) and the report template (lines 81-106); the ICU-opaque caveat is absent from both..
      Fix: Add an explicit instruction in Step 4 and/or the report template telling the agent to append a caveat note under placeholder-parity findings stating: parity is checked at simple-placeholder granularity (`{{var}}` / `{var}` / `%s`) and ICU MessageFormat plural and select bodies (e.g. `{count, plural, …}`) are treated as opaque strings, not structurally validated..
      Verify: After the fix, confirm the agent body contains language equivalent to the spec's §Output MUST on ICU opaque strings. A grep for 'ICU' or 'opaque' in the agent file should return a match..

- [ ] [lektorat.outputs-pipeline-metadata] Example 01 instructs placing the DE pipeline name and version in run.json (step 8), contradicting both the spec's MUST (pipeline_metadata belongs exclusively in findings.json) and the SKILL.md hard rule that explicitly prohibits putting pipeline metadata in run.json.
      Where: /home/nolte/repos/github/claude-shared/skills/lektorat-apply/examples/01-audit-bilingual-repo.md line 24.
      Fix: Remove 'the DE pipeline name and version' from the run.json description in example 01 step 8. run.json should record only: operation, severity floor, resolved scope summary, and ran_at. Pipeline metadata (tool, version, configured_path) lives solely in findings.json's pipeline_metadata block per spec §Outputs and SKILL.md hard rules..
      Verify: After fix: confirm step 8 of example 01 no longer references DE pipeline in run.json; confirm SKILL.md hard rules and spec §Outputs still agree that pipeline_metadata.de goes in findings.json only..

- [ ] [mermaid-diagrams.authoring-rules] The `graph TD` blocks in the development-lifecycle pages also violate the authoring-rules MUST that requires an explicit direction declaration on the first line of every `flowchart` block. Using `graph TD` is not a `flowchart` block and therefore sidesteps the direction-declaration rule entirely, in addition to the catalog-conformance issue already captured in finding [0].
      Where: /home/nolte/repos/github/claude-shared/docs/de/guides/development-lifecycle.md line 19; /home/nolte/repos/github/claude-shared/docs/en/guides/development-lifecycle.md line 19.
      Fix: Replace `graph TD` with `flowchart TD` (or `flowchart TB` — both are equivalent; `TD` and `TB` are Mermaid synonyms) on line 19 of both files. This simultaneously satisfies the catalog conformance rule and the explicit-direction MUST..
      Verify: After the change, grep for `^graph` in docs/de/guides/development-lifecycle.md and docs/en/guides/development-lifecycle.md returns no results, and each Mermaid fence opens with `flowchart TD` or `flowchart TB`..

- [ ] [mermaid-diagrams.diagram-catalog] Two Mermaid blocks use the legacy graph TD keyword instead of the spec-catalog flowchart TD keyword. The spec diagram catalog lists flowchart as the only supported type for dependency/lifecycle graphs; graph is the deprecated Mermaid v9 alias and is not in the catalog. No explicit reason for deviating is captured on either page.
      Where: /home/nolte/repos/github/claude-shared/docs/de/guides/development-lifecycle.md line 19; /home/nolte/repos/github/claude-shared/docs/en/guides/development-lifecycle.md line 19.
      Fix: Replace graph TD with flowchart TD in both files. The subgraph and edge syntax is identical between the two keywords, so no further changes are needed..
      Verify: grep -n 'graph TD' docs/de/guides/development-lifecycle.md docs/en/guides/development-lifecycle.md should return no matches after the fix..

- [ ] [permission-allowlist.AC4] PR bodies for the two settings.json-touching PRs (#7 and #18) name the added entries and characterise them as read-only, but do not walk the three selection criteria (frequency evidence, read-only justification, not-autoallowed confirmation) per entry. AC#4 requires 'the PR body justified each change.'
      Where: PR #7 Changes section and PR #18 Risk/rollout notes (retrieved via gh pr list --state merged --base develop --search '.claude/settings.json').
      Fix: The next allowlist PR (the cadence-review change above) should serve as the corrective model: its PR body Risk/rollout notes must explicitly walk all three criteria for each added entry and state the removal reason for any pruned entry, satisfying AC#4 going forward. No retroactive PR edits are needed since the historical PRs are already merged..
      Verify: The next merged PR that touches .claude/settings.json contains a Risk/rollout notes block listing every added entry with (a) frequency evidence, (b) read-only justification, (c) not-autoallowed confirmation, and every removed entry with its removal reason..

- [ ] [permission-allowlist.SHOULD-narrowest-form] Three prefix-form entries — Bash(task lint *), Bash(task test*), Bash(task docs *) — were added in PR #18 without any documented evidence of flag-variance being observed. The spec's SHOULD requires the prefix form only when flag variance has been observed; otherwise the exact form (Bash(task lint)) is correct.
      Where: /home/nolte/repos/github/claude-shared/.claude/settings.json lines 5, 7, 9 (task lint*, task test *, task docs*); PR #18 Risk/rollout notes.
      Fix: Open a chore/allowlist-narrow-task-targets branch. Remove Bash(task lint *), Bash(task test*), Bash(task docs *) from .claude/settings.json (keeping the exact forms Bash(task lint), Bash(task test), Bash(task docs)). In the commit message and PR body Risk/rollout notes, state the removal reason: 'no observed flag variance justifies the prefix form — exact forms already in the allowlist cover all usage.' Route through the standard PR flow per spec §Authoring flow integration..
      Verify: After merging, git log --oneline -- .claude/settings.json shows a chore-typed commit. The file no longer contains Bash(task lint*), Bash(task test *), or Bash(task docs*). task lint passes on the PR branch..

- [ ] [permission-allowlist.SHOULD-quarterly-drift-check] No quarterly allowlist drift check has been performed or recorded. The spec was introduced in April 2026; Q2 2026 is the first applicable quarterly window. The Q2 spec-drift audit artifact (.audits/spec-drift/2026-Q2.md) does not mention the permission-allowlist. No fewer-permission-prompts drift scan artifact exists.
      Where: /home/nolte/repos/github/claude-shared/.audits/spec-drift/2026-Q2.md (no permission-allowlist mention); no .audits entry for allowlist drift.
      Fix: The cadence-review fix above satisfies this SHOULD when it is documented as a quarterly drift check. Add to the PR body or a brief note in .audits/open-questions/ (or the living watch-list issue #229) that Q2-2026 allowlist drift check was performed as part of that PR..
      Verify: The chore/allowlist-cadence-review PR body or a linked issue/note explicitly records that this run serves as the Q2-2026 drift check for the permission-allowlist..

- [ ] [portfolio-inflight-management.AC-6] AC-6 requires that a synthetic security trigger fed into the audit produces a finding carrying security_relevance: true. No such synthetic-trigger test has been executed or documented anywhere in the skill examples, references, or audit artefacts. The axis is computed (defaulting false when no signals are present), but the detection path for security_relevance: true has never been exercised end-to-end.
      Where: skills/portfolio-inflight-triage/ — no example or test demonstrates the security_relevance: true path; .audits/portfolio-inflight/ — all artefacts show security_relevance=false for every finding.
      Fix: Add a worked example or test-run record demonstrating the security_relevance: true detection path. Options: (a) add skills/portfolio-inflight-triage/examples/04-security-relevance-detection.md showing a synthetic run where a dependency-audit CVE indicator or leaked-credential alert surfaces a finding with security_relevance: true and Critical severity; or (b) run the audit against a repo with a known open security advisory and document the finding in the next audit artefact, explicitly noting the synthetic trigger in the Processing log..
      Verify: An example file or audit artefact entry demonstrates that security_relevance: true produces a Critical-severity finding, with the matrix-axes input values showing security_relevance: true in the report line..

- [ ] [portfolio-inflight-management.AC-7] AC-7 requires that a synthetic cross-reference produces a finding carrying cross_repo_blocking: true. The cross_repo_blocking scan was executed in v2 (8 source files, zero matches), but the detection path for cross_repo_blocking: true has never been exercised. No synthetic test in the examples or artefacts demonstrates the true-positive path.
      Where: skills/portfolio-inflight-triage/ — no example demonstrates the cross_repo_blocking: true path; .audits/portfolio-inflight/2026-05-23-v2.md — Processing log confirms all findings have cross_repo_blocking: false.
      Fix: Add skills/portfolio-inflight-triage/examples/05-cross-repo-blocking-detection.md showing a synthetic run where a nolte/<repo>#<number> short cross-reference in a project/roadmap.md or project/sprints/*.md file triggers cross_repo_blocking: true and Critical severity on the referenced finding. The example should cite the exact spec rule from §Classification and prioritisation..
      Verify: The new example file demonstrates the full path from cross-reference text match to cross_repo_blocking: true in the finding's matrix-axes output, including how the skill performs the exact-string text scan per §Classification and prioritisation MUST..

- [ ] [portfolio-inflight-management.AC-9] AC-9 requires that running the audit against an empty in-flight surface produces a valid Findings-Report with zero findings under each severity rather than no report at all. No such artefact exists — every existing Findings-Report has non-zero findings. The zero-findings code path in step 9 of the skill ('always writes, never short-circuits on emptiness') has not been verified by an actual run.
      Where: /home/nolte/repos/github/claude-shared/.audits/portfolio-inflight/ — contains only 2026-05-23.md and 2026-05-23-v2.md, both with non-zero findings.
      Fix: Either (a) run a targeted single-repository audit against a repo with zero stalled items and confirm the resulting artefact contains the four mandatory sections with 'Critical: 0, Warning: 0, Suggestion: 0, Info: 0' in the ## Summary; or (b) add a note in the skill Gotchas section with a Processing log excerpt from such a confirmed zero-findings run. Option (a) produces a verifiable artefact..
      Verify: An artefact under .audits/portfolio-inflight/<YYYY-MM-DD>-zero-findings.md (or similar) exists with the four sections present and zero findings under every severity heading..

- [ ] [portfolio-management.mermaid-diagram] AC-10 (SHOULD) requires docs/<canonical_language>/portfolio/index.md to include a Mermaid diagram of the capability-to-repository mapping. The docs/en/portfolio/ directory does not exist, so the diagram is absent.
      Where: /home/nolte/repos/github/claude-shared/docs/en/portfolio/ (does not exist).
      Fix: When executing the Render operation (see Critical finding for docs/en/portfolio/), ensure the generated index.md includes a Mermaid flowchart LR diagram per spec/project/mermaid-diagrams/ portfolio spec showing the 6 declared capabilities and their peer references..
      Verify: docs/en/portfolio/index.md contains a ```mermaid block with flowchart LR content representing capabilities and cross-references..

- [ ] [portfolio-management.portfolio-yml-audience-identifiers] The portfolio.yml capability entries use prose labels for the audience field (e.g. 'Downstream Claude Code users in portfolio projects') rather than the id: identifiers declared in AUDIENCES.md (e.g. 'downstream-user', 'dogfooding-author'). The spec §Capability inventory line 46 says the audience field MUST be 'a list of audience identifiers cross-referenced with the project's project/audiences.md artefact per audience-identification'. AUDIENCES.md uses id: values (downstream-user, dogfooding-author, etc.); portfolio.yml uses the prose labels, not the machine-readable identifiers. The AC at line 102 requires 'valid project/portfolio.yml' with mandatory fields; whether prose-label audience entries satisfy 'audience identifiers' is ambiguous enough to be a Warning rather than Critical, but the spec's explicit use of 'identifiers' implies the id: values.
      Where: /home/nolte/repos/github/claude-shared/project/portfolio.yml (audience fields in every capability entry) vs /home/nolte/repos/github/claude-shared/AUDIENCES.md (id: fields).
      Fix: Replace prose audience labels in project/portfolio.yml with the id: values declared in AUDIENCES.md (e.g. 'downstream-user' instead of 'Downstream Claude Code users in portfolio projects', 'dogfooding-author' instead of 'Plugin author dogfooding inside this repo', 'external-contributor' for external contributors)..
      Verify: Run grep on project/portfolio.yml audience fields and confirm each value matches an id: entry in AUDIENCES.md..

- [ ] [portfolio-management.quarterly-cadence] The spec requires the portfolio audit to run on a quarterly cadence at minimum (§Portfolio audit MUST run quarterly). No scheduled GitHub Actions workflow or cron trigger implements this. On-demand invocation exists via the skill but the automated cadence is absent.
      Where: /home/nolte/repos/github/claude-shared/.github/workflows/ (no scheduled workflow for portfolio audit).
      Fix: Add a scheduled GitHub Actions workflow (e.g. .github/workflows/portfolio-audit-quarterly.yml) with schedule: cron: '0 9 1 */3*' (or equivalent quarterly trigger) that invokes the portfolio-audit skill or equivalent automation..
      Verify: The new workflow file contains a schedule.cron trigger; gh workflow list shows the workflow active..

- [ ] [portfolio/tech-stack-discovery.AC7] The capture skill (skills/tech-stack-capture/SKILL.md) exists and implements the §Discovery sequence in the correct order, but the AC requires a skill-review against spec/claude/skill-review/ to confirm the order — and no such review artefact exists in .audits/skill-review/.
      Where: /home/nolte/repos/github/claude-shared/.audits/skill-review/ (directory is empty; no tech-stack-capture review file present).
      Fix: Run /nolte-shared:skill-review against skills/tech-stack-capture/SKILL.md and write the result to .audits/skill-review/tech-stack-capture-<date>.md (or the pattern the skill-review skill uses). The review should explicitly confirm the 8-step discovery sequence order..
      Verify: ls .audits/skill-review/ shows a tech-stack-capture review file; the review document states the discovery-sequence order is confirmed against spec/portfolio/tech-stack-discovery/ §Discovery sequence per repository..

- [ ] [portfolio/tech-stack-discovery.AC8] The rendered portfolio documentation page under docs/<canonical_language>/portfolio/ does not exist. AC8 requires this page to include either the verbatim §Benefits section or a short paraphrase with a backlink to this spec. The spec SHOULD (§Benefits-documentation gate) also says to quote the Benefits section verbatim in the portfolio docs page.
      Where: /home/nolte/repos/github/claude-shared/docs/en/ (no portfolio/ subdirectory exists; docs/de/ likewise has no portfolio/ directory).
      Fix: Create docs/en/portfolio/index.md (and the DE counterpart) with at minimum the §Benefits section from spec/portfolio/tech-stack-discovery/en.md verbatim or paraphrased, plus a backlink to the spec. Register the page in mkdocs.yml nav..
      Verify: ls docs/en/portfolio/ shows at least one .md file; the file contains the five benefit bullets or a paraphrase and a link to spec/portfolio/tech-stack-discovery/..

- [ ] [portfolio/tech-stack.cross-references-with-portfolio-management.portfolio-management-reference] The spec MUST requires that spec/portfolio/portfolio-management/ canonical and every translation each carry a one-sentence cross-reference naming this spec as the owner of tech_stack:. The EN and DE translations of portfolio-management do reference spec/portfolio/tech-stack/ at §Tech-stack block, but the reference is embedded in a MUST-level normative rule about how to treat the key — it is not a standalone one-sentence pointer of the form the spec describes. The acceptance criterion at line 201 requires 'a one-sentence cross-reference to this spec naming it as the owner of the tech_stack: block', whereas what exists is a normative rule that incidentally names the spec. This is a borderline gap, but the acceptance criterion wording is distinct from the implementation.
      Where: /home/nolte/repos/github/claude-shared/spec/portfolio/portfolio-management/en.md line 58 and de.md line 58.
      Fix: Verify whether the existing line 58 wording satisfies the acceptance criterion's intent. If not, add an explicit one-sentence pointer such as 'See spec/portfolio/tech-stack/ for the full schema and inheritance contract governing this key.' The existing wording may be considered sufficient, making this Info rather than Warning..
      Verify: Read spec/portfolio/portfolio-management/en.md line 58 and compare against spec/portfolio/tech-stack/en.md acceptance criterion at line 201 ('carry a one-sentence cross-reference to this spec naming it as the owner of the tech_stack: block'). The EN line 58 does name the spec as owner ('defined entirely by spec/portfolio/tech-stack/'), so this is likely satisfied — auditor should confirm by checking whether the acceptance criterion is met in letter, not just spirit..

- [ ] [portfolio/tech-stack.documentation-rendering] The rendered portfolio inventory under docs/<canonical_language>/portfolio/ does not exist. Both docs/en/portfolio/ and docs/de/portfolio/ directories are absent. The spec requires a Global tech stack top-level section and per-repository tech-stack subsections with inherited/repo-specific/suppressed/regrouped badges organised by group first and kind second. This is a MUST, but the spec is still in Status: draft, so this is implementation gap rather than a merge blocker — downgraded to Warning.
      Where: /home/nolte/repos/github/claude-shared/docs/ — no portfolio/ subdirectory in either docs/en/ or docs/de/.
      Fix: Implement the Render operation in portfolio-audit to generate docs/en/portfolio/tech-stack.md and docs/de/portfolio/tech-stack.md from portfolio/tech-stack.yml plus each Portfolio-Member's project/portfolio.yml. The render must include: (1) a Global tech stack section with all portfolio-wide entries grouped by group then kind; (2) a per-repository section for claude-shared showing inherited entries (with 'inherited' badge), additions (with 'repo-specific' badge). Mark files as auto-generated per mkdocs-gen-files convention so task docs doesn't flag drift..
      Verify: Run task docs and confirm docs/en/portfolio/ and docs/de/portfolio/ are populated with tech-stack pages including a Global tech stack section and at minimum the claude-shared per-repository section with correctly badged entries..

- [ ] [project-structure.python-pyproject-toml] The repository contains Python source files (scripts/validate_skills.py, scripts/docs/gen_catalog.py) but has no pyproject.toml at the root. The spec's Python development section MUST requires a pyproject.toml declaring [build-system], project metadata, and Python tooling configuration when *.py files are present.
      Where: /home/nolte/repos/github/claude-shared/ — no pyproject.toml; Python files at /home/nolte/repos/github/claude-shared/scripts/validate_skills.py and /home/nolte/repos/github/claude-shared/scripts/docs/gen_catalog.py.
      Fix: Add a pyproject.toml at the repository root declaring at minimum [build-system], project metadata (name, version, license, authors), and tooling config (e.g. [tool.ruff], [tool.pytest.ini_options]). Since these scripts have no external runtime dependencies, a minimal pyproject.toml with a placeholder [project.optional-dependencies] or an empty [tool.ruff] section suffices..
      Verify: Run `python3 -c "import tomllib; tomllib.load(open('pyproject.toml','rb'))"` at the repository root and confirm [build-system] and [project] sections are present..

- [ ] [project-structure.python-requirements-txt] The repository contains Python source files in scripts/ but no requirements.txt at the repository root. The spec MUST requires a root-level requirements.txt when Python source code is present. The only requirements.txt found is docs/requirements.txt, which is the MkDocs documentation build list, not a project runtime dependency file.
      Where: /home/nolte/repos/github/claude-shared/ — missing root-level requirements.txt; docs/requirements.txt exists but covers only docs tooling.
      Fix: Create a requirements.txt at the repository root. If scripts/validate_skills.py and scripts/docs/gen_catalog.py have no external runtime dependencies beyond the standard library, a comment-only placeholder (# No external runtime dependencies for scripts/) is permitted by the spec until a real runtime dependency lands..
      Verify: Confirm requirements.txt exists at the repository root and every non-comment, non-blank line has a version specifier..

- [ ] [project-structure.python-venv-runtime] The spec MUST (line 105) requires all Python project dependencies to be installed and run inside a project-local virtual environment. The Taskfile runs 'python3 scripts/validate_skills.py' and 'python3 scripts/docs/gen_catalog.py' without creating or activating any venv. The gen_catalog.py script has an external runtime dependency (PyYAML/yaml) that is resolved from the system Python. No 'task install' or equivalent venv-creation target exists in Taskfile.yml.
      Where: /home/nolte/repos/github/claude-shared/Taskfile.yml lines 56 and 61 — python3 invoked directly; /home/nolte/repos/github/claude-shared/scripts/docs/gen_catalog.py line 30 imports yaml (external dep); no venv creation step anywhere in the Taskfile.
      Fix: Add a 'task install' target that creates .venv and runs 'pip install -r requirements.txt' (once root requirements.txt is added), then update the validate:skills and docs:catalog targets to run via .venv/bin/python3 or source .venv/bin/activate..
      Verify: Run 'grep -n venv Taskfile.yml' — should return at least one activation or interpreter path reference; currently returns nothing..

- [ ] [project/audience-identification.ac-readme-structure-reference] spec/project/readme-structure/en.md instructs the §Purpose section to explain 'who the intended consumers are' (line 46) but carries no cross-reference to spec/project/audience-identification/ or AUDIENCES.md. The acceptance criterion explicitly requires that readme-structure reference this spec at the point where it speaks of intended consumers.
      Where: /home/nolte/repos/github/claude-shared/spec/project/readme-structure/en.md line 46 (§Purpose MUST bullet) and the §Requirements section overall.
      Fix: In spec/project/readme-structure/en.md, add a note to the §Purpose MUST bullet (line 46) and/or a line in §Requirements referencing spec/project/audience-identification/: e.g. 'The intended-consumers clause here SHOULD reference the audience artifact produced under spec/project/audience-identification/ (AUDIENCES.md or the chosen alternative) rather than re-stating the audience list inline.'.
      Verify: grep -n 'audience-identification\|AUDIENCES' spec/project/readme-structure/en.md returns at least one hit..

- [ ] [project/audience-identification.ac-spec-drift-audit-flags-surface-mismatch] The acceptance criterion requires that spec-drift-audit can flag a module whose documented audiences no longer match its actual interaction surface. Neither the spec-drift-audit SKILL.md nor spec/project/spec-drift-audit/en.md contains any explicit instruction, checklist item, or detection heuristic for audience-to-surface drift. The general per-criterion loop could theoretically cover this, but the specific check is not enumerated, making it unverifiable and unreliable.
      Where: /home/nolte/repos/github/claude-shared/skills/spec-drift-audit/SKILL.md (Operations §1, per-criterion check step) and /home/nolte/repos/github/claude-shared/spec/project/spec-drift-audit/en.md (§Execution).
      Fix: Add an explicit bullet to the spec-drift-audit SKILL.md §Operations step 5 (per-criterion check) instructing the auditor to compare each audience's documented interaction surface in AUDIENCES.md against the actual public surfaces found in code (skills/, agents/), config (.github/, Taskfile.yml), and docs. Also add a corresponding acceptance criterion to spec/project/spec-drift-audit/en.md noting that a full-scope audit covers audience-surface mismatch..
      Verify: grep -n 'audience\|AUDIENCES\|interaction surface' skills/spec-drift-audit/SKILL.md returns at least one result in the Operations section..

- [ ] [project/docs-multilingual-authoring.symmetric-rename-delete] No skill or agent in the corpus declares a hard rule for symmetric rename or delete of docs/<lang>/ pages across all configured language trees. The spec MUST (§Authoring protocol) requires that renaming docs/<canonical_language>/a.md to docs/<canonical_language>/b.md MUST rename the counterpart in every other language tree in the same step, and deleting docs/<canonical_language>/foo.md MUST delete every counterpart. This case is unrepresented in all hard-rules corpora.
      Where: agents/audience-doc-author.md, skills/mkdocs-structure-apply/SKILL.md, skills/docs-audience-tracks-apply/SKILL.md, skills/docs-dry-refactor/SKILL.md, skills/mermaid-diagrams-apply/SKILL.md, skills/skill-agent-catalog-apply/SKILL.md, skills/portfolio-audit/SKILL.md.
      Fix: Add a hard rule to every documentation-producing skill and to audience-doc-author that explicitly covers rename and delete: renaming or deleting a page under docs/<canonical_language>/ MUST rename or delete the counterpart under every other docs/<lang>/ in the same operation; a partial rename or delete is a violation regardless of the skill's exit status..
      Verify: Check hard-rules sections in the above artefacts for an explicit rename/delete symmetry rule. AC item 8 test: simulate git mv docs/en/foo.md docs/en/bar.md via a documentation-producing skill and confirm the DE counterpart is renamed in the same step..

- [ ] [project/docs-multilingual-authoring.translation-quality-needs-review-marker] No documentation-producing skill or agent implements the needs-review marker rule. The spec MUST (§Translation quality and review) requires placing <!-- translation-status: needs-review --> as an HTML comment on the first body line after the frontmatter block when fidelity cannot be guaranteed. This rule is absent from all hard-rules corpora and from CI.
      Where: All documentation-producing skills (skills/mkdocs-structure-apply/SKILL.md, skills/docs-audience-tracks-apply/SKILL.md, skills/docs-dry-refactor/SKILL.md, skills/mermaid-diagrams-apply/SKILL.md, skills/skill-agent-catalog-apply/SKILL.md, skills/portfolio-audit/SKILL.md) and agents/audience-doc-author.md.
      Fix: Add a hard rule to every documentation-producing skill and to the audience-doc-author agent that states: when a best-effort translation is emitted (e.g. the skill generates structured-data-derived text rather than human-reviewed prose), the translation file MUST carry <!-- translation-status: needs-review --> as the first HTML comment immediately after the closing --- of the frontmatter. The rule must also state that the marker MUST NOT be a frontmatter key. Optionally add a grep-based CI step that scans for the marker so docs-freshness can surface it as a warning finding..
      Verify: Check hard-rules sections of each documentation-producing skill and the audience-doc-author agent for the needs-review marker placement rule. Optionally run grep -r 'translation-status' .github/ to confirm a CI scan exists..

- [ ] [project/feature.consistency-rerun-trigger] The spec MUST requires re-running the consistency check when a ready or in_progress feature's Description, ACs, roadmap_item, or sprint field change, or when an overlapping feature is added/removed. No skill (sprint-plan, sprint-execute, feature-decompose) implements this detection-and-dispatch logic.
      Where: skills/sprint-plan/SKILL.md, skills/sprint-execute/SKILL.md — no re-run trigger logic.
      Fix: Add a 'consistency re-run' check to sprint-plan (when roadmap_item or sprint changes on a feature) and to feature-decompose (when an existing feature's description or ACs are edited). The check dispatches feature-consistency-reviewer with 'run kind: re-run' and appends a new dated findings block to consistency_check (preserving historical findings per spec §Consistency check)..
      Verify: Edit the ## Description of a ready feature by more than typo-level wording; confirm the consuming skill detects the change, dispatches the re-run, and appends new findings without overwriting the historical block..

- [ ] [project/feature.frontmatter-schema-lint] No machine lint exists for project/features/*.md frontmatter. The spec's acceptance criterion requires lints that flag unknown keys and reject 'closed' as a field name, but validate_skills.py and all pre-commit hooks cover only skills/ and agents/. Schema compliance relies entirely on authoring discipline.
      Where: scripts/validate_skills.py, .pre-commit-config.yaml — no coverage of project/features/.
      Fix: Add a validate_features.py script (or extend validate_skills.py) that reads every project/features/*.md, checks the nine required fields are present in declared order, rejects unknown keys, and rejects 'closed' as a field name. Wire it as a pre-commit hook (files: '^project/features/.*\.md$') and a Taskfile target alongside validate:skills..
      Verify: Run python3 scripts/validate_features.py against a feature file that has an unknown key (e.g. 'effort: 3') and confirm it exits non-zero with a Critical finding; run against all existing features and confirm exit 0..

- [ ] [project/feature.lifecycle-draft-to-ready-gate] sprint-plan/SKILL.md Operation 5 sets feature.sprint=N but does not set feature.status=ready and does not validate the draft→ready gate requirements (non-empty description, ≥1 AC, populated consistency_check, populated ## Consistency notes). The feature spec names sprint-plan as write authority for the draft→ready transition, but the skill's procedure is silent on the status flip and the gate checks.
      Where: skills/sprint-plan/SKILL.md, Operation 5 (lines 80-87).
      Fix: Add a step to sprint-plan Operation 5 that: (a) reads each chosen feature's consistency_check.findings (must be non-empty), ## Consistency notes (must be non-empty), ## Description (must be non-empty), and ## Acceptance criteria (must contain ≥1 bullet); (b) on passing checks, sets feature.status=ready in the same atomic write that sets feature.sprint=N; (c) on failing checks, surfaces the gap and blocks the sprint-plan write until the operator resolves it via feature-decompose..
      Verify: Attempt to plan a sprint with a feature whose consistency_check.findings is empty; confirm sprint-plan refuses. Attempt with a valid feature; confirm feature.status becomes 'ready' after sprint-plan completes..

- [ ] [project/feature.manual-fallback-operator-name] F-1 (mission-statement-published.md) and F-2 (plugin-published-via-automated-release.md) used the manual-fallback consistency check path. The spec MUST requires '## Consistency notes section names the operator who performed it' when agent_version starts with 'manual-'. Both features' ## Consistency notes describe the fallback circumstances and investigation surfaces but never identify the operator by name.
      Where: project/features/mission-statement-published.md §Consistency notes, project/features/plugin-published-via-automated-release.md §Consistency notes.
      Fix: Append a one-line attribution to each affected ## Consistency notes section, e.g. 'Manual pass performed by: nolte (2026-05-09)' for F-1 and 'Manual pass performed by: nolte (2026-05-11)' for F-2..
      Verify: Read ## Consistency notes in both files and confirm the operator's identity is explicitly named..

- [ ] [project/feature.slug-rename-gate] The spec MUST NOT rename a feature's slug after it leaves `draft` (§Directory layout and file shape). There is no git hook, pre-commit check, or skill enforcement that detects or prevents a post-draft slug rename. Enforcement relies entirely on author discipline, exactly as the auditor found for the frontmatter schema lint gap.
      Where: scripts/validate_skills.py, .pre-commit-config.yaml — no coverage of project/features/ slug stability; no git hook inspects rename events on project/features/*.md.
      Fix: Add a pre-commit hook (e.g., a `check-renamed-feature-slug` local hook) that reads the git diff for renames under `project/features/` and cross-references the feature's `status` frontmatter; reject renames on any feature whose status is not `draft`..
      Verify: Attempt `git mv project/features/mission-statement-published.md project/features/renamed.md` on a feature with `status: done`; the pre-commit hook should reject the commit with an explanatory error..

- [ ] [project/mission.stabilisation-gate] Post-MVP roadmap items R-2 (mvp: false) and R-8 (mvp: false) were progressed to done while mvp_status was in_progress, violating the intent of the stabilisation gate. Sprint 2 review notes record that R-2 had a 'lifecycle drift — it should have been active while the sprint ran'; Sprint 4 review notes record the same for R-8. The mission.md ## Source reconciliation note acknowledges the lag retroactively. Both items bypassed the active state entirely (proposed → done), which technically sidesteps the MUST gate ('MUST NOT allow proposed → active while not stabilised'), but the economic intent — no post-MVP scope worked before MVP stabilises — was violated.
      Where: /home/nolte/repos/github/claude-shared/project/roadmap.md (R-2 and R-8 entries), /home/nolte/repos/github/claude-shared/project/sprints/0002-release-pipeline-end-to-end.md (review notes line 58), /home/nolte/repos/github/claude-shared/project/sprints/0004-reviewer-agent-coverage.md (review notes line 60), /home/nolte/repos/github/claude-shared/project/mission.md (## Source reconciliation note).
      Fix: No corrective write is needed for the current artefacts — the reconciliation note in mission.md ## Source already captures the decision. The forward-looking fix is to update mission-revise (and roadmap-refine) to proactively detect when a post-MVP item is being advanced while mvp_status is not stabilised, rather than only surfacing the violation when the user self-reports it. Specifically: mission-revise Hard Rules line 136 says 'surface this as a violation when the user reports it' — that reactive stance should become a proactive check run during any mission-revise or roadmap-refine session where a post-MVP item transitions..
      Verify: After updating the skills, author a test scenario where a post-MVP roadmap item is proposed for activation while mvp_status is in_progress; confirm the skill blocks the transition with the verbatim spec error without requiring the operator to self-report..

- [ ] [project/mkdocs-structure.audience-baseline-coverage] AUDIENCES.md contains no release-manager audience entry and no explicit exclusion note for the release-manager portfolio baseline audience. The spec SHOULD requires covering user, contributor, operator, release-manager or explicitly noting each omission.
      Where: /home/nolte/repos/github/claude-shared/AUDIENCES.md (no entry or exclusion note for release-manager).
      Fix: Either add a release-manager entry to AUDIENCES.md (e.g. under 'Contributors / maintainers' or a new 'Release authorities' subsection), or add an explicit note such as: '**release-manager**: none — release authority is exercised by the repo maintainer (id: maintainer) via the automated release pipeline; no separate release-manager audience exists.' in the relevant section..
      Verify: grep -i 'release.manager' AUDIENCES.md should return at least one line that either declares the audience with an id: field or states an explicit exclusion..

- [ ] [project/mkdocs-structure.audience-baseline-coverage] AUDIENCES.md also lacks a plain 'user' audience entry and no explicit exclusion note, beyond the project-specific 'downstream-user' alias. The spec SHOULD requires covering the portfolio baseline or explicitly noting omissions; 'user' is one of the four baseline IDs and the project clearly serves end-users of the plugin via slash commands.
      Where: /home/nolte/repos/github/claude-shared/AUDIENCES.md — no 'user' id entry and no exclusion note (closest entry is 'downstream-user' which is a project-specific label, not the portfolio baseline id 'user').
      Fix: Either add an audience entry with id: user (mapping it to the downstream-user surface), or add an explicit exclusion note in AUDIENCES.md explaining that the portfolio-baseline 'user' label is intentionally replaced by the more specific 'downstream-user' id so reviewers can confirm the omission is deliberate..
      Verify: grep -n '\buser\b' /home/nolte/repos/github/claude-shared/AUDIENCES.md — confirm an entry with id: user or a clearly worded exclusion sentence appears.

- [ ] [project/parallel-working-copies.gitignore-concealment-repo] The repo .gitignore contains '.claude/settings.local.json' and '.claude/*.local.json' entries. These patterns match paths *inside* any nested worktree artifact under .claude/ (for example .claude/worktrees/<slug>/.claude/settings.local.json), which has the practical effect of suppressing the only file inside the stale catalog-overhaul directory from git status, defeating the spec's secondary safeguard. While the .gitignore entry does not literally match '.claude/worktrees/' (the example given in the spec's MUST NOT), its effect—hiding the only content of a nested worktree directory—is to suppress that spec-violating worktree path from git status in the exact failure mode the MUST NOT targets. The spec says 'MUST NOT add a .gitignore entry whose effect is to hide a spec-violating worktree path from git status'. The global git ignore also contributes the same pattern, but the repo-level .gitignore (.claude/settings.local.json on line 2) independently matches the same file and would produce the same concealment even without the global ignore.
      Where: /home/nolte/repos/github/claude-shared/.gitignore line 2: .claude/settings.local.json.
      Fix: This is a tension between the §Local state and untracked configuration MUST ('MUST treat .claude/settings.local.json as worktree-local') and the §Path layout MUST NOT. The correct resolution is to remove the stale .claude/worktrees/catalog-overhaul/ directory via git worktree remove (or manual rm since it is not a registered worktree) so there is nothing to conceal. The .gitignore entry itself is legitimately required for settings.local.json in its intended location (.claude/settings.local.json directly under the repo root); the problem is the stale nested directory, not the ignore entry..
      Verify: After removing .claude/worktrees/catalog-overhaul/, run `git status --untracked=all` and confirm the directory is absent. Then confirm `git check-ignore -v .claude/settings.local.json` still suppresses the legitimate settings file at the root level..

- [ ] [project/prose-style.MinAlertLevel-consistency] MinAlertLevel in .vale.ini is set to 'suggestion', but the lint:prose Taskfile target overrides it to error via --minAlertLevel=error. Running 'vale .' or 'vale <file>' directly produces a different alert set than what CI and the pre-commit hook enforce, violating the MUST requirement that local and CI runs produce the same alerts.
      Where: /home/nolte/repos/github/claude-shared/.vale.ini line 2 (MinAlertLevel = suggestion); /home/nolte/repos/github/claude-shared/Taskfile.yml lint:prose target.
      Fix: Change .vale.ini to set MinAlertLevel = error so that any direct Vale invocation is aligned with what lint:prose and CI enforce. The --minAlertLevel=error flag in lint:prose can then be removed as redundant, or kept as an explicit guard..
      Verify: Run 'vale README.md' directly and confirm it only reports errors. Then run 'task lint:prose' and confirm the same alert set. No suggestion-level alerts should surface in either invocation..

- [ ] [project/prose-style.llm-artifacts-scope-vale-ini] .vale.ini does not explicitly exclude LLM-instruction source files (skills/**/SKILL.md, agents/*.md) from the configured lint scope. Only the generated catalog output under docs/*/skills/** and docs/*/agents/** is excluded. The spec MUST requires both the .vale.ini format sections and the Taskfile lint:prose target to declare the exclusion.
      Where: /home/nolte/repos/github/claude-shared/.vale.ini — [*.md] BasedOnStyles = Vale, Microsoft applies to skills/ and agents/ source files; no format-section override exists for those paths.
      Fix: Add to .vale.ini: [skills/**/SKILL.md] BasedOnStyles =, [skills/**/templates/**] BasedOnStyles =, [skills/**/examples/**] BasedOnStyles =, [agents/*.md] BasedOnStyles =. This aligns the configured scope with the Taskfile's path-based exclusion..
      Verify: Run 'vale skills/lektorat-apply/SKILL.md' and confirm Vale reports no alerts. Run 'vale agents/review.md' (if present) and confirm the same..

- [ ] [project/prose-style.multilingual-scope-vale-ini] .vale.ini lacks explicit format-section exclusions for non-English content paths (docs/de/**, spec/**/de.md, *.de.md). The global [*.md] rule would include all German-language files when Vale is invoked without explicit path arguments. Spec MUST defines the English-only scope in both .vale.ini format sections and the Taskfile target.
      Where: /home/nolte/repos/github/claude-shared/.vale.ini — no [docs/de/**], [spec/**/de.md], or [*.de.md] sections present; 62 spec/*/de.md files and 93 docs/de/*.md files exist in scope.
      Fix: Add exclusion sections to .vale.ini for at minimum: [docs/de/**] BasedOnStyles =, [spec/**/de.md] BasedOnStyles =. This ensures 'vale .' is safe without path scoping and the .vale.ini alone correctly documents the lint boundary..
      Verify: Run 'vale docs/de/index.md' and confirm Vale reports no alerts (excluded via .vale.ini). Run 'vale docs/en/index.md' and confirm it still lints..

- [ ] [project/prose-style.release-notes-vale-check] The release-publish workflow does not include a Vale check against the release notes body before publishing. The spec SHOULD verify the final release notes body against Vale before the release is published.
      Where: /home/nolte/repos/github/claude-shared/.github/workflows/release-publish.yml — delegates directly to reusable-release-publish.yml with no Vale step.
      Fix: Add a step in release-publish.yml (before the publish call) that fetches the draft release body via the GitHub API, writes it to a temp file, and validates it with 'vale --minAlertLevel=error'..
      Verify: Confirm whether the reusable-release-publish.yml in nolte/gh-plumbing includes a prose check; if not, add a pre-publish Vale step to release-publish.yml..

- [ ] [project/quality-gate.composition-task-check-composed] The spec MUST (line 26) requires `task check` to be composed from `task lint`, `task test`, and `task typecheck` (per-category targets compose into it). The Taskfile `check` task (lines 39-46) omits `task typecheck` entirely without any explicit declaration that typecheck is absent because the repository has no relevant typed code. The spec allows omission only 'when the repository has relevant code for each' (line 24) — Python skills and agents files exist but no pyproject.toml with mypy/pyright configured is declared. The omission is likely correct, but it is undocumented in the Taskfile's desc or README, so a consumer cannot distinguish a deliberate N/A from an oversight.
      Where: Taskfile.yml lines 39-46 (check task) and README.md lines 91-109.
      Fix: Either add a comment to the `check` task desc noting 'typecheck omitted: no typed language in this repo' or add a `typecheck` stub target that exits 0 with a skipped message, so the gate's output is complete per the Output shape MUST..
      Verify: Check whether any pyproject.toml, tsconfig.json, or go.mod exists in the repo root; if none exists, confirm the omission is documented in the Taskfile desc or README..

- [ ] [project/quality-gate.invocation-contract-identical] The spec requires the documented gate invocation to be identical between the README and the CI workflow (AC line 69: 'identical between the repository's README and its CI workflow'). The README documents `task check` as the entry point; ci.yml invokes `task --yes lint` and `task --yes test` directly in separate jobs and never calls `task check`. The commands executed are equivalent, but the literal invocation surface diverges.
      Where: README.md lines 93-99 vs .github/workflows/ci.yml lines 40 and 50.
      Fix: Change ci.yml so the composite check job runs `task --yes check` instead of calling `task --yes lint` and `task --yes test` separately. Alternatively — if separate CI jobs are deliberately retained for parallelism — update the README and Taskfile docs to make the equivalence explicit and acknowledge the surface divergence (e.g. add a note: 'CI fans out into separate jobs but runs the same commands that task check composes')..
      Verify: Confirm ci.yml contains at least one `task --yes check` step, or that the README explicitly states the local command and CI commands are equivalent decompositions with a documented rationale..

- [ ] [project/quality-gate.invocation-contract-parallel] The spec SHOULD requires running categories in parallel when the ecosystem permits it. `task check` uses `cmds:` (sequential) rather than `deps:` (parallel). No documented dependency between lint and test outputs requires sequential ordering. SKILL.md itself mandates parallel batch execution in step 3, creating a conflict between the skill's own rules and the repository's own Taskfile.
      Where: Taskfile.yml lines — `check` task using `cmds:` instead of `deps:`.
      Fix: Replace the `cmds:` block in `task check` with `deps: [lint, test]` to run the categories concurrently, matching both the spec SHOULD and SKILL.md's own mandate. If a future dependency between categories arises, document it in the task's `desc:` comment..
      Verify: Run `task check` and confirm lint and test start within the same second (no sequential wait). Also verify the Taskfile change uses `deps:` not `cmds:`..

- [ ] [project/spec-drift-audit.triggers-new-skill-agent] Spec-coupling for 18 new agents and several new skills added after the Q2 audit is unaudited; the SHOULD requires their spec coupling to be included in the same audit cycle.
      Where: agents/ (18 files added post-2026-05-19: code-security-reviewer.md, diagram-opportunity-reviewer.md, graphic-prompt-generator.md, i18n-completeness-checker.md, mermaid-diagram-reviewer.md, project-structure-reviewer.md, quality-gate-enforcer.md, roadmap-coherence-reviewer.md, sprint-readiness-reviewer.md, tech-stack-drift-reviewer.md, test-case-extractor.md, vocab-drift-scanner.md, webview-ui-expert.md, and others).
      Fix: Include spec-coupling checks for all post-Q2 agents and skills in the next audit cycle. At minimum document that spec coupling was checked inline when each agent/skill was merged..
      Verify: Next audit artifact lists each post-Q2 agent/skill with a per-criterion result row (pass / fail / not-applicable with reason)..

- [ ] [project/spec-driven-development.AC5] The spec's own ## Open Questions section does not contain verifiable cross-references to skills/spec/SKILL.md or skills/spec/templates/spec.template.md. The references appear in the Requirements body and inside the AC checklist statement text but not in Open Questions, so the recursion claim is not auditable from that section as required.
      Where: spec/project/spec-driven-development/en.md, lines 61-63 (## Open Questions section).
      Fix: Add an explicit cross-reference note to the ## Open Questions section. Suggested addition: append a line such as '- Recursion anchors: `skills/spec/SKILL.md` governs spec authoring; `skills/spec/templates/spec.template.md` defines the six-section template this spec follows.' and mirror it in de.md..
      Verify: grep -n 'skills/spec/SKILL.md' spec/project/spec-driven-development/en.md | grep -q 'Open Questions' || grep -A20 '## Open Questions' spec/project/spec-driven-development/en.md | grep 'skills/spec'.

- [ ] [project/spec-driven-development.REQ-2] Seven implementation-path PRs landed on develop after spec adoption (#50) without a Refs spec/ line in their Linked issues section: #224 (README.md, Taskfile.yml), #219 (CLAUDE.md, docs/), #218 (agents/*.md), #216 (agents/*.md), #214 (agents/*.md), #212 (agents/*.md), #211 (.github/workflows/ci.yml). The spec's own revisit trigger (three missed PRs caught after the fact) is now met.
      Where: GitHub PR bodies for #224, #219, #218, #216, #214, #212, #211.
      Fix: The spec's Open Questions deferred enforcement tooling until the three-PR threshold was reached. That threshold is now met. Extend the gh-plumbing reusable-pr-lint workflow with a 'Refs spec/' body-content assertion rather than creating a standalone skill (per the spec's own Revisit guidance). Until the lint is wired, retroactively annotate the seven PR bodies by editing them on GitHub to add the missing Refs lines..
      Verify: After the lint extension, re-run: gh pr list --base develop --state merged --json number,body | python3 -c "import json,sys; [print(p['number'], 'OK' if 'Refs spec/' in (p['body'] or '') else 'MISSING') for p in json.load(sys.stdin)]".

- [ ] [project/spec-driven-development.REQ-3] Three spec folders exist on disk but are absent from spec/README.md, indicating they bypassed the spec-skill index-regeneration step: spec/claude/skills-agents-sweep/, spec/frontend/webview-ui-optimization/, spec/project/yaml-json-schema/.
      Where: spec/README.md (missing 3 entries); spec/claude/skills-agents-sweep/en.md, spec/frontend/webview-ui-optimization/en.md, spec/project/yaml-json-schema/en.md (orphaned).
      Fix: Run /nolte-shared:spec to regenerate the spec/README.md index, or manually add the three missing rows so every spec folder appears in the index. Verify with: comm -23 <(find spec -mindepth 2 -maxdepth 2 -type d | sort) <(grep '^| ' spec/README.md | awk -F'|' '{print "spec/"$2"/"$3}' | tr -d ' ' | sort).
      Verify: After fix, the count from 'find spec -mindepth 2 -maxdepth 2 -type d | wc -l' equals the count from 'grep -c "^| " spec/README.md' (excluding header rows)..

- [ ] [project/spec-readiness.audit-artifact-worktree-should] The agent (agents/spec-readiness-reviewer.md) does not implement the §Audit artifact SHOULD to consult spec/project/parallel-working-copies/ §Audit artefacts in multiple worktrees when the audit is run inside a worktree. The agent body contains no mention of parallel-working-copies, no worktree-disambiguation guidance, and the output template's Scope block has no worktree-path field. Given that this repository is routinely worked in multiple worktrees (per CLAUDE.md), this SHOULD gap is likely to surface as an artifact-uniqueness ambiguity on the first multi-worktree audit run.
      Where: /home/nolte/repos/github/claude-shared/agents/spec-readiness-reviewer.md §Preconditions, §Output shape Option A Scope block.
      Fix: Add a Precondition step: if the working directory is a worktree (detectable via `git rev-parse --git-dir`), record the worktree path in the artifact and note that the artifact is worktree-local per spec/project/parallel-working-copies/ §Audit artefacts in multiple worktrees. Add a Scope line `- Worktree path: <path or 'primary checkout'>` to the Option A output template..
      Verify: Run the agent from inside a worktree path under ~/repos/.worktrees/claude-shared/; confirm the produced Scope section names the worktree path and references parallel-working-copies..

- [ ] [project/workflow-health.known-platform-constraints-interim-doc] README.md line 116 states 'the portfolio-level fix lives in nolte/gh-plumbing. Until that ships...' but the fix has already shipped: gh-plumbing v1.1.19 (2026-05-22) resolves the GITHUB_TOKEN cascade gap via App-token authentication (issues #330 and #357 closed). Recent release-drafter runs (five consecutive successes since 2026-05-29) confirm the fix is live. The stale 'Until that ships' framing misleads readers and does not include the tracking-issue link the spec SHOULD requires.
      Where: /home/nolte/repos/github/claude-shared/README.md line 116.
      Fix: Update the cascade-constraint note in README.md: remove 'Until that ships' and the manual-workaround instructions now that the fix is in production; add a retroactive reference to gh-plumbing issues #330 and #357 (now closed) so the resolution is traceable; or shorten the note to 'The constraint was resolved in gh-plumbing v1.1.19 (issues nolte/gh-plumbing#330 and #357).'.
      Verify: Read README.md and confirm the cascade note no longer says 'Until that ships' and references the gh-plumbing resolution. Optionally re-run a release-drafter via workflow_dispatch to confirm the cascade is live..

- [ ] [project/workflow-health.triage-before-remediation-audit-trail] The last four post-spec-implementation workflow-fix PRs (#160, #162, #211, #90) carry no triage classification (defect / flake / infra / stale pin / secret drift / other) and no dispatched-agent record in their Risk / rollout notes. The spec mandates both pieces of evidence for every fix PR so failure patterns are visible across PR history and portfolio coverage gaps can be spotted.
      Where: GitHub PRs #160, #162, #211, #90 — Risk / rollout notes sections.
      Fix: For future workflow-fix PRs, invoke /nolte-shared:workflow-health-triage to produce the classification before opening the fix PR. The triage skill seeds the Risk / rollout notes with the classification label and the dispatched agent name (or the literal 'no matching specialised agent—generalist remediation'). For the four existing merged PRs there is no retroactive action required, but the pattern must start with the next workflow-fix PR..
      Verify: After the next workflow-fix PR is merged, confirm its Risk / rollout notes section contains one of the six classification terms and either a subagent_type reference or the 'no matching specialised agent' phrase..

- [ ] [pull-request-workflow.pr-lint-workflow] The spec SHOULD clause recommends implementing the PR linter as a reusable workflow under nolte/gh-plumbing so all portfolio repos inherit it. No `reusable-pr-lint.yaml` exists in gh-plumbing. When the pr-lint workflow is created, implementing it as a reusable workflow in gh-plumbing is the recommended approach.
      Where: <https://github.com/nolte/gh-plumbing> (no reusable-pr-lint.yaml found in .github/workflows/).
      Fix: When addressing the Critical pr-lint finding, add a `reusable-pr-lint.yaml` to nolte/gh-plumbing rather than a standalone workflow in claude-shared, so all portfolio repositories can call it..
      Verify: After implementation: `gh api repos/nolte/gh-plumbing/contents/.github/workflows/reusable-pr-lint.yaml` returns 200..

- [ ] [readme-structure.header-block-tagline] The header block contains two paragraphs before ## Purpose: one explaining the CI badge (line 5) and one that is the actual tagline (line 7). The spec mandates a single tagline paragraph under the badges stating what the repository is and who it is for. The badge-explanation paragraph ('The ci workflow bundles the three required status checks…') is neither a badge, a tagline, nor a spec-defined optional element; it creates an unexpected double-paragraph header.
      Where: /home/nolte/repos/github/claude-shared/README.md line 5.
      Fix: Remove the badge-explanation paragraph (line 5) or fold its information into the Notes subsection under ## Usage. The single tagline on line 7 already satisfies the spec requirement..
      Verify: After removal, the header block should read: H1 → badge → blank line → single tagline → blank line → ## Purpose..

- [ ] [readme-structure.header-block-tagline-length] The spec SHOULD keeps the tagline at or under 280 characters (excluding markdown link syntax). Line 7 reads: 'A shared foundation of [Claude Code](https://docs.claude.com/en/docs/claude-code) agents and skills, intended to be reused across multiple software development projects.' Excluding the link syntax the text is approximately 140 characters — this SHOULD is satisfied. No violation here; audit correctly omitted it.
      Where: /home/nolte/repos/github/claude-shared/README.md line 7.
      Fix: No fix needed — tagline is within the 280-character SHOULD limit..
      Verify: Count characters in the tagline excluding markdown link syntax: ~140 chars, well under 280..

- [ ] [readme-structure.prose-style-vale] Running Vale against README.md yields 2 warnings: Microsoft.Adverbs for 'strictly' (line 44) and Microsoft.GeneralURL for 'URL' (line 113). The spec requires the README to pass the Vale configuration that implements the prose-style spec. Warnings are non-passing.
      Where: /home/nolte/repos/github/claude-shared/README.md lines 44 and 113.
      Fix: Line 44: remove 'strictly' from '…conforming to every spec under spec/claude/.' Line 113: replace 'URL' with 'address' per Microsoft.GeneralURL guidance, or add 'URL' to the accepted vocabulary if it is a deliberate term of art in this context..
      Verify: `vale README.md` exits with 0 errors, 0 warnings (suggestions are allowed)..

- [ ] [release-artifact.cross-links-downward] The release-artifact spec (en.md) contains no navigable markdown hyperlinks to release-automation or release-skill-layer at the validation-rules table or dispatch-boundary sections; all references are bare backtick prose (`release-automation`, `release-skill-layer`). AC 11 explicitly requires cross-links at the relevant boundary points.
      Where: /home/nolte/repos/github/claude-shared/spec/project/release-artifact/en.md lines 39, 54, 62-63.
      Fix: Add relative markdown links at the two load-bearing boundary points: (1) in §Artefact taxonomy Claude-plugin row, link `release-automation` to `../release-automation/en.md`; (2) in §Dispatch boundary §MUST NOT and §MUST paragraphs, link `release-automation` and `release-skill-layer` to their respective specs. Mirror the same approach in de.md..
      Verify: grep -nE '\[.*\]\(.*release-automation.*\)' spec/project/release-artifact/en.md returns at least one hit; same for release-skill-layer..

- [ ] [release-artifact.sprint-spec-cross-links] The sprint spec (en.md) references the release-artifact spec in prose at five points (artifact_ref field definition line 46, artefact contract line 16, lifecycle line 64, validation delegation line 84, out-of-band line 91) but carries no navigable markdown hyperlink. AC 12 requires 'cross-links to this spec where it requires artifact_ref to be set'.
      Where: /home/nolte/repos/github/claude-shared/spec/project/sprint/en.md lines 9, 16, 46, 64, 84.
      Fix: Add at least one relative markdown link from the sprint spec to the release-artifact spec at the artifact_ref field definition (line 46) and at the validation-delegation paragraph (line 84), e.g. `[release-artifact spec](../release-artifact/en.md)`. Mirror in de.md..
      Verify: grep -nE '\[.*\]\(.*release-artifact.*\)' spec/project/sprint/en.md returns at least one hit..

- [ ] [release-automation.acceptance-criteria] Acceptance Criterion: 'the last three published releases were produced by release-publish.yml'. v0.1.2 (published 2026-05-11) has no corresponding `release-publish.yml` run — it was manually published via `gh release edit --draft=false`. Only the last two non-draft releases (v0.1.3, v0.1.4) were produced by the workflow.
      Where: GitHub Actions run history for nolte/claude-shared: gap between 2026-04-24 runs (v0.1.1) and 2026-05-28 run (v0.1.3); no run on or around 2026-05-11.
      Fix: This is a historical gap that cannot be remediated retroactively. Document the v0.1.2 manual publish as a known exception in `project/release-artifacts/out-of-band/` per `branching-model` §Hotfix flow, so the audit trail is clear. Going forward, v0.1.5 and subsequent releases must be published via the workflow, at which point the last-three-releases AC will be satisfied from v0.1.5 onward..
      Verify: After v0.1.5 is published via `release-publish.yml`, run `gh run list --workflow=release-publish.yml --limit 10` and confirm v0.1.5, v0.1.4, v0.1.3 all show `completed success`..

- [ ] [release-automation.operational-contract] The `tag` workflow_dispatch input is declared `required: true` in both the local release-publish.yml wrapper and the upstream reusable-release-publish.yml. The spec mandates an *optional* tag input so that: (a) dispatching without a tag when a single draft is open could auto-resolve, and (b) dispatching without a tag when multiple drafts are open produces the required actionable error listing all open drafts. With `required: true`, neither path is reachable; the GitHub UI and `gh workflow run` both enforce the field before the workflow even starts.
      Where: .github/workflows/release-publish.yml line 9 (`required: true`) and nolte/gh-plumbing:.github/workflows/reusable-release-publish.yml line 7 (`required: true`).
      Fix: Change `required: true` to `required: false` (with no `default:`) in both the local wrapper and the reusable. Add a `Resolve tag` step that (1) when `inputs.tag` is empty and exactly one draft exists, uses that draft's tag; (2) when `inputs.tag` is empty and multiple drafts exist, exits 1 listing all open drafts; (3) when `inputs.tag` is provided, validates it against the draft list as today. The `Resolve draft` step already implements branches (2) and (3) — only branch (1) and the optional-input change are new..
      Verify: Dispatch `release-publish.yml` via `gh workflow run release-publish.yml` without supplying a tag; when a single draft is open the run should proceed; when multiple drafts are open it should fail with a list. Verify the GitHub UI shows the tag field as optional..

- [ ] [release-automation.version-bearing-file-alignment] At the v0.1.2 tag, `.claude-plugin/marketplace.json` (a mandatory version-bearing file for Claude Code plugin repos per the portfolio convention table) still declared version `0.1.1`. The `chore(release): v0.1.2 (#87)` commit only updated `plugin.json`. Every published release MUST land on a commit whose tree has every VBF equal to the target tag.
      Where: `git show v0.1.2:.claude-plugin/marketplace.json` returns `{"metadata":{"version":"0.1.1"},...}`; commit 5e560b5 touches only `.claude-plugin/plugin.json`.
      Fix: Cannot be remediated on the published v0.1.2 tag. Ensure the reusable's VBF-alignment verification step covers marketplace.json (it does as of v1.1.19 for repos with the current file present). Add an out-of-band record under `project/release-artifacts/out-of-band/` noting the v0.1.2 misalignment. Verify v0.1.5 alignment covers both files before dispatching release-publish..
      Verify: Run `git show v0.1.5:.claude-plugin/marketplace.json` and `git show v0.1.5:.claude-plugin/plugin.json` after the v0.1.5 release-publish run completes and confirm both show version `0.1.5`..

- [ ] [release-automation.workflow-existence-and-trigger] The spec SHOULD requires the workflow to fail explicitly (non-zero exit, actionable message) when `release-cd-refresh-master.yml` is absent or disabled, because publishing without the downstream refresh leaves `main` out of sync. The reusable only performs a post-publish cascade check (30-second sleep + `::warning`) and has no pre-publish guard that inspects whether the workflow file exists or is enabled.
      Where: nolte/gh-plumbing:.github/workflows/reusable-release-publish.yml lines 346–354 (post-publish warning block).
      Fix: Add a pre-publish step in the reusable that calls `gh workflow list --repo $GITHUB_REPOSITORY` and verifies `release-cd-refresh-master.yml` appears and is in an active (non-disabled) state. If absent or disabled, exit 1 with an actionable message. This satisfies the SHOULD before the irreversible `--draft=false` call is made..
      Verify: Temporarily disable `release-cd-refresh-master.yml` in a test repo and dispatch `release-publish.yml`; the run should fail with an actionable message before the publish step executes..

- [ ] [release-notes-audience-analysis.release-automation-cross-link] spec/project/release-automation/en.md §Non-Goals states 'Release-notes content generation — that remains release-drafter's responsibility' but does not cross-link to release-notes-audience-analysis. The boundary between mechanics and content is not explicit in writing as the AC requires.
      Where: /home/nolte/repos/github/claude-shared/spec/project/release-automation/en.md line 25.
      Fix: Amend the Non-Goals bullet to read: 'Release-notes content generation — that remains `release-drafter`'s responsibility, fed by Conventional-Commits PR titles; audience-driven content structure is governed by `release-notes-audience-analysis`.' This makes the mechanics/content boundary explicit and satisfies AC line 6..
      Verify: spec/project/release-automation/en.md §Non-Goals contains a hyperlinked or backtick-referenced mention of release-notes-audience-analysis in the content-generation bullet..

- [ ] [release-notes-audience-analysis.release-drafter-traceability] The .github/release-drafter.yml is a single-line extends of nolte/gh-plumbing:commons-release-drafter.yml with no local category overrides. No category-to-audience traceability exists for this project.
      Where: /home/nolte/repos/github/claude-shared/.github/release-drafter.yml — one line: `_extends: nolte/gh-plumbing:.github/commons-release-drafter.yml`.
      Fix: After the release-notes audience artefact is produced (see Finding 1), review the inherited commons categories against the identified audiences. Add local overrides in release-drafter.yml for any category the inherited config lacks that an identified audience requires; document the category-to-audience mapping inline or in the release-notes audience artefact..
      Verify: Each category in the effective release-drafter configuration (expanded from the nolte/gh-plumbing commons file) can be traced to at least one audience in the release-notes artefact. Any category with no audience is removed or marked with an explicit reason..

- [ ] [release-skill-layer.AC11] The release-automation spec (spec/project/release-automation/en.md) has no cross-link to release-skill-layer in either §Non-Goals or §Relationship to other specs. The release-skill-layer AC requires that release-automation §Non-Goals or §Relationship cross-link to this spec as the local-skill counterpart for body curation and dispatch ergonomics.
      Where: /home/nolte/repos/github/claude-shared/spec/project/release-automation/en.md (§Non-Goals lines 21-29, §Relationship to other specs lines 120-125).
      Fix: Add a bullet to release-automation §Relationship to other specs: '- **SHOULD** cross-link to `release-skill-layer` as the local operator-side counterpart for draft-body curation (`release-notes-curate`) and pre-publish gate validation + dispatch (`release-publish-trigger`); the two skills are the ergonomic layer that sits between the operator's terminal and the workflow audit trail.' Alternatively add a non-goal line: '- Providing the local operator-side curation and dispatch layer — that is owned by `release-skill-layer`.'.
      Verify: grep -n 'release-skill-layer' /home/nolte/repos/github/claude-shared/spec/project/release-automation/en.md should return at least one hit..

- [ ] [release-skill-layer.AC1b] Neither release-notes-curate nor release-publish-trigger has a passing skill-review plan recorded under .audits/skill-review/. The directory contains only .gitkeep.
      Where: /home/nolte/repos/github/claude-shared/.audits/skill-review/.gitkeep.
      Fix: Run /nolte-shared:skill-review against both skills and commit the resulting plan files to .audits/skill-review/release-notes-curate.md and .audits/skill-review/release-publish-trigger.md..
      Verify: find /home/nolte/repos/github/claude-shared/.audits/skill-review/ -name 'release-notes-curate.md' -o -name 'release-publish-trigger.md' | wc -l should return 2..

- [ ] [release-skill-layer.AC2] Both skill frontmatter descriptions say 'equivalent German-language requests' instead of listing concrete German trigger phrases. The AC requires 'concrete user-trigger phrases (EN + DE)'. No German phrases appear in the description or use_when fields of either skill.
      Where: /home/nolte/repos/github/claude-shared/skills/release-notes-curate/SKILL.md (line 3), /home/nolte/repos/github/claude-shared/skills/release-publish-trigger/SKILL.md (line 3).
      Fix: Add at least two concrete German trigger phrases to each skill's description field, e.g. for release-notes-curate: '"Release-Notes kuratieren", "Draft-Release mit Projektkontext anreichern"'; for release-publish-trigger: '"Release veröffentlichen", "Release-Publish auslösen", "Release ausliefern"'..
      Verify: Read both SKILL.md description fields and confirm at least two German-language phrases are listed literally (not as 'equivalent …')..

- [ ] [research-triangulate.documentation-of-the-triangulation] cookiecutter-template-author.md requires ≥2 independent sources cited inline but does not require recording source class or retrieval date per source — two of the three mandatory fields defined by the spec's documentation section.
      Where: /home/nolte/repos/github/claude-shared/agents/cookiecutter-template-author.md (lines 43, 56, 93, 102, 137).
      Fix: Extend the sources-cited section in the agent's output contract (line 93) to require: URL or doc heading, source class (Primary / Secondary / Web aggregator), and retrieval date for each source..
      Verify: After the fix, the 'Sources cited' section of the agent output contract should list all three mandatory fields..

- [ ] [research-triangulate.interaction-with-existing-skills] cookiecutter-template-manage (the dispatching skill for cookiecutter-template-author) does not review the agent's source list before the operator approval gate, as required by the spec's SHOULD clause for dispatching skills.
      Where: /home/nolte/repos/github/claude-shared/skills/cookiecutter-template-manage/SKILL.md.
      Fix: Add a step to cookiecutter-template-manage's operator approval gate that reads the agent's returned sources-cited section, checks for any flagged conflicts, and re-surfaces them to the operator before proceeding..
      Verify: A scenario where the agent flags a contradiction should result in the dispatching skill repeating the conflict to the operator rather than silently proceeding..

- [ ] [research-triangulate.minimum-source-count-scales-with-blast-radius] cookiecutter-template-author.md requires ≥2 independent sources for 'non-trivial' web-sourced recommendations (lines 43, 56, 93, 136) but does not apply the blast-radius tier logic. The agent writes template files (Local edit tier: requires 2 sources) and may guide hooks that alter generated project structure (potentially Release/dispatch tier: requires 3 sources). The flat '≥2 sources' rule does not satisfy the 3-source minimum required by the spec for assertions that direct writes above the Local-edit tier, and does not distinguish blast-radius tiers at all.
      Where: /home/nolte/repos/github/claude-shared/agents/cookiecutter-template-author.md (lines 43, 56, 93, 136-137).
      Fix: Replace the flat '≥2 independent sources' rule with a blast-radius-tiered rule: 2 sources (one Primary, one independent Secondary) for local template file edits; 3 sources for assertions that will propagate into generated projects' configuration files (e.g., pins in generated pyproject.toml, CI workflow version pins, release-automation config)..
      Verify: Confirm the agent's tool-selection rationale and hard rules now cite the blast-radius table and apply the 3-source minimum for any web-sourced assertion that lands in a generated project's configuration or release-automation files..

- [ ] [research-triangulate.source-classes-and-independence] SHOULD: every triangulation should include at least one source carrying a verifiable date. cookiecutter-template-author.md omits the date requirement; no other skill or agent tracks it.
      Where: /home/nolte/repos/github/claude-shared/agents/cookiecutter-template-author.md.
      Fix: Add 'retrieval date' as a required field in the agent's sources-cited output contract (and in any future skill triangulation sections)..
      Verify: Agent output includes a dated source for each cited claim..

- [ ] [review-plan.file-location-naming] Two portfolio-inflight plans exist simultaneously for the same (review-type='portfolio-inflight', target='.') pair, both with status: open; the v1 plan was never set to superseded when v2 was created.
      Where: .audits/portfolio-inflight/2026-05-23.md (v1, status: open) and .audits/portfolio-inflight/2026-05-23-v2.md (v2, status: open).
      Fix: Set status to `superseded` in .audits/portfolio-inflight/2026-05-23.md and add a ## Processing log entry noting it was superseded by 2026-05-23-v2.md on the overwrite date..
      Verify: Confirm only one file under .audits/portfolio-inflight/ has status: open; the other should have status: superseded..

- [ ] [review-plan.file-location-naming] The portfolio-inflight plan filenames include timestamps (2026-05-23.md, 2026-05-23-v2.md), violating the MUST NOT timestamp-in-filename rule; this is a cross-spec conflict because portfolio-inflight-management §Findings-Report shape MUST use <YYYY-MM-DD>.md, directly contradicting review-plan's MUST NOT.
      Where: .audits/portfolio-inflight/2026-05-23.md and .audits/portfolio-inflight/2026-05-23-v2.md; conflict between spec/claude/review-plan/en.md §File location and spec/portfolio/portfolio-inflight-management/en.md §Findings-Report shape line 105.
      Fix: Resolve the inter-spec conflict via a spec amendment: either grant portfolio-inflight-management an explicit carve-out in review-plan §File location (per-run archival artifacts), or align portfolio-inflight-management to use a single target-slug filename (e.g. portfolio.md) with the run-date recorded only in frontmatter and ## Processing log..
      Verify: After amendment: review-plan §File location and portfolio-inflight-management §Findings-Report shape no longer prescribe contradictory filename formats..

- [ ] [review-plan.findings-format] Portfolio-inflight plan findings do not use the required markdown checkbox format (- [ ] / - [x]) and substitute Action: for the required Fix:/Verify: lines, so the four-line structure mandated by review-plan §Findings format is absent.
      Where: .audits/portfolio-inflight/2026-05-23-v2.md §Findings — every finding block uses unlabeled bullet lines with Action: instead of Fix:/Verify:, and no checkboxes are present.
      Fix: Either (a) amend portfolio-inflight-management to explicitly declare a portfolio-inflight-specific finding format as an approved deviation from review-plan §Findings format, with a documented rationale, or (b) update the plans to use checkbox items and Fix:/Verify: lines as required..
      Verify: Every finding in the portfolio-inflight plan either uses the four-line review-plan structure, or portfolio-inflight-management carries a documented carve-out that review-plan §Relationship cross-references..

- [ ] [review-plan.frontmatter] Plans deleted in commits 978965a and b2d150c carried status: closed — an invented value not in the spec-allowed set (open, in-progress, complete, superseded).
      Where: Commit 978965a changed .audits/skill-review/portfolio-inflight-triage.md status from in-progress to closed; commit b2d150c deleted both .audits/agent-review/portfolio-inflight-collector.md and .audits/skill-review/portfolio-inflight-triage.md with status: closed in each. The spec §Frontmatter MUST lists exactly four valid values and MUST NOT invent values..
      Fix: When closing a plan, set status to complete (all items resolved) or superseded (rerun replaced it) — never a custom value like closed. Correct any tooling or workflow step that writes status: closed..
      Verify: git log -S 'status: closed' -- '.audits/**/*.md' returns no results; all future deletions use status: complete or status: superseded before the file is removed..

- [ ] [review-plan.lifecycle] Historical plan deletion commits (#29, #34, #35) use B (Blocker) in the count suffix rather than C (Critical) as the current spec requires; no post-vocabulary-unification (post-2026-05-09) deletion commit exists to confirm the corrected pattern review(<review-type>): close <target>—<C>C/<W>W/<S>S/<I>I is followed going forward.
      Where: git log: 875898a review(skill-review): close release-publish-trigger — 0B/0W/0S/2I, e475ff2 review(skill-review): close release-notes-curate — 0B/1W/0S/2I, a876c77 review(skill-review): close github-issue-templates-apply — 0B/0W/1S/3I.
      Fix: Add a note to spec/claude/review-plan/en.md or skills/skill-review/SKILL.md confirming the current required suffix format (C not B), so the next deletion commit uses the correct pattern. The historical commits cannot be rewritten but the trail is observable..
      Verify: The next review-plan deletion commit in git log uses the pattern review(<review-type>): close <target>—<N>C/<N>W/<N>S/<N>I..

- [ ] [review-plan.lifecycle] The post-vocabulary-unification deletion commit b2d150c does not follow the required message format review(<review-type>): close <target>—<C>C/<W>W/<S>S/<I>I, using 'review: close completed portfolio-inflight review plans (#203)' instead (no review-type in parentheses, no target slug, no severity counts).
      Where: Commit b2d150c (2026-05-28, after vocabulary unification on 2026-05-09) deleted .audits/agent-review/portfolio-inflight-collector.md and .audits/skill-review/portfolio-inflight-triage.md. The spec §Lifecycle MUST requires the exact pattern review(<review-type>): close <target>—<C>C/<W>W/<S>S/<I>I for each plan deletion commit so the git log is searchable..
      Fix: Each plan deletion MUST be its own commit with a message matching the required pattern. For multi-plan deletions, either use one commit per plan or confirm whether a single commit covering multiple deletions is acceptable (spec says 'the deletion commit message MUST be' singular — one plan per commit is the unambiguous reading)..
      Verify: git log --oneline -- '.audits/**/*.md' | grep DELETE shows only commit messages matching review(<review-type>): close <target>—[0-9]+C/[0-9]+W/[0-9]+S/[0-9]+I..

- [ ] [roadmap.sprint-and-feature-linkage] The 'closed sprint without pickup' re-targeting path has no canonical executor. The spec says 'sprint-plan owns this update at sprint closure', but sprint-plan is a CREATE-only skill that does not fire at closure. sprint-review step 6 (normal review→closed path) advances done items to done but does NOT clear or re-target target_sprint for roadmap items that pointed at the closing sprint yet were never added to its roadmap_items list. Only the cancellation path (sprint-review step 7.3) correctly handles re-targeting. Additionally, roadmap-plan and roadmap-refine each attribute ownership of the stale-reference lint to the other skill, creating a circular gap.
      Where: skills/sprint-review/SKILL.md §6 Promote review→closed (step 6.3 handles done-promotion only); skills/roadmap-refine/SKILL.md step 2.4 ('That is a separate lint owned by sprint-plan per the spec'); skills/roadmap-plan/SKILL.md Gotchas ('target_sprint pointing at a closed or cancelled sprint isn't this skill's violation — that's a separate lint owned by roadmap-refine').
      Fix: Add a step 6.4 to sprint-review's review→closed operation: 'For every roadmap item in the full roadmap.md whose target_sprint equals this sprint's number but is NOT in the sprint's roadmap_items list and is NOT status:done or status:cancelled, set target_sprint to null and surface the list to the operator.' Update the attribution in roadmap-refine step 2.4 and roadmap-plan's Gotchas to point consistently at sprint-review as the enforcement point for the closed-sprint case..
      Verify: Create a roadmap item with target_sprint pointing at a planned sprint; close the sprint without including that item in roadmap_items; confirm sprint-review reports and clears the stale reference..

- [ ] [skill-agent-catalog.cross-linking-referenced-by] The spec SHOULD render a chrome-localized 'Referenced by' section on each artifact page listing every artifact whose see_also includes this artifact, derived by inverting the cross-link index. The generator does not implement this section at all.
      Where: scripts/docs/gen_catalog.py — render_page function (around line 922) has no inversion pass; confirmed absent in generated pages such as docs/en/skills/nolte-shared/spec.md..
      Fix: After_build_link_index, build an inverted index {artifact_name: [referencing_artifact, ...]} by iterating all artifacts' see_also lists. Pass this inverted index into render_page and emit a ## Referenced by section (using chrome['referenced_by_label']) listing clickable links when the artifact appears in one or more see_also lists. Add 'referenced_by_label' and its German equivalent to the CHROME dict..
      Verify: After fix: run task docs and confirm that skills/agents with see_also references (e.g. spec-drift-audit, which appears in spec's see_also) show a 'Referenced by: spec' section on their catalog pages..

- [ ] [skill-review.ac1] The worked example in examples/walkthrough.md is a prose transcript describing expected skill behavior, but no actual review plan file has ever been persisted under .audits/skill-review/ for audience-identify (or any other skill used as the example target). The acceptance criterion requires a real, git-tracked plan artifact, not a walkthrough description.
      Where: skills/skill-review/examples/walkthrough.md (exists); .audits/skill-review/audience-identify.md (never existed per full git log).
      Fix: Run the skill-review skill against audience-identify (or another skill), confirm the plan is written to .audits/skill-review/<name>.md, and commit it so the worked-example artifact exists in git history. The plan may be immediately closed per the lifecycle rules once all items are addressed..
      Verify: git log --all --oneline -- .audits/skill-review/<name>.md shows at least one 'review(skill-review): open plan for <name>' commit..

- [ ] [skill-review.ac2] 39 of 43 skills in skills/ have never been reviewed against any version of the skill-management spec since this spec was adopted (2026-04-24). The 4 skills that were reviewed (github-issue-templates-apply, portfolio-inflight-triage, release-notes-curate, release-publish-trigger) were reviewed against skill-management rev 38f4fc0, which is now significantly outdated (current HEAD is 84aafe7, updated 2026-05-29 with ~178 question resolutions).
      Where: .audits/skill-review/ (only .gitkeep present); git log -- .audits/skill-review/*.md shows plans for 4 skills only.
      Fix: Run the skill-review skill across all 43 skills or triage which ones can defer review. Prioritize skills added or heavily modified after the spec adoption date. The 4 previously-reviewed skills should be flagged for re-review since the skill-management spec has changed significantly..
      Verify: git log --all -- .audits/skill-review/*.md shows a plan creation or closure commit for every skill currently in skills/..

- [ ] [skill-review.ac7] No closure commit in the repository's git history matches the current required format 'review(skill-review): close <skill>—<C>C/<W>W/<S>S/<I>I' exactly. The three pre-rename closures (2026-04-30/05-01) use the old BLOCKER severity letter ('0B') and spaced em-dash (' — ') instead of the current Critical letter ('C') and unspaced em-dash. The portfolio-inflight-triage closure (2026-05-28, post-rename) uses wrong prefix 'review: close completed portfolio-inflight review plans' with no severity counts.
      Where: git log output: commits 875898a, e475ff2, a876c77 (old format B), b2d150c (wrong prefix); spec requires format 'review(skill-review): close <skill>—<C>C/<W>W/<S>S/<I>I'.
      Fix: The next closure of a skill-review plan MUST use the current format (C not B, unspaced em-dash). The SKILL.md step 3.5 correctly encodes the new format; the issue is that no closure has happened under the current spec. The historical commits cannot be changed, but documenting this in a brief spec note or waiting for the next closure to verify format compliance are the options..
      Verify: Run 'git log --grep="review(skill-review): close" --oneline' and confirm at least one entry matches 'review(skill-review): close <skill>—NNc/NNw/NNs/NNi'..

- [ ] [skill-review.spec-anchor-ordering] The spec's §Review procedure ordering (line 127) lists eight named check categories but omits the §Checks derived from spec-driven-development slot. The spec-anchor check (MUST run, added in #134) has no named position in the declared review order. Correspondingly, SKILL.md step 6 enumerates the check sequence but names only best-practices checks by section, not the spec-anchor check. A reviewer following the SKILL.md verbatim would run best-practices checks but might not explicitly run the spec-anchor check.
      Where: spec/claude/skill-review/en.md line 127 (ordering list); skills/skill-review/SKILL.md step 6 (check enumeration); spec/claude/skill-review/en.md lines 118–122 (§Checks derived from spec-driven-development).
      Fix: Amend the §Review procedure ordering list (line 127) to insert 'spec-anchor check' between 'system-prompt body' and 'rationale section' (or as a named sub-step under body checks). Also update SKILL.md step 6 to explicitly name the spec-anchor check alongside the best-practices checks..
      Verify: Both spec line 127 and SKILL.md step 6 name the spec-anchor check in the ordered sequence..

- [ ] [skill-vs-agent.hybrid-pattern] spec/project/workflow-health/en.md line 75 refers to 'the pull-request-create agent' in the sequential-chaining example. pull-request-create is a skill, not an agent. The skill-vs-agent spec's MUST NOT on agents invoking skills is not violated in practice (workflow-health-triage correctly chains pull-request-create as a skill), but the misleading terminology in the spec body contradicts the hybrid-pattern rule and can confuse future implementors.
      Where: /home/nolte/repos/github/claude-shared/spec/project/workflow-health/en.md:75.
      Fix: Change 'the pull-request-create agent' to 'the pull-request-create skill' in the example on line 75 to align with the artifact's actual type and to prevent future implementors from reading the chain as an agent invoking a skill..
      Verify: grep 'pull-request-create' spec/project/workflow-health/en.md should not contain the word 'agent' adjacent to 'pull-request-create'..

- [ ] [test-case-derivation.inputs-and-discovery] The MUST to report which documents were processed is not implemented. Phase 4's chat summary instructs only to list derived case IDs and open requirements; the source documents fed to the agent are never explicitly reported back.
      Where: /home/nolte/repos/github/claude-shared/agents/test-case-extractor.md, Phase 4 (line 96).
      Fix: Extend the Phase 4 chat-summary instruction to include an explicit list of the source documents that were processed (e.g. 'Return a chat summary listing: the source documents processed, each derived case id with a one-line title, plus the open requirements.')..
      Verify: Run the agent on two requirement documents and confirm the chat summary names both input paths..

- [ ] [test-case-derivation.output-contract] The MUST to keep each test case self-contained and retrieval-friendly with 'explicit cross-references to related cases' has no enforcement in the template. Spec line 54 lists cross-references as part of the MUST; the Phase 3 template (lines 77-90) has no cross-reference field, and the prose reminder at line 92 only loosely echoes the requirement without providing a template slot.
      Where: /home/nolte/repos/github/claude-shared/agents/test-case-extractor.md, Phase 3 template (lines 77-90).
      Fix: Add a **Related Cases** or **See Also** field to the test-case template in Phase 3 (e.g., `**Related Cases**: [{TC-id}, …]`) so the template operationalises the cross-reference MUST..
      Verify: Check that the Phase 3 template block contains a cross-reference field and that the Hard rules or Phase 4 instruct the agent to populate it..

- [ ] [yaml-json-schema.meta-validation-gate] The lint gate enforces no property-description check (AC-7), no skeleton-order check (AC-10), no type-or-composition check (AC-11), and no unevaluatedProperties check (AC-13). These are MUST-level AC requirements that require lint-gate enforcement, not just skill-time authoring discipline.
      Where: /home/nolte/repos/github/claude-shared/.pre-commit-config.yaml.
      Fix: These checks are difficult to enforce purely with standard validators because JSON Schema meta-validation alone doesn't enforce property ordering, description presence, or $defs naming conventions. Add a small Python script (analogous to scripts/validate_skills.py) that reads every *.schema.yaml and enforces: (a) top-level key order matches the spec's ten-entry list, (b) every top-level property sub-schema has a description, (c) every property sub-schema has type or oneOf/anyOf/enum, (d) no allOf schema uses additionalProperties: false without unevaluatedProperties: false. Wire it as a pre-commit hook and a Taskfile validate: target..
      Verify: Commit a*.schema.yaml that omits a property description; confirm task lint exits non-zero..

### Suggestion

- [ ] [blog-author-trigger.reference-example-annex-wiring] skills/blog-author-trigger/SKILL.md §Automatic dispatch from sprint-execute (line 120) states 'step 5' as the dispatch step, but sprint-execute Operation C defines the blog-trigger dispatch at step 6. CLAUDE.md correctly says 'step 6'. The stale reference creates an internal contradiction.
      Where: /home/nolte/repos/github/claude-shared/skills/blog-author-trigger/SKILL.md line 120.
      Fix: Change 'step 5' to 'step 6' in the §Automatic dispatch from sprint-execute section..
      Verify: After the edit, confirm SKILL.md line 120 reads 'Operation C (in_progress → done), step 6' and matches the step count in sprint-execute/SKILL.md Operation C..

- [ ] [claude/plugin-scoping.distribution-contract-recorded] The plugin-scoping spec §When to split SHOULD (line 46) requires recording each plugin's single distribution contract in its project/portfolio.yml capability rationale. The AC (line 69) requires the contract to be recorded in the README or top-level orientation file. portfolio.yml (project/portfolio.yml) has capability rationales but none of them explicitly states the distribution contract as a composed unit (shared release cadence + marketplace entry + consumer install decision). CLAUDE.md mentions the marketplace entry but not the full contract definition.
      Where: /home/nolte/repos/github/claude-shared/project/portfolio.yml (capability rationale fields) and /home/nolte/repos/github/claude-shared/CLAUDE.md.
      Fix: Add a brief explicit distribution-contract statement to at least one of: the portfolio.yml top-level comment, a capability rationale, or the CLAUDE.md orientation section. The statement should name all three elements: shared release cadence (0.1.x version line), single marketplace entry (nolte/claude-shared), and a single consumer install decision. This satisfies both the SHOULD in §When to split and the AC..
      Verify: After adding the statement, confirm the three elements (release cadence, marketplace entry, install decision) are named in one visible location readable without traversing the full spec..

- [ ] [cookiecutter-template-authoring.rendered-project-documentation-readme-link] The spec's §Documentation MUST (spec line 72) requires the rendered template-level README to link to spec/project/cookiecutter-template-authoring/. The agent's Output contract (lines 85-94) lists eight mandatory report sections but none of them include a check that the authored template README contains this link. Without an explicit output-contract item or hard-rule entry, the agent has no mechanical step that enforces this MUST.
      Where: /home/nolte/repos/github/claude-shared/agents/cookiecutter-template-author.md — Output contract section (lines 85-94) vs. spec/project/cookiecutter-template-authoring/en.md line 72.
      Fix: Add a ninth output-contract item or a new Spec-conformance rule (rule 4) that requires the agent to verify the template-level README contains a link to spec/project/cookiecutter-template-authoring/ before returning success..
      Verify: Check the Output contract section for an explicit README-link verification step; run a scaffold invocation and confirm the generated template README includes the spec link..

- [ ] [project/mission.acceptance-criteria] The spec acceptance criteria (lines 110-123) repeatedly reference 'lints flag' and 'fails validation', implying automated lint enforcement of the mission.md schema. No such automation exists: validate_skills.py covers only skill and agent frontmatter; there is no pre-commit hook, CI check, or script that validates mission.md frontmatter field order, unknown keys, audience-to-goals cross-references, verifies_via resolution, or time_bound shape. All validation is currently manual (as documented in the feature's test hooks). The spec does not carry an explicit MUST for automated linting, so this is a SHOULD-class gap.
      Where: /home/nolte/repos/github/claude-shared/scripts/validate_skills.py (no mission coverage), /home/nolte/repos/github/claude-shared/.pre-commit-config.yaml (no mission hook).
      Fix: Add a mission-specific validation step to scripts/validate_skills.py (or a sibling script) that parses project/mission.md, checks field order, rejects unknown keys, verifies audience cross-references to AUDIENCES.md, resolves relevant_outcomes to goals.md O-<n> entries, resolves verifies_via to the feature file and its acceptance criterion, and validates the time_bound shape. Wire it into the Taskfile test target and the pre-commit config..
      Verify: Run task test and confirm mission.md is validated. Intentionally introduce an unknown key in mission.md frontmatter and confirm the validator rejects it with an appropriate message..

- [ ] [project/mission.frontmatter-schema] The audiences frontmatter field uses the full label text of each audience entry ('Downstream Claude Code users in portfolio projects', 'Plugin author dogfooding inside this repo') rather than the short machine-readable id fields added to AUDIENCES.md in 2026-05-19 ('downstream-user', 'dogfooding-author'). The spec says each entry MUST match 'an audience entry' in the audience artefact, and both forms unambiguously resolve to the correct entries, so this is not a MUST violation. However, the short id form is more stable — it is invariant to label wording changes — and the docs-audience-tracks spec already uses the short id form for docs page frontmatter.
      Where: /home/nolte/repos/github/claude-shared/project/mission.md (frontmatter lines 7-8), /home/nolte/repos/github/claude-shared/AUDIENCES.md (downstream-user, dogfooding-author id fields).
      Fix: Update the audiences frontmatter in project/mission.md to use the short id form (downstream-user, dogfooding-author) and update the ## Audiences body paragraphs to reference those identifiers by name. Also update skills/mission-define/SKILL.md Operation 1 to specify extraction of id fields from AUDIENCES.md entries as the canonical form for the frontmatter list..
      Verify: Confirm project/mission.md audiences: lists downstream-user and dogfooding-author. Confirm ## Audiences paragraphs name those identifiers. Confirm AUDIENCES.md entries still have the matching id: fields..

- [ ] [project/quality-gate.timeouts-exit-code-in-details] The spec SHOULD says the exit code of the underlying tool should be surfaced in the Details column so consumers can distinguish 'lint found 3 errors' (exit 1) from 'lint crashed' (exit > 1). SKILL.md step 4 captures the exit code but the rendered table template in step 5 shows only error counts and pass/fail ratios — the exit code is not explicitly included in the Details column.
      Where: skills/quality-gate/SKILL.md — step 5 table template (lines 119-125) and step 4 parse result section.
      Fix: Update the table template in SKILL.md step 5 to include the exit code in the Details column, e.g. `<n> errors, <m> warnings (exit 1)`. Alternatively add an explicit instruction: 'append the exit code to the Details value for fail/timeout rows'..
      Verify: Check that the SKILL.md table template in step 5 includes an exit-code placeholder or instruction in the Details column..

- [ ] [project/spec-drift-audit.artifact-structure] The template (audit.template.md) adds two sections not in the review-plan four-section layout (## Per-criterion results and ## Decisions) while omitting ## Summary. This creates a minor internal inconsistency between the spec's stated reference to review-plan's 'four-section layout' and what the template actually mandates. Note: review-plan §Relationship explicitly says MUST NOT be used as the output of spec-drift-audit, so the template's divergence is intentional and reasonable — but the spec prose should be updated to say 'severity vocabulary of review-plan' rather than 'four-section layout'.
      Where: spec/project/spec-drift-audit/en.md §Audit result artifact (line: 'MUST structure the artifact per the four-section layout … mandated by spec/claude/review-plan') vs skills/spec-drift-audit/templates/audit.template.md.
      Fix: Amend the spec wording from 'four-section layout and canonical severity vocabulary' to 'canonical severity vocabulary' (since review-plan itself says MUST NOT use its layout for spec-drift output). No change needed to the template..
      Verify: spec/project/spec-drift-audit/en.md §Audit result artifact no longer references 'four-section layout'; only 'canonical severity vocabulary mandated by spec/claude/review-plan' remains..

- [ ] [project/workflow-health.auditing-project-structure-apply] skills/project-structure-apply/SKILL.md visits and audits .github/workflows/ but does not include a workflow-health pass (checking for redness at HEAD on required checks, FLAKES.md presence, or gh run list scan). The spec SHOULD mandates a workflow-health pass in any portfolio audit that already visits .github/workflows/.
      Where: /home/nolte/repos/github/claude-shared/skills/project-structure-apply/SKILL.md — Operations §1 Audit section.
      Fix: Add a workflow-health sub-step to the project-structure-apply Audit operation: after auditing .github/workflows/ file presence, run gh run list --status failure --branch develop --limit 5 and gh run list --status failure --branch main --limit 5, cross-check that no required-check workflow is currently red at HEAD without an open fix/ PR, and verify FLAKES.md existence (if the repository has acknowledged flakes). Report the results in the same audit table under a 'Workflow health' group..
      Verify: After the skill is updated, invoke /nolte-shared:project-structure-apply on any repository and confirm the audit output includes a 'Workflow health' section with failed-run counts and FLAKES.md status..

- [ ] [skill-vs-agent.rationale-documentation] skills/dependency-audit/SKILL.md's rationale section under '## Why this is a skill, not an agent' does not name any counter-dimension. The spec SHOULD name at least one dimension that pointed the other way, and notes that absence of a counter-dimension implies the choice was uncontested. The rationale is otherwise substantive (three bullets covering orchestration, interactivity, and hybrid split), so this is a mild SHOULD gap.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md.
      Fix: Add a brief counter-dimension bullet to the rationale — for example: 'Counter-dimension considered: the scan step alone (detect lockfile, run auditor) is a strong context-isolation candidate. That isolation is provided by the dependency-audit-scanner agent; the parent skill remains a skill because follow-up triage needs mid-flow user approval.'.
      Verify: grep -i 'counter' skills/dependency-audit/SKILL.md should return at least one match..

- [ ] [test-case-derivation.derivation-discipline] The SHOULD to name the derivation technique in the category field is partially met: Phase 2 lists all six techniques (user-journey, input/boundary, state-transition, navigation, visual-feedback, error-guessing) but the template's Category field uses test-type labels (happy-path, validation, error, state-transition, navigation) rather than technique names. A consumer cannot distinguish which technique was applied from the category field alone.
      Where: /home/nolte/repos/github/claude-shared/agents/test-case-extractor.md, line 80 (Category template) and line 62 (Phase 2 derivation instruction).
      Fix: Either (a) update the Category placeholder to include technique names (e.g. '{user-journey | input/boundary | state-transition | navigation | visual-feedback | error-guessing | …}'), or (b) add a separate 'Technique' field to the test-case template to preserve the current type label while also capturing the derivation technique..
      Verify: Derive test cases from a sample requirement and confirm every test case's category (or technique field) maps to one of the six standard technique names..

### Info

- [ ] [branching-model.settings-version-pin-drift] .github/settings.yml extends commons-settings.yml@v1.1.15 while all four required workflows pin gh-plumbing at v1.1.19. The settings pin is four minor versions behind the workflow pins. While not a blocker, this creates a gap between the branch-protection rules in use and what the latest commons declares.
      Where: /home/nolte/repos/github/claude-shared/.github/settings.yml line 1.
      Fix: Update _extends: nolte/gh-plumbing:.github/commons-settings.yml@v1.1.15 to @v1.1.19 to align with the workflow pins. Follow the workflow-health spec bump cadence (gated PR, no Renovate automerge for nolte/gh-plumbing bumps)..
      Verify: grep '_extends' .github/settings.yml shows @v1.1.19..

- [ ] [claude/skills-agents-sweep.context] The spec's Context section references a 2026-05-20 baseline sweep documented in .audits/skills-agents-sweep/2026-05-20-baseline.md, but this file was never committed to git. The sweep appears to have been used informally to develop the methodology but the artefact was not preserved on disk. This is historical context in the spec, not a normative requirement, but it creates a documentation discrepancy.
      Where: spec/claude/skills-agents-sweep/en.md line 11 references .audits/skills-agents-sweep/2026-05-20-baseline.md.
      Fix: Either (a) note in the spec that the 2026-05-20 baseline was informal and no report was committed, or (b) on the next sweep run, ensure the report is committed so the spec's historical reference no longer describes a missing file..
      Verify: If clarified via spec edit, confirm spec/claude/skills-agents-sweep/en.md no longer references a file path that does not exist..

- [ ] [claude/skills-agents-sweep.context] The spec carries Status: draft. All Requirements and Acceptance Criteria are normative in their wording (MUST/SHOULD/MAY), but the draft status may suggest the spec is not yet considered stable. The binding skill (skills/skills-agents-sweep/SKILL.md) treats the spec as authoritative via the hard rule 'When spec/claude/skills-agents-sweep/ and this skill disagree, the spec wins.'
      Where: spec/claude/skills-agents-sweep/en.md line 3.
      Fix: Promote the spec status from draft to stable if the Requirements and Acceptance Criteria are settled, so consumers can treat it as a committed contract..
      Verify: Confirm spec/claude/skills-agents-sweep/en.md header reads Status: stable after the change..

- [ ] [dependency-audit.audit-artifact-should-link-prior] The spec §Audit artifact SHOULD says 'link to the prior audit artifact so the progression is traceable across quarters'. The Step 5 report template and the agent Output shape contain no field for a prior-audit link. This is a SHOULD so the severity is Info, but the auditor's finding [5] only flagged MUST-level fields; the SHOULD link was omitted from their list.
      Where: /home/nolte/repos/github/claude-shared/skills/dependency-audit/SKILL.md — Step 5 report template.
      Fix: Add an optional 'Prior audit: <path or URL>' line to the Step 5 report template header block..
      Verify: Search Step 5 template for 'prior' or 'previous audit' — neither term appears..

- [ ] [design/graphic-prompt-authoring.context] The spec is still in draft status (en.md line 2: 'Status: draft'). The binding agent is fully implemented, but the spec itself has not been promoted to stable. No acceptance criterion is missed, but the draft flag signals the spec owner intends to revisit open questions (style-reference equivalent for sref-less generators; asset-type vocabulary ownership if imagery-style/ lands).
      Where: /home/nolte/repos/github/claude-shared/spec/design/graphic-prompt-authoring/en.md line 2.
      Fix: Once the open questions in §Open Questions are resolved (corporate-design-colors declaring the sref-less generator equivalent; confirming whether the asset-type vocabulary migrates to imagery-style/), promote the spec status from draft to stable..
      Verify: grep 'Status:' spec/design/graphic-prompt-authoring/en.md should read 'Status: stable'..

- [ ] [lektorat.outputs-audit-trail] The .audits/lektorat/ directory does not exist in this repository. The spec SHOULD write both outputs there to accumulate a reviewable audit trail. The spec itself acknowledges this: '.audits/lektorat/ is empty.' This is a gap in the first-run experience, not a conformance violation since the SHOULD is output-path guidance rather than a MUST.
      Where: /home/nolte/repos/github/claude-shared/.audits/ (lektorat/ subdirectory absent).
      Fix: Run the lektorat-apply audit operation against at least one representative artefact (e.g. README.md) to initialise the .audits/lektorat/ audit trail. This also validates the skill end-to-end and satisfies the Acceptance Criterion that asks for a demonstrated audit run..
      Verify: After fix: .audits/lektorat/<YYYY-MM-DD-HHMM>/findings.json and summary.md exist; JSON shape matches spec §Outputs verbatim..

- [ ] [project/docs-multilingual-authoring.docs-freshness-section-name-mismatch] AC item 6 references 'spec/project/docs-freshness/ §Finding categories' but the actual section in docs-freshness/en.md is named '### Categories of drift'. The cross-reference content is correct and the link is unambiguous to a reader, but the AC's section anchor citation does not match the real section heading.
      Where: spec/project/docs-multilingual-authoring/en.md line 89 (AC item 6), spec/project/docs-freshness/en.md line 30.
      Fix: Update AC item 6 in docs-multilingual-authoring/en.md to read '§Categories of drift' instead of '§Finding categories', matching the actual section heading in docs-freshness..
      Verify: Confirm docs-freshness/en.md §Categories of drift contains the cross-reference sentence and that AC item 6 now cites the correct section name..

- [ ] [project/spec-drift-audit.ac-revision-reference] The Q2 artifact's Git revision (9cad860) is accessible in the git object store, but the artifact pre-dates the spec-drift-audit skill (added 2026-05-21). There is no skill version to record for Q2; future artifacts will have a skill available. This is expected and not a blocker.
      Where: .audits/spec-drift/2026-Q2.md (no skill-version field; the skill did not exist when Q2 ran).
      Fix: No action required for Q2. For all future artifacts: populate the tools-used entry for the spec-drift-audit skill with its git SHA at time of invocation..
      Verify: Next artifact's tools-used list includes spec-drift-audit with a git SHA or semver tag..

- [ ] [project/spec-readiness.open-question-ci] The spec's sole Open Question (§Open Questions, line 104) concerns when to promote from advisory to CI-enforced gate. The §Triggers text (line 71) already encodes the answer as the default: stay advisory, revisit when four full audits with <10% false-Critical rate exist. The OQ is therefore a parking-lot question with its default explicitly recorded in the spec body. No downstream work is blocked.
      Where: /home/nolte/repos/github/claude-shared/spec/project/spec-readiness/en.md lines 104–105.
      Fix: No action required. The OQ could be collapsed into a note in §Triggers to reduce reader confusion, but this is a cosmetic improvement, not a gap..
      Verify: Confirm that the §Triggers text at line 71 covers the OQ's decision scope and that no implementation step is gated on answering the OQ differently..

- [ ] [research-triangulate.context] The spec is Status: draft. Per spec-drift-audit SKILL.md §step 3, draft specs are in scope for audits. The findings above represent real implementation gaps that must be closed when the spec is promoted; they are not conditional on the draft status.
      Where: /home/nolte/repos/github/claude-shared/spec/claude/research-triangulate/en.md (line 3).
      Fix: No immediate action required solely on account of draft status, but the Critical findings above should be tracked and resolved before the spec is promoted to active..
      Verify: spec/claude/research-triangulate/en.md Status field reads something other than 'draft' only after implementation gaps are resolved..

- [ ] [review-plan.file-location-naming] Several .audits/ files predate the review-plan spec or serve non-review-plan purposes (docs-catalog-refresh.md, spec-drift/2026-Q2.md, spec-readiness/*.md, webview-ui-expert/*.md) and lack YAML frontmatter; they use custom formats incompatible with review-plan §Frontmatter. These are not formally review-plan artifacts but their presence under .audits/ may mislead tooling that scans all .md files for frontmatter.
      Where: .audits/docs-catalog-refresh.md (directly under .audits/ root, not in a review-type subdirectory); .audits/webview-ui-expert/*.md (no frontmatter); .audits/spec-drift/2026-Q2.md and .audits/spec-readiness/*.md (excluded from review-plan scope by §Non-Goals).
      Fix: No action required for spec-drift/spec-readiness (explicitly excluded). For docs-catalog-refresh.md and webview-ui-expert/*.md, consider either migrating them to review-plan format or moving them to a directory outside .audits/ to avoid format confusion..
      Verify: All .audits/ files that are not spec-drift or spec-readiness artifacts either have valid review-plan frontmatter or are in a directory documented as a review-plan-exempt zone..

- [ ] [review-plan.file-location-naming] The .audits/open-questions/2026-05-29/ directory is untracked (shown as ?? in git status) and contains six files with no YAML frontmatter; these are not review-plan artifacts but violate the MUST keep .audits/ checked into git requirement if they are intended to persist.
      Where: .audits/open-questions/2026-05-29/ — git status shows ?? .audits/open-questions/.
      Fix: Either commit the open-questions files (git add .audits/open-questions/) if they are intended to be persistent audit artifacts, or explicitly document in .gitignore or CLAUDE.md that they are ephemeral working files..
      Verify: git status shows no untracked files under .audits/ after the decision is applied..

- [ ] [skill-review.status-draft] spec/claude/skill-review/en.md declares 'Status: draft'. All binding procedural requirements are fully authored in the spec and the implementing SKILL.md, but several acceptance criteria remain unmet (AC1, AC2, AC7). The draft status is consistent with this state, but no promotion checklist or target milestone is recorded in the spec.
      Where: spec/claude/skill-review/en.md line 2.
      Fix: Consider promoting the spec to 'active' once AC1 (worked example plan in git), AC2 (bulk skill review backlog started), and the spec inconsistency at line 82 are resolved. This is an operator decision; no immediate action required..
      Verify: Status line reads 'active' and the blocking ACs are satisfied..

## Processing log

2026-05-30—audit-created—full-scope bidirectional audit at b91b67b; raw findings recorded—verified by agent:full-spec-audit-pass-a + agent:full-spec-audit-pass-b

---

## Prior Q2 audit record — 2026-05-19 (spec-change follow-up, retained for history)

_The following is the previous `2026-Q2.md` record (a narrow spec-change follow-up audit at HEAD `9cad860`, all findings already RESOLVED). It is superseded in scope by the full-scope audit above but retained in-file so its resolved-findings trail is not lost. The full original also survives via `git log --follow`._

<details>
<summary>Prior 2026-05-19 spec-change follow-up audit (6C/4W/4I, all resolved)</summary>

### Spec ↔ Implementation Drift Audit — 2026-Q2

**Date:** 2026-05-19 · **Trigger:** spec-change follow-up (mkdocs-structure, docs-audience-tracks, prose-style erweitert) · **Auditor:** Claude Code (Opus 4.7) per `spec/project/spec-drift-audit/` § Execution

**Repo:** `/home/nolte/repos/github/claude-shared` · **Branch:** `feat/tech-stack-cicd-plugin-layers` · **HEAD at audit start:** `9cad860`

#### Scope

- 41 Specs (canonical EN) under `spec/<topic>/<slug>/`
- 30 Skills under `skills/<name>/SKILL.md`
- 9 Agents under `agents/<name>.md`
- Method: name-based Spec ↔ Skill/Agent mapping, plus targeted SKILL.md / agent-md reads for the four implementations most affected by the recent spec edits

#### Summary

| Finding class | Critical | Warning | Info |
|---|---|---|---|
| Coverage gaps (Spec without implementation) | 1 | 2 | 4 |
| Drift (Implementation doesn't know spec update) | 4 | 1 | 0 |
| Orphan implementation (Skill/Agent without spec) | 1 | 1 | 0 |
| **Total** | **6** | **4** | **4** |

#### Findings & Resolutions

##### Critical

**C-1 — `docs-audience-tracks` has zero implementation coverage** → **RESOLVED**
- New skill `skills/docs-audience-tracks-apply/SKILL.md` created with audit / migrate / patch operations against `spec/project/docs-audience-tracks/`.

**C-2 — `mkdocs-structure-apply` doesn't know §Content modes (Diátaxis alignment)** → **RESOLVED**
- `skills/mkdocs-structure-apply/SKILL.md` extended: §Audit operation now lists Content-modes-Diátaxis-alignment as a separate audit area; per-page-frontmatter check includes `content_mode` and `track`; patch operation proposes `content_mode` heuristically.

**C-3 — `audience-identify` ignores the `track:` mapping MUST** → **RESOLVED**
- `skills/audience-identify/SKILL.md` extended: step 5a (audience-to-track mapping) added with default mapping; validate-operation extended; `templates/audiences.template.md` updated.

**C-4 — `docs-freshness-checker` recognises neither `track` nor `content_mode` drift** → **RESOLVED**
- `spec/project/docs-freshness/` §Categories of drift extended (EN+DE) with three new categories (track-frontmatter drift, content-mode drift, audience-track mismatch) plus updated severity grammar.
- `agents/docs-freshness-checker.md` extended: Phase 6b (track / content_mode frontmatter) + Phase 6c (audience-track consistency) added; severity table updated; output shape updated.

**C-5 — `prose-vale-curator` doesn't know §Voice and tone** → **RESOLVED**
- `agents/prose-vale-curator.md` extended: step 5a added (Voice-and-tone spot check with heuristic regex packs for passive voice, second-person violations, sentence-case violations, gendered pronouns, militaristic terms, exclamation marks, cultural idioms); output shape extended with §"Voice-and-tone spot check".

**C-6 — `cookiecutter-template-author` without spec anchor** → **RESOLVED**
- New spec `spec/project/cookiecutter-template-authoring/` (EN+DE) created — lifts the agent-body MUSTs into a normative shape covering rendered-project conformance, hook contract, test harness, anti-pattern refusal, and documentation requirements.
- `agents/cookiecutter-template-author.md` updated to cite the new spec as canonical normative anchor.

##### Warning

**W-1 — `tech-stack-discovery` without dedicated skill** → **RESOLVED**
- `skills/portfolio-audit/SKILL.md` extended with fourth operation "Discover tech stack" — operationalises `spec/portfolio/tech-stack-discovery/`. Audits per repo against the portfolio baseline (`spec/portfolio/tech-stack/`), classifies findings as in-baseline / drift / net-new.

**W-2 — `readme-structure` without dedicated skill** → **RESOLVED**
- New skill `skills/readme-structure-apply/SKILL.md` created with audit / scaffold / patch operations against `spec/project/readme-structure/`.

**W-3 — `prose-style/de.md` references "Microsoft-DACH-Hausstil" without source** → **RESOLVED**
- Source ratification added: Microsoft Localization Style Guides — German listed in §Sources of both EN and DE versions; in-text citations updated to point at the canonical reference.

**W-4 — Weak cross-reference between skill ↔ spec (one-way)** → **INFO, no action required**
- Documented; Sources sections now provide a back-pointer mechanism, so future Reviewers can navigate skill → spec → other consuming skills.

##### Info

**I-1, I-2, I-3, I-4 — confirmed parking-lot / OK status**: `docs-dry-refactor` cleanly anchored, `spec` skill self-referential by design, `spec-drift-audit` is a process spec (this audit is the process), five conventions specs without skills are intentional.

#### Caller follow-ups

Done in this audit; no remaining items. The next quarterly Spec-Drift audit (2026-Q3) inherits the trajectory from this artefact.

#### Related artefacts

- Spec-readiness audit: `.audits/spec-readiness/2026-Q2.md`
- This audit's source process: `spec/project/spec-drift-audit/`

#### Health

- Specs parsed: 41 (full corpus)
- Skills parsed: 30 (full corpus, targeted deep reads on mkdocs-structure-apply, audience-identify, docs-dry-refactor, spec, portfolio-audit, audience-identify template)
- Agents parsed: 9 (full corpus, targeted deep reads on docs-freshness-checker, prose-vale-curator, cookiecutter-template-author)
- Cross-references verified: ~60 spec-path references in skill / agent bodies; all spec paths resolve on disk after this audit's edits
- Previous audit: none — `.audits/spec-drift/` did not exist before this audit
- Findings: 14 total / 14 resolved / 0 remaining

</details>
