# License Check — nolte/claude-shared (2026-07-24)

Performed against `spec/project/license-check/` via the `/nolte-engineering:license-check`
skill (SBOM stage in-skill, read-only inventory via the `license-check-scanner` agent,
policy gate + this artifact in-skill). This is the automated re-run mandated by
[issue #455](https://github.com/nolte/claude-shared/issues/455): the implementing skill
did not exist at the 2026-06-01 manual run.

**Prior record (traceability):** `.audits/license-check/license-2026-06-01.md`
(verdict **Pass**) was retired to git history in the 2026-07-24 audit-folder cleanup.
It is reachable at commit
[`92086e1`](https://github.com/nolte/claude-shared/commit/92086e1) (its parent `92086e1^`
holds the file), created originally in `e466cf8`. This run confirms the prior verdict
still holds under the drifted pins.

- **Revision checked:** `develop` @ `db0aaf4` (branch `chore/license-check-rerun`)
- **Trigger:** operator-requested full check (issue #455; no implementing skill existed at the prior run)
- **Scope:** whole repo. Stacks: Python only (docs build + validator/test tooling) plus the
  arm's-length pre-commit/Vale CI toolchain. No Node (`package.json`) or Go (`go.mod`)
  manifest exists, so `license-checker-rseidelsohn` and `go-licenses` were correctly skipped
  (stack absent). 50 components in scope (10 direct, 40 transitive/toolchain).
- **Tools + versions:** `cyclonedx-py 7.3.1` / `cyclonedx-python-lib 11.11.0`
  (SBOM `environment` mode against an ephemeral venv, removed after); `reuse 6.2.0`
  (`reuse lint`, read-only); PyPI JSON API + GitHub license API for Trove-classifier
  disambiguation and the non-Python toolchain.
- **SPDX License List version:** **3.28.0** (current latest release, published 2026-02-20;
  unchanged from the prior record). Every SPDX identifier below resolves to full text via
  `https://spdx.org/licenses/<ID>.json` (endpoint verified reachable this run).
- **License source of truth:** the generated CycloneDX SBOM (`task license:sbom`,
  38/38 components with a resolved license), gap-filled read-only for the root
  `requirements-dev.txt` `jsonschema` chain (5 components, all MIT — see §SBOM coverage note)
  and for the non-Python pre-commit/Vale toolchain (7 components).

## Outbound license (compatibility anchor)

Root `LICENSE` is **MIT** (valid SPDX identifier). Every dependency is checked for
compatibility against MIT. REUSE 3.3 compliant: `LICENSES/MIT.txt` + blanket `REUSE.toml`
(`path = "**"`, `SPDX-License-Identifier = "MIT"`); `reuse lint` reports 748/748 files with
copyright and license info, 0 errors.

## Conveyed vs. arm's-length

The distributed plugin artifact (the marketplace) is Markdown (skills, specs, agents),
JSON manifests, curated SVG brand masters, and the bundled `image-generate` Python script —
**all own code under MIT. No third-party dependency is shipped.** The entire Python
dependency set (`mkdocs*`, `pytest`, `jsonschema`, …) plus the pre-commit hooks
(`pre-commit-hooks`, `yamllint`, `markdownlint-cli`) and the Vale style packages run
**arm's-length** (docs build, dev, CI) and never reach a consumer. This determines that
**every copyleft finding below sits in the `review` tier, not `deny`** — no strong- or
network-copyleft component is conveyed, linked, or networked.

## Policy gate

- **allow: 45** — the permissive and public-domain categories (MIT, BSD-2/3-Clause,
  Apache-2.0, and the dual `Apache-2.0 OR BSD-2-Clause` / `Apache-2.0 OR BSD-3-Clause`
  expressions), all arm's-length.
- **review: 5** — 2 weak-copyleft (MPL-2.0), 1 strong-copyleft (GPL-3.0-or-later),
  1 `NOASSERTION`, 1 `LicenseRef` content-attribution note. Each carries a recorded response
  below, so none blocks.
- **deny: 0** — no conveyed/linked/networked strong- or network-copyleft component.

## Review-tier findings (all acceptable; all arm's-length; each with a recorded response)

### 1. yamllint `v1.38.0` — GPL-3.0-or-later — strong copyleft — use-context: arm's-length
Run as a `pre-commit` hook in a separate external process; never imported, linked, or
shipped. The GPL source-disclosure obligation triggers on *conveying*, not on *running* the
tool, so no obligation falls on this repository. The FSF static=dynamic-linking position is
the conservative default the gate uses, and is recorded here as FSF interpretation, not
settled case law — but it is not even reached, since no linking event exists.
**Response:** exception with rationale — approver `nolte`, `valid-until 2027-07-24`.
Re-confirmed from the prior record; the pin drifted `v1.35.1 → v1.38.0`, license unchanged.
A permissive alternative for plain YAML validity (`check-yaml`, already wired) exists but
does not replace style linting.

### 2. certifi `2026.7.22` — MPL-2.0 — weak (file-level) copyleft — use-context: arm's-length
Transitive (via `requests`), invoked only as a CA-bundle data file inside the docs-build
venv; not modified, not conveyed. MPL-2.0 file-level copyleft attaches per covered file and
does not propagate to this repository's own files.
**Response:** exception with rationale — approver `nolte`, `valid-until 2027-07-24`.
Re-confirmed from the prior record (pin drifted, license unchanged).

### 3. pathspec `1.1.1` — MPL-2.0 — weak (file-level) copyleft — use-context: arm's-length
Transitive (via `mkdocs`/`properdocs`), arm's-length, not conveyed. Same MPL-2.0 file-level
reasoning as certifi.
**Response:** exception with rationale — approver `nolte`, `valid-until 2027-07-24`.
Re-confirmed from the prior record (license unchanged).

### 4. vale-style-nolte-styles `v0.1.17` — NOASSERTION — use-context: arm's-length — NEW
Pinned via `.vale.ini` `Packages =` to release `v0.1.17` of the operator's own
`nolte/vale-style` repo. No `LICENSE`/`LICENSE.md` exists at that repo's root, and GitHub's
license-detection API returns none, so it resolves to `NOASSERTION` and routes to `review`
(never `allow`, per the spec). It is an arm's-length CI style package that is never
conveyed, so no obligation flows to this repository regardless. Very likely intended as MIT
(matching the sibling `claude-shared` pattern) but that MUST NOT be assumed.
**Response:** exception with rationale — approver `nolte`, `valid-until 2026-10-24`
(shorter window, because this is a fixable documentation gap in an owned repo).
**Follow-up disposition:** add a `LICENSE` file (SPDX `MIT`) to `nolte/vale-style` so the
identifier resolves cleanly. Not performed in this run — a cross-repo write is outside the
mandate of issue #455; recorded here and to be opened as a separate issue against
`nolte/vale-style`.

### 5. vale-style-Microsoft (content layer) — LicenseRef-Microsoft-Writing-Style-Guide-CC-BY-4.0 — use-context: arm's-length — NEW (info)
The `errata-ai/Microsoft` rule-implementation **code** is MIT (raw `LICENSE` fetched;
GitHub's detection API returned `null` — a detection gap, not an absence), classified
`allow`. Its rules implement the *Microsoft Writing Style Guide* content, whose upstream
license is CC-BY-4.0. Vale rules run arm's-length at dev/CI time and this repository does not
redistribute the style-guide text, so the CC-BY attribution obligation is not triggered by
any conveyance here.
**Response:** documented as informational; no obligation triggered. No action required.

No `deny`-tier findings: no conveyed or networked component carries strong or network
copyleft.

## Compatibility against the outbound MIT license

Every `allow` component is a lax-permissive license (MIT/BSD/ISC/Apache-2.0) or a
public-domain-equivalent expression, all mutually compatible with and absorbable under MIT.
The dual expressions (`Apache-2.0 OR BSD-2-Clause` for `packaging`,
`Apache-2.0 OR BSD-3-Clause` for `python-dateutil`) each offer an MIT-compatible arm.
No BSD-4-Clause advertising-clause exception is present. The three copyleft findings are
arm's-length and non-conveyed, so no conveyed-work compatibility question against MIT arises.

## Own code

- Root `LICENSE` is MIT with a valid SPDX identifier.
- REUSE 3.3 compliant (`LICENSES/MIT.txt` + blanket `REUSE.toml`); `reuse lint` clean.

## Attribution / NOTICE

No third-party code is shipped in the plugin artifact, so no third-party attribution or
NOTICE file is obligated for distribution. No Apache-2.0 dependency is conveyed, so no
`NOTICE` propagation is required.

## AI provenance

- The brand/mascot **raster** renders under `design/assets/` are AI-generated (provider
  `cloudflare`, model `@cf/black-forest-labs/flux-1-schnell`, recorded per file in
  `.meta.json` sidecars). FLUX.1-schnell on Cloudflare Workers AI emits Apache-2.0 output
  (permissive). These files are deliberately gitignored (`.gitignore` §65–72) and
  reproducible from the tracked prompts under `design/prompts/*.md`; the account id in the
  sidecars is redacted. Unchanged from the prior record.
- **NEW (SHOULD gap):** the committed **SVG** brand masters
  (`design/brand/{logo,mascot}/*.svg`) are hand-vectorized from those gitignored AI renders
  via `design/brand/vectorize.py`, but carry **no in-tree `.meta.json` provenance sidecar**
  (provenance data exists only at generation time). They are covered by the blanket REUSE
  MIT annotation, but the spec's §AI provenance MUST for committed AI-derived artefacts is
  only partially met. **Recommendation:** record a machine-readable provenance annotation
  (generator/model/tier) alongside the committed SVG masters. Suggestion-level; non-blocking.
- No open-weight model licenses (OpenRAIL / Llama / Gemma) are in use.

## Health / gaps for follow-up

- **SPDX List version surfacing:** no tool in the read-only chain prints its bundled SPDX
  List version on the CLI; the version above (3.28.0) is anchored to the current
  `spdx/license-list-data` release, matching the prior manual record. Pinning
  `reuse`/`license-expression` to an exact bundled-list version would let a future run
  surface this mechanically.
- **SBOM coverage:** `task license:sbom` installs `docs/requirements.txt` +
  `evals/requirements-dev.txt` only; it does not install the root `requirements-dev.txt`
  (`jsonschema>=4.21,<5`, used by `scripts/validate_schemas.py`), so `jsonschema` and its 4
  transitive deps were gap-filled read-only (all MIT). Recommend widening the target's
  install set so future SBOMs cover that manifest natively.

## Verdict

**Pass.** No `deny`-tier findings. The toolchain is permissive end to end. The three prior
`review`-tier copyleft exceptions still hold under drifted pins and are re-recorded
(yamllint GPL-3.0-or-later; certifi + pathspec MPL-2.0), all arm's-length and non-conveyed.
One new arm's-length `review` item surfaced — `nolte/vale-style` resolving to `NOASSERTION`
for lack of a `LICENSE` file — recorded with a time-bounded exception and a follow-up to add
that file in the owning repo. Own license is clean and the repository remains REUSE 3.3
compliant. Every non-`allow` finding carries a recorded response.

## Follow-ups

- Re-run `task license:sbom` and this check before each release tag and on manifest changes
  (spec's continuous-CI requirement).
- Open an issue against `nolte/vale-style` to add a `LICENSE` file (SPDX `MIT`), clearing the
  `NOASSERTION` finding at its source before the `2026-10-24` exception window closes.
- Consider adding provenance annotations for the committed SVG brand masters.
- Widen `task license:sbom`'s install set to include the root `requirements-dev.txt`.
