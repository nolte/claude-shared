# Portfolio Audit — 2026-07-24

On-demand cross-repository capability audit (issue #452), per
`spec/portfolio/portfolio-management/` §Portfolio audit. Run to work off the
three open carry-findings of the retired `2026-07-03` quarterly report and to
re-verify the whole portfolio against the current `develop` tip. The cadence
trigger and the on-demand trigger produce the same artefact shape, so this
report supersedes `2026-07-03` as the current canonical record under
`.audits/portfolio/`.

## Scope

- **Member-set resolution** (§Portfolio scope): the `nolte` account is a GitHub
  **user**, not an org. Enumerated via
  `gh api users/nolte/repos --paginate`
  (`select(.archived==false and .private==false and .fork==false)`): **19**
  public, non-archived, non-fork repositories. No upstream forks entered the
  set (the `.fork==false` filter drops them per §Portfolio scope). Rate limit
  ~4994/5000 at start.
- **Resolved Portfolio-Member set: 19 own repositories.** Of these, **15 ship a
  valid `project/portfolio.yml`** (ansible-reachy-mini-bootstrap, blog,
  claude-home-assistant, claude-reachy-mini, claude-shared,
  cookiecutter-gh-project, gh-plumbing, kamerplanter, kamerplanter-ha,
  reachy-mini-app, reachy-mini-mcp, taskfiles, terraform-github-bootstrap,
  vale-style, workstation), **2 carry a valid `portfolio: excluded` opt-out
  marker** (esphome-configs, k8s-home-lab), and **2 have neither** — genuine
  gaps (gh-portfolio-ops, pre-commit-hooks). This is unchanged from `2026-07-03`.
- **Manifest collection**: performed directly via `gh api` (orchestration and
  reduction stayed in the skill, as the runbook permits). Each of the 15
  manifests was downloaded, YAML-parsed, and reduced to its per-repository
  capability summary. **24 capabilities** across the 15 manifests.
- **Checks run**: manifest presence, manifest validity (5 required fields),
  cross-repository semantic duplicate detection, gap analysis (broken peer /
  spec-demanded / copy-paste smell), tech-stack consistency per
  `spec/portfolio/tech-stack/` §Portfolio audit integration, and rendered-
  inventory membership parity + byte-freshness.

## Summary

| Severity | Count |
|---|---|
| Critical | 0 |
| Warning | 1 |
| Suggestion | 2 |
| Info | 6 |

No `Critical` findings: all 24 declared capabilities carry the five required
fields (`name`, `description`, `audience`, `status`, `rationale`), every
manifest parses as YAML, all 14 peer references resolve, no cross-repository
duplicate is confirmed, and the four declared tech-stack blocks are
structurally clean. The single `Warning` and the two `Suggestion`s are the
three carry-findings from `2026-07-03`, re-verified as still open and now
routed to tracking issues (below). One fresh `Info` was added this run
(experimental capabilities missing `since:`).

## Findings

### Warning

- **[portfolio-management §Portfolio scope / §Capability inventory per repository] (W1) `nolte/gh-portfolio-ops` and `nolte/pre-commit-hooks` ship no `project/portfolio.yml` and no opt-out marker.**
  Every own (non-fork, non-archived, non-opted-out) repository is a
  Portfolio-Member and MUST either ship a manifest or declare
  `portfolio: excluded` in `CLAUDE.md`. Both repositories carry a normal,
  non-excluded `CLAUDE.md` and are unambiguously capability-bearing:
  `gh-portfolio-ops` = "operational automation over the `nolte/*` GitHub
  portfolio, sibling to gh-plumbing"; `pre-commit-hooks` = "a collection of
  reusable pre-commit hooks for the nolte portfolio, consumed remotely". Carried
  forward unchanged from `2026-07-03` (and `2026-06-02` before that, for the
  then-gh-plumbing/taskfiles gap now closed).
  **Disposition**: routed through the `continuous-improvement` triage loop as a
  precise per-repository issue (bootstrap-a-manifest **or** add-the-marker), no
  direct push to either foreign repo:
  - nolte/gh-portfolio-ops#1
  - nolte/pre-commit-hooks#9

### Suggestion

- **[tech-stack §Per-repository tech-stack block] (S1) 11 manifest-bearing members carry no `tech_stack:` block.**
  Only 4 of 15 manifests (`claude-shared`, `blog`, `vale-style`, `workstation`)
  declare a `tech_stack:` block. The other 11
  (`ansible-reachy-mini-bootstrap`, `claude-home-assistant`,
  `claude-reachy-mini`, `cookiecutter-gh-project`, `gh-plumbing`,
  `kamerplanter`, `kamerplanter-ha`, `reachy-mini-app`, `reachy-mini-mcp`,
  `taskfiles`, `terraform-github-bootstrap`) inherit the portfolio baseline by
  construction, so the missing block is **not** an error — only a completeness
  opportunity. Members with a genuine repo-specific stack delta
  (`kamerplanter`, `kamerplanter-ha`, `reachy-mini-app`, `reachy-mini-mcp`,
  `cookiecutter-gh-project`, `terraform-github-bootstrap`) would benefit from
  running `tech-stack-capture`. The fresh run **confirmed the same 11-member
  list** as `2026-07-03`.
  **Disposition**: `tech-stack-capture` is an interactive per-repo skill, not
  run cross-repo; collective tracking issue opened in `claude-shared`:
  nolte/claude-shared#462. Distinct from #457 (tech-stack **rendering**).

- **[portfolio-management §Decision documentation] (S2) The Reachy-Mini cluster declares no intra-cluster `peers:` edges.**
  `reachy-mini-app`, `reachy-mini-mcp`, `claude-reachy-mini`, and
  `ansible-reachy-mini-bootstrap` form a coherent cluster (behavior app, MCP
  server, Claude Code plugin, and Ansible provisioning for the same device) but
  declare no `peers:` references among one another. This is not a duplicate;
  adding the `<repo>:<capability>` cross-references would let the rendered
  inventory draw the cluster relationship and its Mermaid peer edges. Operator
  to confirm the intended topology.
  **Disposition**: documented analogously to S1; coordination tracking issue
  opened in `claude-shared`: nolte/claude-shared#463.

### Info

- **[portfolio-management §Capability inventory per repository] (I1) Two experimental capabilities carry no `since:` field.**
  `reachy-mini-app:reachy-mini-behavior-app` and
  `vale-style:vocabulary-curation-spec` are `status: experimental` but declare
  no `since:`. The spec's stagnation SHOULD (surface an experimental capability
  older than four closed owner-repository sprints as a `Suggestion`) measures
  from `since:`, which it calls "effectively required for experimental
  capabilities". No stagnation can be measured for these two today; `since:` is
  a schema-optional field so this is an observation, not a `Warning`. Recording
  it so a future audit can measure stagnation once the field is added.
- **[portfolio-management §Cross-repository duplicate detection] (I2) Intentional plugin adjacency, no duplicate.**
  `claude-home-assistant`, `claude-reachy-mini`, and `claude-shared`
  (`shared-claude-code-skills` / `shared-claude-code-agents`) each ship "Claude
  Code skills/agents for domain X". This is the consumer-audience split
  sanctioned by `spec/claude/plugin-scoping/`, not a cross-repository
  duplicate. No action.
- **[portfolio-management §Cross-repository duplicate detection] (I3) Kamerplanter core/HA split is intentional, no duplicate.**
  `kamerplanter:plant-lifecycle-management-system` and
  `kamerplanter-ha:kamerplanter-home-assistant-integration` are the core system
  and its decoupled Home Assistant surface, explicitly peer-linked
  (`kamerplanter-ha → kamerplanter:plant-lifecycle-management-system`). Distinct
  ownership, not a duplicate. No action.
- **[portfolio-management §Portfolio scope] (I4) Opt-out markers confirmed.**
  `esphome-configs` (small reusable ESPHome config fragments) and `k8s-home-lab`
  (personal home-lab Kubernetes infrastructure) carry valid `portfolio:
  excluded` markers in `CLAUDE.md` and are correctly excluded from the member
  set. Re-verified this run.
- **[tech-stack §Kind enum] (I5) Tech-stack watchlist not yet at revision threshold.**
  The four `kind: other` baseline entries (`release-drafter`, `probot-settings`,
  `boring-cyborg`, `stale-bot`, all `since: 2026-05-18`) remain watchlist
  candidates for enum revision, but at ~67 days have not crossed the
  two-consecutive-quarterly-audits or 180-day threshold (this on-demand run is
  not a new quarter). Reminder only, no action.
- **[portfolio-management §Documentation rendering] (I6) Rendered inventory is current.**
  `portfolio/aggregate.yml` covers all 15 manifest-bearing members (membership
  parity, matches the collected manifest set). A read-only run of
  `scripts/docs/gen_portfolio.py` produced **no diff** against the committed
  `docs/{en,de}/portfolio/index.md` pages — they are byte-fresh against the
  snapshot, matching the CI freshness gate. No re-render needed; the Render
  operation stays out of scope (issue #452, deferred per #457).

## Tech stack

Tech-stack-consistency findings from this same run per
`spec/portfolio/tech-stack/` §Portfolio audit integration. No `Critical` or
`Warning` in this section; the standalone per-repo `tech_stack:`-block
completeness item is recorded as `Suggestion` S1 under Findings, and the
`kind: other` watchlist as `Info` I5.

- **No forbidden duplication** (`group: automation` / baseline ownership). Each
  of the 14 non-`claude-shared` manifest-bearing members returns HTTP 404 on
  `portfolio/tech-stack.yml`; the global baseline lives only in `claude-shared`
  → clean (**0 Critical**).
- **Blocks structurally valid** (`group: build-tooling` / additions). All
  additions in the 4 declared blocks (`blog`, `vale-style`, `workstation`,
  `claude-shared`) carry the mandatory `group:` field; no addition shadows an
  inherited baseline entry (baseline names: `mkdocs`, `renovate`,
  `github-actions`, `task`, `vale`, `pre-commit`, `markdownlint`, `gh-plumbing`,
  `release-drafter`, `probot-settings`, `boring-cyborg`, `stale-bot`,
  `claude-code`, `claude-code-plugin`); every `overrides:` target (`mkdocs`,
  `claude-code`, `claude-code-plugin`, `markdownlint`) references an existing
  global entry; no `regroup:` records are present → **0 Critical**.
- **`acknowledged-missing-signal` markers correct** (`group: build-tooling` /
  rationale). `blog` (node, npm, astro, typescript, tailwindcss, github-pages)
  and `vale-style` (github-releases) carry the marker written by
  `tech-stack-capture`; their `source_of_truth` files exist, so no
  status-active-not-in-signals `Warning` applies and no rationale-downgrade
  triggers.
- **Scope note.** Per-repository inherited-signal drift verification (inherited
  `status: active` baseline entries vs. actual on-disk signals across all 15
  members) is delegated to `tech-stack-drift-reviewer`, not performed in this
  portfolio-wide run.

## Processing log

- Enumerated members via `gh api users/nolte/repos --paginate`
  (`select(.archived==false and .private==false and .fork==false)`): 19 own
  repositories; no forks in the set.
- Probed `project/portfolio.yml` presence per member via the contents API
  (`--jq '.sha'`, HTTP-status-authoritative): 15 present, 4 absent
  (esphome-configs, gh-portfolio-ops, k8s-home-lab, pre-commit-hooks).
- Downloaded and YAML-parsed all 15 manifests; reduced each to its structured
  per-repository summary; raw YAML discarded after reduction. Validated the 5
  required fields across all 24 capabilities → zero violations; zero empty or
  template rationales.
- Resolved all 14 `peers:` references (blog → claude-shared:{shared-claude-code-skills,
  portfolio-spec-corpus}; claude-shared intra-repo + vale-style:vale-style-package;
  cookiecutter-gh-project → gh-plumbing:{reusable-github-actions-workflows,
  probot-commons-config}; kamerplanter-ha → kamerplanter:plant-lifecycle-management-system;
  terraform-github-bootstrap → gh-plumbing:probot-commons-config) → zero broken
  peers.
- Ran the semantic duplicate comparison across all 24 capabilities: the three
  domain-scoped Claude Code plugin capabilities and the kamerplanter core/HA
  pair are intentional adjacencies (Info I2/I3); gh-plumbing (reusable
  workflows) / taskfiles (Task includes) / cookiecutter-gh-project (scaffolding
  template) and terraform-github-bootstrap (Terraform config) / gh-plumbing
  (Probot commons) are adjacent-but-distinct, peer-linked where dependent → zero
  confirmed duplicates, zero `Critical`.
- Verified the 4 missing-manifest repos' `CLAUDE.md` headers: `esphome-configs`
  and `k8s-home-lab` carry valid `portfolio: excluded` markers;
  `gh-portfolio-ops` and `pre-commit-hooks` carry normal, non-excluded
  `CLAUDE.md` → the one `Warning` (W1).
- Tech-stack consistency: directly probed `portfolio/tech-stack.yml` on all 14
  non-`claude-shared` manifest-bearing repos (all HTTP 404, no forbidden
  duplication); validated the 4 declared `tech_stack:` blocks in full (group
  fields present, override targets resolve, no regroup/shadowing).
- Rendered-inventory check: confirmed `portfolio/aggregate.yml` covers all 15
  manifest-bearing members (membership parity), then ran
  `python3 scripts/docs/gen_portfolio.py` read-only →
  `git diff` on `docs/{en,de}/portfolio` and `portfolio/aggregate.yml` was
  empty (byte-fresh; CI freshness gate would pass).

### Caller follow-ups

- **W1** routed through `continuous-improvement`'s triage loop as reversible
  per-repo issues (no direct pushes): nolte/gh-portfolio-ops#1,
  nolte/pre-commit-hooks#9.
- **S1** tracked collectively in nolte/claude-shared#462 (interactive
  `tech-stack-capture` per repo; low priority).
- **S2** tracked in nolte/claude-shared#463 (operator confirms cluster peer
  topology, then per-repo manifest edits + Render).
- **I1** no action now; a future audit measures experimental stagnation once
  `since:` is added to the two capabilities.
- Render / tech-stack-rendering remain deferred (#457); no re-render was needed
  this run (I6).
