---
artifact-type: issue-orchestration-analysis
repo: nolte/claude-shared
issue: "626"
classification: spec-change
secondary-classes: [docs]
route: direct
status: draft
created: "2026-09-13"
---

# Issue Orchestration — Pre-analysis

## Issue metadata

- **Repository**: nolte/claude-shared
- **Issue**: #626 — project-structure: stale.yml extends a commons file gh-plumbing removed in 2021
- **URL**: https://github.com/nolte/claude-shared/issues/626
- **Labels**: none
- **Linked items**: #625 (fixed the Boring Cyborg `_extends` only), nolte/gh-plumbing#416
- **Prior art checked**: no open PR or roadmap item addresses it (`gh pr list --search "626 in:body"` → only #625, merged).

## Classification

- **Primary class**: spec-change
- **Secondary class(es)**: docs
- **Rationale**: `spec/project/project-structure/` SHOULDs and checks a file whose only possible source was deleted upstream; the change is to the requirement, with its skill and portfolio restatements following.

## Operator decisions (requirements gate override)

No requirement artefact exists under `project/requirements/` for this issue (established: `ls project/requirements/ | grep -i stale` returns nothing). The operator settled the open decision directly on 2026-09-13 instead of an elicitation run: **drop the stale requirement** (option 2 of #626), scope limited to this repository. Recorded here as the explicit override the requirements gate allows.

## Scope

- **In scope**: the requirement and its acceptance criterion in `spec/project/project-structure/` (EN/DE); `project-structure-apply` references and examples; the global `stale-bot` tech-stack entry and every rendered restatement of it; the `tech-stack-capture` signal map row; this repository's own `.github/stale.yml`.
- **Out of scope**: other portfolio repositories that carry a `.github/stale.yml` (established: portfolio scan on 2026-09-13 found about ten pointing at the missing `commons-stale.yml` and three with standalone Probot Stale configs) — recorded as a finding in the PR and the issue comment, not changed here. `spec/project/cookiecutter-template-authoring/` only references project-structure's MUSTs; the stale rule is a SHOULD, so no change there (established: `en.md:36`).

## Evidence

- `nolte/gh-plumbing` deleted `.github/commons-stale.yml` in `11deb9e` on 2021-12-26; it returns 404 at `develop` and at `v1.1.15` (established: `git log -- .github/commons-stale.yml`, `gh api …/contents/.github/commons-stale.yml?ref=…`).
- gh-plumbing manages staleness with `.github/workflows/stale.yaml` → `reusable-stale.yaml`, whose exempt labels are hard-coded to `bug,wip,on-hold` (established: file contents read on `develop`).
- References to remove or amend (established: `git grep -n -i "stale\.yml\|commons-stale\|stale-bot\|apps/stale"`):
  - `spec/project/project-structure/en.md:63`, `:147`; `de.md:63`, `:147`
  - `skills/project-structure-apply/references/operations.md:28`, `:36`, `:74`; `examples/01-fresh-python-app-scaffolding.md:30`, `:42`, `:60`, `:68`; `examples/02-claude-plugin-github-config.md:29`, `:43`, `:49`; `examples/03-renovate-app-not-installed.md:18`
  - `portfolio/tech-stack.yml:118-126`; `portfolio/aggregate.yml:78-81`; `docs/{en,de}/portfolio/index.md` (generated by `scripts/docs/gen_portfolio.py` from `portfolio/aggregate.yml`)
  - `skills/tech-stack-capture/references/signal-source-map.md:55`, `:95`; `skills/tech-stack-capture/examples/01-fresh-capture-empty-additions.md:25`
  - `project/portfolio.yml:126` (comment listing inherited entries)
  - `.github/stale.yml`
- `spec/portfolio/tech-stack/` knows only `active | experimental | deprecated`, and `deprecated` points at a replacement (established: `en.md:65`, `:70`). There is no replacement, so the entry is removed rather than deprecated.

## Route

- **Decision**: direct
- **Rationale**: one coherent outcome (the portfolio stops requiring and advertising a stale bot that can't be configured), one PR strand, no roadmap item.

## Work packages

### P1 — Drop the stale requirement from the project-structure spec

- **Problem statement**: `en.md:63` SHOULDs `.github/stale.yml` extending a deleted commons file and `:147` checks its presence; both mislead every consumer.
- **Acceptance criteria**: EN and DE no longer mention `stale.yml`, `commons-stale.yml` or the Stale app; the AC at `:147` checks only `.github/boring-cyborg.yml`; requirement and AC counts stay equal between EN and DE; Vale clean on EN; any restatement the edit invalidates is walked (spec skill §Update).
- **Touched files / artifacts**: `spec/project/project-structure/en.md`, `de.md`
- **Specialist**: `skill: nolte-shared:spec`
- **Depends on**: none

### P2 — Align project-structure-apply

- **Problem statement**: the skill's references and three examples scaffold and audit the dropped file.
- **Acceptance criteria**: no reference or example scaffolds, lists or audits `.github/stale.yml` or the Stale app; the Probot-app table and the `_extends` list keep the remaining files; `scripts/validate_skills.py` reports no new finding for the skill.
- **Touched files / artifacts**: `skills/project-structure-apply/references/operations.md`, `examples/01-…`, `examples/02-…`, `examples/03-…`
- **Specialist**: `nolte-claude-dev:claude-plugin-developer`
- **Depends on**: P1

### P3 — Remove the stale-bot tech-stack entry and its renderings

- **Problem statement**: the global tech stack lists `stale-bot` as `active`, so eleven members render it as an inherited, active tool.
- **Acceptance criteria**: `stale-bot` is absent from `portfolio/tech-stack.yml` and `portfolio/aggregate.yml`; `docs/{en,de}/portfolio/index.md` are regenerated with `scripts/docs/gen_portfolio.py` and contain no `stale-bot` row; the docs freshness diff in CI passes; the schema validation for the tech stack passes; `tech-stack-capture` no longer maps `.github/stale.yml` to a global entry; `project/portfolio.yml`'s comment matches.
- **Touched files / artifacts**: `portfolio/tech-stack.yml`, `portfolio/aggregate.yml`, `docs/{en,de}/portfolio/index.md`, `skills/tech-stack-capture/references/signal-source-map.md`, `skills/tech-stack-capture/examples/01-fresh-capture-empty-additions.md`, `project/portfolio.yml`
- **Specialist**: `skill: nolte-shared:portfolio-audit` for the aggregate snapshot and render (its Render operation authors `portfolio/aggregate.yml`); `nolte-claude-dev:claude-plugin-developer` for the `tech-stack-capture` references
- **Depends on**: P1

### P4 — Remove this repository's dead stale.yml

- **Problem statement**: `.github/stale.yml` extends the missing file with an invalid `@v1.1.15` suffix and configures nothing.
- **Acceptance criteria**: the file is gone; `project-structure-reviewer` run against the worktree reports no finding about it.
- **Touched files / artifacts**: `.github/stale.yml`
- **Specialist**: `skill: nolte-shared:project-structure-apply` (patch operation), verified by `nolte-shared:project-structure-reviewer`
- **Depends on**: P1

## Dependency ordering

P1 → (P2 ; P3 ; P4 independent of each other)

## Risks

- A consumer that relied on the SHOULD keeps a dead `stale.yml`; mitigation: the issue comment and PR list the affected repositories as a finding.
- Removing a global tech-stack entry could trip the portfolio audit's "declared entry not detected" class for members that override it; mitigation: `grep -rn stale-bot` over `portfolio/` and `project/` before and after, and the tech-stack schema check.
- The rendered portfolio pages are gated by the CI freshness diff; mitigation: regenerate with the committed generator, never by hand.
- No security-sensitive path is touched (no workflow, no secret, no auth code).

## Open questions

none

## Dispatch log
