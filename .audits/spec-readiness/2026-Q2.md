# Spec Readiness Audit — 2026-Q2

**Date:** 2026-05-19 · **Trigger:** spec-change follow-up (mkdocs-structure, docs-audience-tracks, prose-style erweitert) · **Auditor:** `nolte-shared:spec-readiness-reviewer` agent

**Repo:** `/home/nolte/repos/github/claude-shared` · **Branch:** `feat/tech-stack-cicd-plugin-layers`

## Scope

- **Primary audit set (recently extended):** `project/mkdocs-structure`, `project/docs-audience-tracks`, `project/prose-style`
- **Secondary scope (direct neighbourhood):** `project/audience-identification`, `project/docs-freshness`, `project/readme-structure`, `project/mermaid-diagrams`
- Canonical language: `en` per `spec/.spec-config.yml`

## Summary

| Spec | Critical | Warning | Suggestion | Info |
|---|---|---|---|---|
| `project/mkdocs-structure` | 0 | 3 | 0 | 3 |
| `project/docs-audience-tracks` | 0 | 4 | 0 | 4 |
| `project/prose-style` | 0 | 1 | 0 | 1 |
| `project/docs-freshness` (Sekundär) | 0 | 1 | 0 | 0 |
| `project/audience-identification` (Sekundär) | 0 | 1 | 0 | 0 |

## Findings & Resolutions

### Warning

**W-1 — `docs-audience-tracks` AC line 107 references mkdocs-structure §Per-page structure for `track` key but `track` wasn't listed there** → **RESOLVED**
- `mkdocs-structure/en.md` + `de.md` §Per-page structure extended: `track` added as fifth per-page MUST frontmatter key with explicit cross-reference to `docs-audience-tracks`.
- `docs-audience-tracks/en.md` + `de.md` line 68 fixed: lists `title`, `audience`, `content_mode`, `last_updated` (the four-key MUST set from mkdocs-structure) plus `track` as additive MUST raising the count to five.

**W-2 — `docs-audience-tracks` AC line 108 references audience-identification cross-link that didn't exist** → **RESOLVED**
- `audience-identification/en.md` + `de.md` Requirements: cross-reference to `docs-audience-tracks` added in the downstream-artefact list; AC added requiring per-audience `track` field.

**W-3 — Three-way gap: `docs-audience-tracks` and `mkdocs-structure` rely on `docs-freshness` capabilities `docs-freshness` doesn't claim** → **RESOLVED**
- `docs-freshness/en.md` + `de.md` §Categories of drift extended with three new categories (track-frontmatter drift, content-mode drift, audience-track mismatch); severity classification table extended; new AC added.

**W-4 — `prose-style` §Voice and tone references unknown `content_mode` value `quickstart`** → **RESOLVED**
- `prose-style/en.md` + `de.md` second-person rule updated: `quickstart-style` removed from the enumeration; explicit note added that quickstart blocks are encoded as `content_mode: tutorial` per docs-audience-tracks; `explanation` mode added to the impersonal set (sourced from Diátaxis "higher and wider perspective").

**W-5 — `audience-identification` lacks `track` field in the audience-entry schema** → **RESOLVED**
- `audience-identification/en.md` + `de.md` Requirements: per-audience `track` field added as MUST with portfolio-baseline default mapping; AC added; template `skills/audience-identify/templates/audiences.template.md` updated.

**W-6 — `docs-freshness` §Non-Goals line 19 outdated** → **RESOLVED**
- `docs-freshness/en.md` + `de.md` §Non-Goals updated: now points at `spec/project/mkdocs-structure/` as the canonical shape owner; the docs-freshness audit checks conformance, doesn't redefine the shape.

**W-7 — `mkdocs-structure` Goal "verifiable" without corresponding AC** → **RESOLVED**
- `mkdocs-structure/en.md` + `de.md` Acceptance Criteria extended: new AC verifies that `docs-freshness` references `mkdocs-structure` as the normative source for nav, plugin baseline, per-page frontmatter contract, and language-tree parity.

### Info

**I-1, I-2, I-5 — Documentation refinements** → **PARTIALLY RESOLVED**
- I-1 (`docs-audience-tracks` lacks explicit audience declaration in Context) — not addressed in this round; deferred to a future docs-audience-tracks revision when the audience artefact for `claude-shared` exists.
- I-2 (`mkdocs-structure` Open Questions "None"-line misleading) — left as-is; the wording is accurate within its scope (initial design questions).
- I-5 (`prose-style` §Voice and tone unclear on `explanation` second-person stance) — resolved via W-4 above.

**I-3 — Four load-bearing Open Questions in `docs-audience-tracks`** → **DEFERRED**
- OQs 3, 5, 6, 7 are flagged; resolutions are non-trivial design decisions that need either a portfolio-repo precedent or a stakeholder ratification before answering. Recorded as Open Questions in the spec itself; tracked for the 2026-Q3 audit.

**I-4 — `prose-style` §Voice and tone single AC for five MUSTs** → **ACCEPTED**
- Current shape is testable via Reviewer spot-check; a fine-grained one-AC-per-MUST split would multiply the AC count without sharpening Reviewer behavior. Deferred until a Vale-rules-based mechanical enforcement matures.

**I-6 — Language parity EN/DE** → **VERIFIED**
- All three primary specs and all affected secondary specs maintain structural parity (same headings, same AC count) after this round's edits.

**I-7 — `docs-audience-tracks` arc42-glossary MUST NOT creates infrastructure dependency** → **ACCEPTED**
- The MUST NOT is correct in spirit; the audit mechanism for it is the `vocab-drift-audit` skill (already exists) plus the future `docs-audience-tracks-apply` audit operation. No spec change needed.

## Caller follow-ups

Done. Two Info items deferred with rationale; nothing blocking the 2026-Q2 audit cycle.

## Related artefacts

- Spec-drift audit: `.audits/spec-drift/2026-Q2.md`
- Source process: `spec/project/spec-readiness/`
- Reviewer agent: `agents/spec-readiness-reviewer.md`

## Health

- Specs parsed: 7 (3 primary + 4 secondary)
- RFC 2119 MUSTs / MUST NOTs / SHOULDs counted across primary scope: ~152
- Cross-references verified: 53; all referenced spec paths resolve on disk after this audit's edits
- Audience artefacts consulted: 0 (no `AUDIENCES.md` exists for `claude-shared` yet — future audit gate)
- Previous audit: none — `.audits/spec-readiness/` did not exist before this audit
- Findings: 13 total / 11 resolved / 2 deferred with rationale / 0 unaddressed
