# Skills- & Agents-Sweep — 2026-07-01

Vollständiger qualitativer Review aller **53 Skills** und **46 Agents** über die drei Plugins
(`nolte-shared`, `nolte-media`, `nolte-engineering`), geprüft gegen `spec/claude/`
(`skill-management`, `agent-management`, `skill-vs-agent`, `skill-review`, `agent-review`,
`skill-agent-catalog`, `plugin-scoping`). Durchgeführt mit 12 parallelen Fable-5-Review-Agents,
gruppiert nach Domäne.

Mechanische Baseline vorab: `validate_skills.py` meldet **0 kritisch / 11 Warnungen / 1 Info** —
die untenstehenden Criticals sind allesamt vom Validator **nicht** erfasst (siehe Tooling-Lücken T1/T2).

**Bilanz:** ~17 Critical · ~55 Warning · ~70 Suggestion. Abarbeitung entlang der Cross-Cutting-Themen
(T1–T13) ist der höchste Hebel — die meisten Einzel-Findings sind Instanzen davon.

---

## A. Cross-Cutting-Themen (zuerst, höchster Hebel)

### T1 — YAML-Parser-Lücke maskiert 4 ungültige Frontmatter (CRITICAL)
`validate_skills.py` **und** `scripts/docs/gen_catalog.py` parsen das Frontmatter zeilenweise-lenient
(in gen_catalog.py:259–291 sogar dokumentiert), obwohl die Spec einen Standard-YAML-Parser fordert
(`skill-agent-catalog/en.md:127`, AC:205). Folge: 4 Skills mit unquoted `description`, die einen
Doppelpunkt mit folgendem Leerzeichen enthält, brechen mit PyYAML `ScannerError` — und der Claude-Code-Runtime-Loader (js-yaml) lehnt dieselben
Plain-Scalars evtl. ab → **Skills laden im Consumer u. U. gar nicht**.
- [ ] `skills/sprint-plan/SKILL.md:3` — description quoten (enthält `` `status: planned` ``)
- [ ] `skills/cookiecutter-template-manage/SKILL.md:2` — description quoten (`template: scaffolds…`)
- [ ] `plugins/nolte-engineering/skills/api-error-check/SKILL.md:3` — description quoten (`Read-only: reports…`)
- [ ] `plugins/nolte-engineering/skills/test-cycle-orchestrate/SKILL.md:3` — description quoten (`integrity rules: a regression…`)
- [ ] **Tooling:** PyYAML-`safe_load`-Parse-Check in `validate_skills.py` aufnehmen (fängt Regression künftig).

### T2 — Token-Cap-Lücke maskiert 5 Skills über dem 5.000-Token-MUST (CRITICAL)
`validate_skills.py` prüft `description`-Zeichen/Zeilen, aber **kein Body-Token-Estimate**. Nach Compaction
wird alles jenseits 5.000 Token (≈ 4 Zeichen/Token) beim Re-Attach still abgeschnitten — meist trifft es
Hard rules/Gotchas am Ende.
- [ ] `skills/pull-request-merge/SKILL.md` (~6.100 T) — Step-7b-Runbook, Label-Tabelle, 7c → `references/`
- [ ] `skills/lektorat-apply/SKILL.md` (~5.613 T) — Operations-Details + Output-Baum → `references/operations.md`
- [ ] `skills/blog-author/SKILL.md` (~6.199 T) — Outputs-Layout, Step-4/5-Prosa, Gotchas → `references/`
- [ ] `skills/tech-stack-capture/SKILL.md` (~5.490 T) — Step-8-Checkliste + Step-5/6-Mapping → bestehende `references/signal-source-map.md`
- [ ] `skills/skill-agent-catalog-apply/SKILL.md` (~6.500 T) — Hook-Liste (§2.3) + Audit-Tabelle → `references/`
- [ ] Grenzwertig (im Auge behalten): `pull-request-create` (~5.020), `portfolio-inflight-triage` (~5.200 — obwohl Referenz-Split das verhindern sollte), `issue-orchestrate` (~4.770), `dependency-audit` (~4.843)
- [ ] **Tooling:** 4-Zeichen-Heuristik als Check (Warn ≥ 4.500, Crit ≥ 5.000 est. Token).

### T3 — Imperativ- statt Third-Person-Descriptions (Validator prüft nur Pronomen)
`skill-management/en.md:46` fordert 3. Person; der Validator fängt nur `I/you/we`, nicht die Verbform.
Mind. 12 betroffen — **ein Sammel-PR** + Opener-Verbform-Check im Validator ergänzen:
- [ ] Skills: `roadmap-init` (Scaffold→Scaffolds), `sprint-plan` (Create→Creates), `sprint-execute` (Drive→Drives), `sprint-review` (Close→Closes), `feature-decompose` (Decompose→Decomposes), `spec` (Create, translate…→Creates…), `vocab-drift-audit` (Audit→Audits), `skill-review` (Review→Reviews), `agent-review` (Review→Reviews), `github-issue-templates-apply` (Apply→Applies), `dependency-audit` (Scan→Scans), `license-check` (Run→Runs), `quality-gate` (Run→Runs), `test-pyramid-check` (Audit→Audits)

### T4 — phase: `review` vs `quality`-Drift im Audit/Scanner/Drift-Cluster
Katalog-Vokabular: `review` = PR-Weg nach develop; `quality` = „audits, scans, drift detection".
Audit-/Scan-/Drift-Artefakte tragen fälschlich `review` und werden im Katalog von ihren Dispatchern getrennt.
Als **einheitliche Regel** festlegen („Audit/Scan/Drift → quality; Executor erbt Phase seines Dispatchers"):
- [ ] Agents → `quality`: `vocab-drift-scanner`, `lektorat-scanner`, `portfolio-manifest-collector`, `diagram-opportunity-reviewer`, `code-security-reviewer`, `dependency-audit-scanner`, `license-check-scanner`; `webview-ui-expert` (design→quality)
- [ ] Skills → `quality`: `spec-drift-audit`, `portfolio-inflight-triage`
- [ ] Bewusst-Entscheidung dokumentieren für die Review-Familie `skill-review`/`agent-review`/`skills-agents-sweep` (nur im Verbund ändern, sonst reißt der Cluster)

### T5 — Spec-Fallback-Idiom „plugin install path" ohne `${CLAUDE_PLUGIN_ROOT}/spec/` (Consumer-Portabilität)
13+ Skills sagen „read it at runtime from the plugin install path", ohne den auflösbaren Anker zu nennen;
in Consumer-Repos ist das für das Modell nicht zuverlässig auflösbar (load-bearing — Skills stoppen sonst).
`spec` und `issue-orchestrate` machen es bereits korrekt vor. **Ein mechanical-sweep-PR** auf
`${CLAUDE_PLUGIN_ROOT}/spec/<topic>/<canonical_language>.md`:
- [ ] `docs-dry-refactor`, `docs-audience-tracks-apply`, `mkdocs-structure-apply`, `mermaid-diagrams-apply`, `readme-structure-apply`, `project-structure-apply`, `skill-agent-catalog-apply`, `yaml-json-schema`, `github-issue-templates-apply`, `workflow-health-triage`, `continuous-improvement-triage`, `release-notes-curate`
- [ ] Hard-Stop in `feature-decompose:47` durch denselben Fallback ersetzen (bricht sonst bei Inherited-Spec-Layer)

### T6 — Dead Permissions (`Grep`/`Glob` deklariert, nie genutzt)
`agent-review/en.md:78`: deklariert-aber-ungenutzt = Warning. **Sweep-Fix** (Nutzung im Body verankern
oder Tool streichen):
- [ ] `lektorat-scanner`, `link-rot-scanner` (Glob+Grep), `project-structure-reviewer`, `quality-gate-enforcer`, `audience-doc-author`, `claude-plugin-developer` (Grep)

### T7 — Bash-Justification-Missbrauch & falsche „side-effect-free"-Zusagen
- [ ] `audience-doc-author`, `claude-plugin-developer`: „## Read-only Bash justification" auf **Write-Agents** fehlangewendet (Ausnahme gilt nur read-only) → neutrales Bash-Begründungs-Heading etablieren **oder** Spec um Write-Agent-Fall erweitern
- [ ] dieselben zwei: `task lint` als „side-effect-free" deklariert — falsch, `pre-commit run --all-files` enthält Auto-Fixer (trailing-whitespace etc.) → Formulierung zentral korrigieren
- [ ] `test-result-analyzer` (CRITICAL): read-only-Agent mit `Bash` **ohne** `## Read-only Bash justification`-Sektion (Begründung steht nur unter „Writes vs researches") → Sektion ergänzen; Test-Re-Run überschreitet side-effect-free-Hülle → explizit begründen oder an `quality-gate` delegieren
- [ ] `vocab-drift-scanner`: Justification listet 4 Kommandos, Body ruft zusätzlich `gh --version`/`gh auth status` → aufnehmen
- [ ] `license-check-scanner`: „installs nothing" falsch — `npx --yes`/`uvx` installieren in Caches → prüfen/skippen oder als Ausnahme benennen
- [ ] `dependency-audit-scanner`: Justification verschweigt `cargo audit`/`govulncheck`-Cache-Fetches → benennen
- [ ] `link-rot-scanner`: `check_links.py` schreibt Cache unter `.audits/link-validation/.cache/` → `--no-cache` als Default oder Ausnahme markieren

### T8 — Output-vor-Prozedur-Reihenfolge (~21 Agents, Template-Frage)
`agent-review/en.md:109` (Rolle → Output-Format → Arbeitsweise) wird hausweit verletzt (Output erst in
Phase 4/„Report"). Es ist das etablierte Haus-Template aller Agents. **Einmalige Entscheidung** statt
21 Einzel-Patches: Template anpassen **oder** Abweichung als dokumentierte Konvention in `agent-management` verankern.

### T9 — `dont_use_when`-Füll-Redirects auf `project-structure-apply`
Das Schema erzwingt eine *auflösbare* Alternative, nicht deren *Passung* — der Katalog rendert daraus
falsche Handlungsempfehlungen. Streichen oder passendes Ziel:
- [ ] `dependency-audit:13` (Upgrades), `quality-gate:15` (mkdocs build), `license-check:15` (Lizenzwahl), `tech-stack-capture:13` (global manifest → widerspricht eigenem Body), `frontend-usability-optimizer:19` (Gate-Lauf → korrekt: `quality-gate`-Skill statt `quality-gate-enforcer`)
- [ ] ggf. Spec-Follow-up: `alternative` optional erlauben, wenn kein passendes Ziel existiert

### T10 — Asymmetrische Peer-Verweise (`see_also`/`dont_use_when` nur einseitig)
`agent-review/en.md:103`: bei klarer Überschneidung fehlender Rück-Trigger = Warning. Je Paar Rückrichtung ergänzen:
- [ ] `e2e-result-reviewer` ↔ `test-result-analyzer`
- [ ] `test-case-extractor` → `e2e-test-generator`/`test-cycle-orchestrate`
- [ ] `test-code-adapter` ↔ `fullstack-developer`
- [ ] `spec-readiness-reviewer` ↔ `security-requirements-reviewer`
- [ ] `tech-stack-drift-reviewer` ↔ `tech-stack-fitness-reviewer`
- [ ] `mermaid-diagram-reviewer` ↔ `diagram-opportunity-reviewer` (Twin)
- [ ] `dependency-audit-scanner` ↔ `license-check-scanner`
- [ ] `webview-ui-expert` ↔ `frontend-usability-optimizer` / `i18n-completeness-checker`
- [ ] `lektorat-scanner` ↔ `prose-vale-curator`; `vocab-drift-audit` ↔ `prose-vale-curator`
- [ ] `skills-agents-sweep` ↔ `skill-review`/`agent-review` (Meta-Familie); `lektorat-apply` → `lektorat-auto-revise`
- [ ] `code-security-reviewer` → `security-requirements-reviewer` (see_also); `fullstack-developer` → `frontend-usability-optimizer`; `i18n-completeness-checker`/`graphic-prompt-generator`/`cookiecutter-template-author`: Peer namentlich in description nennen

### T11 — Fehlende `examples/`-Evaluationsszenarien (SHOULD ≥3)
- [ ] Skills ohne: `blog-author`, `requirements-elicit`, `working-copy-start`, `lektorat-auto-revise`, `docs-dry-refactor`, `docs-audience-tracks-apply`, `readme-structure-apply`, `gemini-image-handoff`, `api-error-check`, `test-cycle-orchestrate`, `test-pyramid-check`, `webview-ui-optimize`
- [ ] Nur 2 (→3): `blog-author-trigger`
- [ ] Orphan-Examples **ohne Load-Trigger** (existieren, aber unerreichbar): `portfolio-inflight-triage` (04/05/06), `skill-review` (01–03), `backstage-catalog-generate` (01–03) → Trigger-Zeilen ergänzen oder löschen

### T12 — Heading-Form-Verstöße (`### N. <verb>`)
- [ ] CRITICAL-nah (MUST): `test-pyramid-check` (`## Step N —`, zusätzlich H2), `backstage-catalog-generate` (`### Generate a…`), `working-copy-start` (`### Start a working copy`)
- [ ] Warning: `test-cycle-orchestrate` (`### Phase 1 —`), `pull-request-merge` (`### 7c.`), `release-publish-trigger` (`### 1b.`), `vocab-drift-audit` (Fettdruck-Liste), `sprint-execute` (Body nennt „Operation A/C/D", Headings nummeriert 1–5)

### T13 — Cross-Plugin-Redirects zur Laufzeit (nolte-shared → nolte-engineering)
Im In-Repo-Katalog via `docs/catalog-sources.yml` auflösbar, aber bei Nur-`nolte-shared`-Installation
tote Redirects. **Konventionsentscheidung** (Fußnote „requires nolte-engineering" im Body):
- [ ] `security-requirements-reviewer`, `tech-stack-drift-reviewer`, `tech-stack-fitness-reviewer`, `gdpr-data-protection-reviewer`, `quality-gate-enforcer` (+ Plugin-Platzierung von `quality-gate-enforcer` prüfen)

---

## B. Verbleibende Criticals (artefakt-spezifisch, nicht in T1/T2)

- [ ] **`skills/sprint-plan/SKILL.md:150`** — Gotcha instruiert **verbotenen** Sprint-Frontmatter-Key `verifies_sprint_value` (Spec: 9 Felder + optional `cancelled_reason`, Lints flaggen unknown keys); eigenes Template hat ihn nicht → Verifier ist feature-seitig, kein Sprint-Key
- [ ] **`skills/skill-agent-catalog-apply/SKILL.md`** — Spec-Drift: verdrahtet nur die `mkdocs-gen-files`-Surface; Spec definiert **3 Surfaces**, setzt `on_pre_build` als Default und **verbietet** gen-files bei `mkdocs-static-i18n` folder-mode (Seiten werden silently gedroppt). Eigenes Repo ist bereits auf `hooks:` migriert → Audit meldet falschen Drift, Scaffold in i18n-Repo erzeugt leeren Katalog. Auch Hard rule „Never commit generated catalog" widerspricht Committed-catalog-Fallback der Spec
- [ ] **`plugins/nolte-media/skills/image-generate/SKILL.md`** — kein `spec/`-Anker im Body trotz bindender `spec/tools/image-generation/` → „Implements …, spec wins"-Zeile ergänzen
- [ ] **`plugins/nolte-engineering/skills/webview-ui-optimize/SKILL.md`** — Consumer-Fallbacks unerreichbar + falsches Plugin: (a) Spec-Fallback nennt `nolte-shared`, Skill shippt in `nolte-engineering` (dessen `${CLAUDE_PLUGIN_ROOT}` hat kein `spec/`); (b) Research-Notes `.audits/webview-ui-expert/` nur im Hub-Root, Hard rule 8 macht ohne sie jedes Finding unmöglich; (c) repo-relativer `agents/…`-Verweis → Spec+Notes unter `references/` bündeln, Plugin-Name korrigieren
- [ ] **`plugins/nolte-engineering/agents/webview-ui-expert.md`** — downstream nicht funktionsfähig: verwirft Findings ohne Research-Anchor auf nicht-mitgelieferte Notes → bei erlaubter Direkt-Invocation in Consumer stillschweigend leere Reports; Spec-Fallback ohne auflösbaren Pfad → Missing-Handling definieren oder Notes plugin-mitliefern

---

## C. Warnings & Suggestions je Artefakt (Anhang, abarbeitbar)

### Planungs-Familie (Batch A)
- `mission-define`: [W] mvp:true-Pflege → `roadmap-plan` statt `roadmap-refine` (Zeile 95/102)
- `mission-revise`: [W] Write-Authority Item-Status → `roadmap-plan` §4 statt `roadmap-refine` (Zeile 136)
- `roadmap-plan`: [W] Gotcha Terminal-Sprint-Lint-Owner falsch (→ Spec: `sprint-plan`); [S] Tag `planning` isoliert Cluster; [S] Multi-model-Sektion pinnt Modellversionen (veralten)
- `roadmap-refine`: [S] `use_when` nennt nicht-existente Detailstufe „medium" (Vokabular fine/coarse/backlog); [S] interne Ownership-Route wacklig (2.4 vs Hard rule)
- `sprint-plan`: [W] Step 5 setzt `feature.sprint` ohne Re-Run-Route-back zu `feature-decompose`
- `sprint-execute`: [W] Op 4.3 dito ohne Route-back; [S] Gotcha „skill…hook pending" (Skills haben keinen Hook-Status)
- `sprint-review`: [W] §7.3 macht `target_sprint`-Retargeting selbst (Spec weist es `sprint-plan` zu); [S] Tag `scaffolding` unpassend für Closure-Skill
- **Querschnitt:** Terminal-Sprint-Lint-Ownership 3–4fach widersprüchlich → **einen Owner in `spec/project/roadmap/` festschreiben**, alle 4 Skills ausrichten; Trigger-Kollision „promote roadmap item to fine" (`roadmap-plan` vs `roadmap-refine`) ohne Abgrenzung in Metadaten; Cluster-Tag-Fragmentierung

### Spec/Docs/Prose (Batch B)
- `spec`: [W] Verweis `scripts/validate_schemas.py` mit unklarer Ausführungsintention (fehlt im Consumer); [S] 190 Zeilen > Soft-Target; [S] Operationsnamen außerhalb Lifecycle-Vokabular
- `lektorat-apply`: [S] `see_also` nennt `lektorat-auto-revise` nicht; [S] Weiterleitung auf `docs-multilingual-authoring` als wäre es Artefakt (ist Spec)
- `lektorat-auto-revise`: [W] keine Gotchas-Sektion trotz nicht-offensichtlicher Umgebung (Finding-IDs, LIX, Routing)
- `docs-dry-refactor`: [S] Operation `scan`→`audit`; description 1021/1024 ohne Headroom
- `docs-audience-tracks-apply`: [S] Operation `migrate` als „greenfield" mislabeled (ist brownfield)
- `mkdocs-structure-apply`: [W] „future docs-dry-refactor skill" 3× — Skill existiert längst (time-sensitive falsch); [S] description exakt 1024; Spec-Area-Liste 11 vs 9 inkonsistent
- `mermaid-diagrams-apply`: [W] widersprüchliches Routing bei „MkDocs fehlt" (Body→`project-structure-apply`, dont_use_when→`mkdocs-structure-apply`); [W] Operation 4 dupliziert inline den Scan von Agent `mermaid-diagram-reviewer` (Hybrid-Muster nicht genutzt); [S] Operationsüberschriften zweiwortig; 182 Zeilen
- **Querschnitt:** 4 `-apply`-Skills haben „Tool selection rationale"-Abschnitt, aber **kein `allowed-tools`-Frontmatter** (dokumentiert nicht-existenten Grant) → `allowed-tools` deklarieren oder „Tools used" (auch `webview-ui-optimize` betroffen)

### PR/Release/Portfolio (Batch C)
- `pull-request-create`: [S] description ohne Abgrenzung zu `pull-request-merge`
- `pull-request-merge`: [S] Body nennt `workflow-health-triage`-Skill nicht (nur Spec), see_also fehlt
- `release-notes-curate`: [S] vager Plugin-Install-Pfad → `${CLAUDE_PLUGIN_ROOT}/spec/` benennen
- `portfolio-audit`: [W] Inkonsistenz 3 vs 4 Operationen (Rationale/Detection nicht nachgezogen); [S] Ghost-Anchor §„Coherent units"
- `workflow-health-triage`: [W] `## Old patterns`-H2 zwischen Step 3 und 4 zerreißt Operations-Outline; [W] Precondition ohne Plugin-Fallback (→ T5)
- `continuous-improvement-triage`: [W] hardcodet `en.md` statt `<canonical_language>.md` + kein Fallback; [S] Tag `triage` off-vocab; German-trigger-Sektion dupliziert description; fehlt User-language-policy
- **Querschnitt:** Wait-Mode-Vertrag doppelt gepflegt (`pull-request-merge` `references/wait-mode.md` vs `release-publish-trigger` inline) → Drift-Risiko, geteilte Referenz

### Meta/Plugin/Catalog (Batch D)
- `skill-management`: [W] description/Example versprechen Kette zu `claude-plugin-developer`, Body hat keinen Dispatch-Schritt
- `skill-review`: [W] Body fügt author-time triangulation gate ein, den Spec-Reihenfolge/Anker nicht kennt → Spec ergänzen oder Gate raus
- `skills-agents-sweep`: [W] `templates/sweep-report.template.md` 259 Zeilen ohne ToC; [S] Template hardcodet „skills-ref not provisioned" statt Platzhalter
- `permission-allowlist-maintain`: [W] zeitgebundene „Q2-2026 drift check"-Historie inline → `## Old patterns` oder raus; [S] kein dont_use_when/see_also
- `yaml-json-schema`: [S] Abgrenzungen nur in description, kein maschinenlesbares dont_use_when/see_also
- `github-issue-templates-apply`: [W] `references/project-type-fields.md` 349 Zeilen ohne ToC
- `backstage-catalog-generate`: [S] „resume not applicable" trotz 6 Phasen + Gate; phase:build vs Cluster
- `cookiecutter-template-manage`: [S] hardcodet `en.md` statt `<canonical_language>`

### restliche nolte-shared-Skills (Batch E)
- `issue-orchestrate`: [W] `disable-model-invocation:true` widerspricht description-Discovery-Vertrag; kein Body-Rationale; kein Mid-flow-Dispatcher → Flag entfernen oder auf Explizit-Invocation umstellen + begründen
- `audience-identify` (Zeile 100) + `requirements-elicit` (Zeile 114): [W] **identischer Copy-Paste-Zitatfehler** `agent-management` statt `skill-management` für English-only-Regel → gemeinsamer Sweep
- `tech-stack-capture`: [W] `dont_use_when[0]`→`portfolio-audit` fehlleitend (Body sagt hand-curated PR)
- `vocab-drift-audit`: [W] kein dont_use_when (prose-vale-curator-Grenze); Operations als Fettdruck statt `### N.`
- `working-copy-start`: [W] kein Gotchas trotz nicht-offensichtlicher Umgebung (NOLTE_WORKTREE_ROOT, Guards, Subagent-Pfade); description 1017/1024
- `project-structure-apply`: [S] Spec-Fallback unbestimmt (→ T5)
- **Querschnitt:** uneinheitliche German-Trigger-Konvention (6/9)

### nolte-shared-Agents Teil 1–3 (Batches G/H/I)
- `audience-review`: [S] Verweis auf `review-plan` §90 (Zeilennummer statt stabiler Überschrift)
- `claude-plugin-developer`: [S] Reserved-token-Rationale verweist auf gelöschten `.audits`-Pfad → auf Git-History umformulieren
- `cookiecutter-template-author`: [W] description ohne positive Trigger + ohne „Don't use"-Abgrenzung trotz sicherer Überschneidung mit `cookiecutter-template-manage`
- `diagram-opportunity-reviewer`: [W] Body 371 Zeilen (~2× Soft-Limit), dupliziert Spec; [W] interner Widerspruch §Field-semantics „unless overridden" vs §Inputs/§Volume-control (Overrides verboten); [S] description „persisted under .audits" impliziert Agent schreibt selbst
- `docs-freshness-checker`: [W] Bash-Allowlist nennt nicht-existentes `--no-cache`-Flag von `check_links.py` → argparse-Fehler; [W] Body 319 Zeilen; [S] Hard rule nennt `MultiEdit` (kein Vokabular) → `NotebookEdit`
- `prose-vale-curator`: [W] verspricht neue `accept.txt` anzulegen, aber kein `Write` (Edit kann nicht erzeugen) → Write ergänzen oder report-only; [W] §Voice-and-tone-Spot-Check dupliziert D4-Detektion von `lektorat-scanner`; [S] Verweis auf nicht-existentes `MultiEdit`
- `portfolio-inflight-collector`: [W] Body 261 Zeilen (gh-Allowlist doppelt); [S] §Inputs verweist auf nicht-existentes `Notes`-Feld
- `portfolio-manifest-collector`: [S] gleiches `Notes`-Feld-Drift (Copy-Paste-Template)
- `quality-gate-enforcer`: [S] Rollen-Nomen `enforcer` vs read-only-Auditor (ggf. später `quality-gate-wiring-reviewer`)
- `security-requirements-reviewer`: [W] Body 254 Zeilen > Soft-Limit
- `spec-readiness-reviewer`: [W] Body 304 Zeilen (~50 % über Limit); [S] hardcodet `en.md` (Zeile 74)
- `tech-stack-drift-reviewer`: [S] Surface-4 formuliert git-log konditional, obwohl Agent nie `Bash` hat → unbedingt formulieren
- `tech-stack-fitness-reviewer`: [S] Body 208 Zeilen knapp über Limit
- **Querschnitt:** zwei Severity-Konventionen in derselben Reviewer-Familie (3-stufig lowercase vs kanonische Title-Case aus `review-plan`) → vereinheitlichen; `tools`-Formatdrift (YAML-Liste vs Komma-String); **Spec-Lücke:** `agent-management` regelt für read-only nur `Bash`, nicht `WebSearch`/`WebFetch`-Datenabfluss → „network-read justification"-Klausel ergänzen

### Media + Test-Agents (Batch J/K)
- `graphic-prompt-generator`: [W] Negativ-Trigger nennt Peer `image-generate` nicht; [S] Tag `prose` unpassend → media/design
- `png-to-transparent-svg`: [W] Body ~261 Zeilen > Soft-Limit; [S] **Spec-Drift auf Spec-Seite**: `spec/claude/png-to-transparent-svg/` (EN+DE) verortet den Agent unter `agents/` (Root), er liegt aber in `plugins/nolte-media/agents/` → Spec-Pfad korrigieren; [S] Tag `scaffolding` unpassend
- `e2e-result-reviewer`: [W] einseitige Abgrenzung zu `test-result-analyzer`; [S] Tag `audience` unpassend
- `test-case-extractor`: [W] Negativ-Trigger nennt keinen existierenden Peer; see_also asymmetrisch
- `test-code-adapter`: [W] Überschneidung mit `fullstack-developer` ohne gegenseitigen Negativ-Trigger
- **Querschnitt J:** alle 6 Test-Agents + `graphic-prompt-generator` verlangen Phase-1-Spec-Read ohne Consumer-Fallback (Plugins shippen kein `spec/`) → je Agent „when spec absent, apply inlined requirements"; `Edit`-Grant der 3 Generatoren grenzwertig dead → begründen/streichen; Reviewer-Review+Repair-Konflation begründet (konform), aber Verb-basierte Read-only-Heuristik erzeugt künftig False-Positive-Criticals → Klassifikations-Satz aufnehmen

### Engineering Non-Test (Batch L)
- `code-security-reviewer`: [S] phase:review→quality; see_also fehlt `security-requirements-reviewer`
- `dependency-audit-scanner`: [S] Discovery via Bash-`find` statt `Glob`/`Grep` (dedizierte Tools bevorzugen)
- `frontend-usability-optimizer`: [S] Tag `review` Misfit für schreibenden Optimierer; keine Abgrenzung zu `webview-ui-optimize`-Skill
- `fullstack-developer`: [S] tags `[scaffolding, quality-gate]` semantisch falsch für Feature-Impl
- `dependency-audit` (Skill): [W] Arbeitsteilung Skill↔Agent widersprüchlich (Step 0 dispatcht Scanner, Steps 1–3 dann inline dasselbe) → als Kontrakt/Fallback markieren
- **Querschnitt L:** Starter-Tag `dependency` ungenutzt bei `dependency-audit-scanner`/`license-check-scanner`

---

## D. Positivbefunde (kein Handlungsbedarf, verifiziert)
- Alle 99 Artefakte tragen das exakte Rationale-Heading (`## Why this is a skill/agent, not …`) mit benannter Counter-Dimension.
- Alle `description`s ≤ 1024 Zeichen, Third-Person-Pronomen sauber (nur Verbform-Drift, siehe T3).
- **Kein einziger toter `see_also`/`dont_use_when`-Verweis** — alle Ziele lösen gegen `skills/`, `agents/`, beide `plugins/*/`-Roots auf (cross-plugin via `docs/catalog-sources.yml`).
- Alle referenzierten Specs, Templates, Scripts und `${CLAUDE_PLUGIN_ROOT}`-Wirings (image-generate vorbildlich) existieren.
- Resumable-Wiring dort, wo beansprucht, konsistent (Frontmatter + §Resumability + description).
- Test-Zyklus-Familie und Security-Triade sind inhaltlich sauber geschnitten (nur Verweis-Asymmetrien, keine Kapabilitäts-Duplikate).

---

*Erzeugt 2026-07-01 via 12 parallele Fable-5-Review-Agents. Roh-Findings pro Batch im Session-Scratchpad.
Nach Abarbeitung dieses Plans: Datei löschen — die Git-History bleibt als Audit-Trail (Konvention aus `spec/claude/review-plan/`).*
