---
audit-type: docs-freshness
repo-revision: dac0375
created: 2026-09-13
scope: full
status: closed
---

# Documentation Freshness Report — 2026-Q3

## Scope

- Date: 2026-09-13
- Trigger: first recorded quarterly run, for calendar quarter 2026-Q3 (the 2026-Q2 and 2026-Q3 obligations had never run), remediation of spec-drift-audit 2026-Q4 finding F65 (#587)
- Git revision: `dac0375f8e7aa201496253940c64fa003e15dcd0`
- Performed by: the `docs-freshness-checker` agent, dispatched read-only against the primary checkout on `develop`
- Language trees: `docs/de` (default) and `docs/en`, `mkdocs-static-i18n` with `docs_structure: folder`
- Phases run: inventory, language parity, content-staleness spot-check, internal-link rot and cross-tree reference rot (delegated to `python3 scripts/check_links.py --offline --internal --cross-tree --format json`), ADR hygiene (not applicable, no ADR tree), Mermaid diagram-source drift, track frontmatter, content-mode drift, content-mode mixing spot-check, audience-track consistency, i18n base-URL drift, stale markers
- Out of scope by construction: `docs/<lang>/skills/`, `docs/<lang>/agents/` and `docs/<lang>/references/tags.md`, which the `on_pre_build` catalog hook generates and git ignores. Every check ran over the 28 hand-authored tracked pages per language tree, 56 in total.

## Summary

- Critical: 0
- Warning: 0
- Suggestion: 0
- Info: 1

**Verdict:** the tracked documentation is current and internally consistent: no broken links, no parity gaps, no stale-content deltas, no Mermaid drift, no invalid `track` or `content_mode` values, and no audience-track contradictions. One translation-level frontmatter slip was found and fixed in the same change.

| Category | Critical | Warning | Info |
|---|---|---|---|
| Internal-link rot | 0 | 0 | 0 |
| Cross-tree reference rot | 0 | 0 | 0 |
| Language-parity gap | 0 | 0 | 0 |
| Content-staleness delta | 0 | 0 | 0 |
| Mermaid diagram-source drift | 0 | 0 | 0 |
| ADR index drift | n/a | n/a | n/a |
| ADR status hygiene | n/a | n/a | n/a |
| Stale markers | 0 | 0 | 0 |
| Track-frontmatter drift | 0 | 0 | 0 |
| Content-mode drift | 0 | 0 | 0 |
| Audience-track mismatch | 0 | 0 | 0 |
| i18n base-URL drift | 0 | 0 | 0 |
| **Total** | **0** | **0** | **1** |

## Findings

### Info

- [x] **Cross-language audience frontmatter divergence.** `docs/de/using.md:3` declares `audience: [downstream-user, downstream-end-user]`, `docs/en/using.md:3` only `audience: [downstream-user]`, while both bodies are materially equivalent and share `last_updated: 2026-05-30`. Both IDs map to `track: user-docs` in `AUDIENCES.md`, so the audience-track check itself passed.
  - Where: `docs/en/using.md:3`
  - Fix: align the English list with the German one; `downstream-end-user` exists in `AUDIENCES.md` as the indirect `user-docs` audience.
  - Verify: `sed -n 3p docs/en/using.md docs/de/using.md` shows the same list.

## Processing log

- 2026-09-13 — audit run by `docs-freshness-checker` at `dac0375`; the agent's own method notes: parity computed from `git ls-files` over both trees (28/28 paths), two `derived` Mermaid blocks in `docs/*/portfolio/index.md` checked against `portfolio/aggregate.yml` (source older than the page, no drift), two "placeholder" hits in `docs/en/guides/skill-management.md:40` and `docs/en/guides/contributing.md:75` read in context and excluded as prose, `site_url` matches the GitHub Pages URL.
- 2026-09-13 — Info finding fixed: `docs/en/using.md` gains `downstream-end-user` (#587).
