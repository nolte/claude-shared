# yaml-language-server: $schema=../schemas/spec-config-v1.0.schema.yaml
#
# Per-repository spec configuration. Schema: schemas/spec-config-v1.0.schema.yaml
# (validated by scripts/validate_schemas.py). This is the hub root: it carries no
# inherits: key, so inheritance resolution terminates here per
# spec/project/portfolio-inherited-spec-layer/ §"Shallow chain and root boundary".
canonical_language: en
languages: [en, de]
spec_root: spec
